# 專案現況

更新日期：2026-08-22（第 648 輪：**物品名稱整組是錯的模型，而且看不出來**（spec 1178）——★★★ 名稱**完全不含物品類別**：三個 byte（`+2Fh`／`+30h`／`+31h`）各自去查 `DS:1040h + N×15h` 那張 255 筆的 Pascal 字串表，串起來就是全名；remake 先前拿 `item_type_XX` 當基底再貼成分，靠一條「和基底相同就不加」的守衛才沒穿幫，遇到類別名與中心語不同的（`Miscellaneous` 的伊奧恩石）就露餡；★★ **語系檔那 54 筆有一半編號是錯的**——第 260 輪照一份 reference 實作的陣列填，`24h`..`3Bh` 剛好對，`9Fh` 之後整段偏 3（檔裡 `item_name_9F` 寫 `+1`，原作 `+1` 是 `A2h`），`3Ch`..`3Eh` 是拿物品類別的編號填的 ⇒ **玩家看到的加值是錯的數字**；★ 基底怎麼證的：指令在 `overlay-24:063Bh`（`mul 15h` ＋ `add 1040h`），資料側再把 253 件物品整批組回去比字串——**208 件逐字元相同，基底位移一格或兩格都是 0 件**；★★ **`+32h`（Plus）與 `+36h`（Cursed）不進名字**：加值走名稱編號 `A2h`..`A6h` 那幾個**可以被藏起來**的成分，原版 72 件帶 Plus 的物品有 54 件從頭到尾不顯示加值，而 `+35h` 不是布林是三個 bit（出廠 `6h` ＝ 未鑑定）；⚠ 紀錄的 `+00h..+29h` 是**未鑑定名的快取**（`Deck`／`Beaker`），組名時會被覆寫，不是權威資料；`item_name_XX` 由 54 筆補成 252 筆，126 個用到的編號**缺譯 0**、115 種相異名稱逐件列在 `docs/audit/item-names.md`；翻譯層加一條「三成分而中間是連接詞就前後對調」讓 `Wand of Fireballs` 讀成「火球之魔杖」；第 647 輪：**逐格實測的「沒演出來」現在自動分得出成因**（spec 1177）——★★ 那先前是一個混在一起的桶子：**remake 沒接**與**缺前置劇情**處置相反，而分辨的成本是一格一格反組譯守衛；改法是把守衛的 `COMPARE <格子> <值> / IF <op> / EXIT` 拆成結構，把格子設成**避開 `EXIT`** 的值再站一次；★★ **不是「設成比對的值」就好**——`COMPARE 4C01 01 / IF >= / EXIT` 設成 1 反而保證離開，第一版忽略運算子只多救回 3 格，補上之後是 5 格；全遊戲 250 個索引：**中文 235 → 240、沒演出來 15 → 10、落回原文仍是 0**；★ 剩下那 10 格逐格看過**都是盤點的限制不是缺口**，四種形狀（守衛比移動前快照、旗標被該段前導覆蓋、擷取到的守衛跨過真正的處理常式、處理常式本來就不講話）寫進報表抬頭，下一個人不必重推；第 646 輪：**SAVGAM 的第四塊（ECL 位元組碼）終於寫得回去**（spec 1176）——★★ 匯出原版格式時前三塊都從 session 重編，第四塊卻只是把匯入來源那一份原封抄過去：**新開的遊戲沒有匯入來源，於是匯出的存檔那一塊整塊是 0**；匯入之後換過段再存檔，寫出去的也是匯入當時那一段的碼；★ 這一塊**不能沿用前三塊的編碼**——前三塊刻意跳過值為 0 的格子（原作分不出「寫過 0」與「沒寫過」），而位元組碼的 `00h` 是真的指令（`EXIT`），跳過會把程式打洞，所以第四塊有自己一對 codec、**0 照收**；★★ 順帶量出一件事：`cmd/ecl-cell-refs -range 8000-9DFF` 掃出 **3,012 處而 opcode 只有三種**（`GOTO`／`GOSUB` 的跳躍目標與 `GETTABLE` 的表），**沒有任何一處把值寫進區 3** ⇒ 「自我修改的碼會遺失」在這一款不成立——⚠ 但那是**這一款**的量測不是引擎的性質（`GETTABLE` 的表就住在位元組碼裡，表示引擎本來就當它是可定址記憶體），所以照通用做法存回去，不靠「反正沒人寫」這個前提；第 645 輪：**換裝重投影的漏欄位改成擋得住整類**，並把大／小切換的邊界讀完（spec 1175 增補）——★★ `TestReplaceFighterEquipmentCarriesEveryEquipmentDerivedField` 拿同一個角色**帶武器與不帶武器各投影一次**，有差的欄位就是裝備衍生的：名單由行為算出來，新增欄位不必記得更新任何清單；**突變驗過**——拿掉 `ReplaceFighterEquipment` 裡任何一行搬運都會紅；★ 整個戰鬥 overlay **只有一個**大／小切換點（另外兩處讀 `+151h` 的是背刺與射程），所以切換與近戰／遠程無關，第 644 輪那句「遠程沒讀」的保留可以收掉；⚠ 原作的 `+1A2h` 是**調整不是指派**（`− 小加值 ＋ 大加值`）⇒ 連續兩次大型目標攻擊之間沒有重算的話加值會漂；沒有找到復原的地方，也沒有證據說重算每次攻擊都跑，兩種可能靜態讀碼分不出來——remake 兩組三連都存著、只讀不寫，不會漂；第 644 輪：**大型目標的傷害切換接上了，怪物側的武器投影也補了**（spec 1175）——★★ `overlay-13:15EFh` 在**攻擊結算當下**切：只有攻擊者有槽 0 武器才切（天生攻擊不分大小）、切的是類別表 `+02h`..`+04h`、**加值是換掉不是相加**；★★★ 判準是 `+0DEh > 80h` **或** `and 7 > 1`，而六章 43 種怪物裡 `81h` 有四隻（`BEHOLDER`／`BUGBEAR`／`GIANT SPIDER`／`PHASE SPIDER`）——`81h and 7 = 1`，**只看低 3 位會把牠們判成小型**；bit 7 是獨立的「傷害算大型」旗標、低 3 位是佔格大小，remake 先前只留 `data[0xDE] & 7`，那個位元整個消失；★★ **怪物側的武器投影接上了**（`monster.ProjectMonsterWeapon`）：開戰掛完 `MON*ITM` 就照spec 1174 投影槽 0 的裝備中武器——那是 spec 1120 留了很久的「規則那一側還沒接」；★ `ReplaceFighterEquipment` 也補上新欄位，少了它換裝之後打大型目標用的還是上一把武器的骰；第 643 輪：**兩個掛了很久的未定項一起關掉**（spec 1173／1174）——★★★ **換武器之後傷害怎麼算**（spec 1120 留的問題）：派生值重算`overlay-24:0C28h` 先把記錄的基準骰整組抄進現值，再叫 `0025h` 做武器投影——`武器 := 角色^[151h]`（**槽 0**），為 NIL 就整支返回，否則把類別表的小型傷害三連（`+09h`／`+0Ah`／`+0Bh`）**無條件**寫進現值；⇒ 量到「記錄與武器都非 0 但不一樣」那 13 隻不是矛盾，`BUGBEAR` 的 `2d4` 是**放下武器時**的天生攻擊；★ 大／小體型在**戰鬥當下**才切（`overlay-13:1608h`），兩處都要有；★ remake 把 `FighterWithEquipment` 從「第一件有傷害骰的裝備中物品」改成**槽 0**——舊規則會被排在鏈前面的弓或彈藥搶走；★★ **`DS:7603h` 是下一批怪物的圖示槽**（`+143h` ＝ `ICONINDEX`，`overlay-33 entry#5` 與 `CPIC` 組檔名）：`inc` 在放置迴圈**結束後**才做一次 ⇒ 同一批共用一張圖，而 0..7 是隊伍成員的圖示，所以 `CLEARMONSTERS` 重設成 8 不是 0；它是**快取索引不是遊戲狀態**，remake 不需要對應動作，`1Ch CLEARMONSTERS` 轉 `done`（`partial` 剩 8 個 opcode／818 條指令）；第 642 輪：**`2E10h` 的髒旗標建進 VM，重畫不再靠「回頭掃寫入」猜**（spec 1172）——★★ spec 1150 讀完的模型終於接上：`STOREVALUE` 寫 `C04B`／`C04C`／`C04D` 時**當場**鏡射到 `720Fh`／`7210h`／`7211h` 並立 `8B68h`，`2Dh CALL 2E10h` 取快照後把五個旗標清掉；先前那個「回頭掃同一 block、執行序在前的 `SaveWrites`」啟發式整支拿掉；★★ **`CALL C01Eh` 要在 VM 裡走一步**——原作的 `MoveForward` 當場改地圖暫存器，而 remake 把 `CALL` 的副作用留到執行結束後才套用，鏡射不跟著走的話中間那些 `2E10h` 的快照會停在走之前，盜賊公會的走位動畫少掉第一步；⚠ 留下**兩個 remake 這一側的補丁**（原作沒有，都寫成可以拿掉的條件）：鏡射多記「三格是哪個 block 的腳本寫的」，以及 `Written` 遮罩每次頂層執行重新起算——兩者的成因是同一個：remake 改變隊伍位置的路徑不只一條、不是每一條都回寫 ECL 暫存器；★ 回歸擋板跑的是**真的 bytecode**，手寫 `RunResult` 會把「快照在什麼時候取」的答案寫進輸入裡；第 641 輪：**卷軸的 `USE` 也接上了，戰鬥裡的「使用」整條走得通**（spec 1171）——★★ 原作判「是不是卷軸」看的是**類別表的第一格（裝備槽）**落在 `0Bh`..`0Dh`，**不是物品類別**：`3Ch` 的名字叫 `Scroll` 但槽是 `0Ah`，原作把它當充能物品，所以正確的數字是**卷軸 12／充能 18**（先前寫成 15／15），連帶答掉 spec 1169 留的「`5Fh`／`60h` 沒有持有者」；★★ **`+35h` 一個位元組管兩件事**——它是隱藏名稱旗標，非 0 時連卷軸上有哪些法術都列不出來，清掉它同時把名字補成 `... With 3 Spells`，**辨識與命名是同一個欄位**；★★ **名字編號 `+30h` 就是剩餘支數**：一律 `0D4h`，每唸一支減一、`< 0D2h` 才丟掉 ⇒ 唸完第三支才消失；★★★ 失敗判定正是 AD&D 1e：牧師槽或法師槽有等級 → 一定成功、**盜賊 10 級以上 → 1d100 ≤ 75**、其餘一定失敗只印 `oops!`（失敗**不消耗**卷軸）；★ 選單本體是通用法術挑選器，七個標題字串把用途講死（USE 傳 `on Scroll`）；remake 唸卷軸與記憶施法**選目標的方式一樣**；⚠ 條件 B 的 `+111h > +0E6h` 沒讀出來，只實作「牧師槽有等級」那一半；第 640 輪：**充能物品的 `USE` 接進 remake，15 件全部活過來**（spec 1169／1170）——★★★ 突破口是**法術主表那 13 筆無名列不是占位，是物品效果列**：火球的 handler 第二條就是 `cmp ds:6F97h, 40h`，為飛彈項鍊分了 `(1d3×2＋1)d6` 的路，**如果玩家取不到，這段程式沒有存在的理由**；九支 handler 逐條讀完，四句訊息（`is Speedy`／`is stronger`／`is paralyzed`／`is invisible`）把語意釘死——巨人力量藥水把力量提到 **21**、時間 `(1d4＋4)×10`，**比克魔杖只打 `RACETYPE = 12h`**（資料裡那四種全是 Moander 的植物眷屬）；⚠⚠ 這個缺口**是自己的過濾器造成的**：`spell_damage_table.py` 的 `or spell["placeholder"]` 讓四列從來沒被讀過，而排除條件的來源是 spec 815 一句沒驗證過的「玩家取不到」——**排除條件也是斷言**，已寫進 `AGENTS.md` §3；★ 接線本身沒有新規則：分類用欄位不用類別（粉塵與項鍊的類別是 `46h`）、等級一律 6、目標形狀取自 `+6` 敵我借用 `+7`（⚠ 借用是推論，沒見過的組合一律回錯誤）、扣除順序「數量 > 1 就只扣數量」；`U` 使用、`Alt+U` 換一件；沒接的三件事（卷軸的 `sub_3474h`、`92h` 到期換回力量、選單裡的物品清單）寫在 spec 1170；第 639 輪：**戰鬥選單改成逐項串接，並把 `USE` 的缺口量成數字**（spec 1168）——★★ 原版六章 **253 件物品** ＝ 卷軸 15／充能 15／其他 223（`cmd/item-use-audit`）；⚠ 卷軸的判準（類別 `3Ch`..`3Eh`）是從資料推的，工具逐件檢查「名字含 Scroll」⟺「類別在這三個裡」，253 件全部成立、不成立就整支失敗；★ 選單照 spec 905 的形狀改成逐項串接，`使用` 誠實了（身上沒東西就不顯示）；`移動`／`施法` 還沒照條件收放，理由寫在 spec 1168；⚠ 新工具第一版走了 remake 的解碼路徑讀原版 DAX，被既有的閘門當場擋下來（spec 1121）；第 638 輪：**驅散不死接進 remake**（spec 1167）——規格（spec 834／905）早就 `READY`，缺的只是接線：戰鬥主選單先前七項少了 `退散`；★★ 矩陣逐 byte 取自資料段 dump，**`0` 與 `−1` 不能統一**——兩者驅離／摧毀行為相同，但額外配額那一條原作寫的是 `if 有號(v) < 0`，`0` 那幾格摧毀之後不加回配額；★ 選單改成動態（牧師槽等級 > 0 且 `TRIEDTOTURN` ＝ 0 才接上，用過就消失），⚠ 條件裡沒有「場上要有不死生物」；★★★ 順帶定案 spec 1164 留的未定項（spec 1166）：**DOS 少的那一格就是 `MONSTERTYPE`**——原版怪物資料的 `+14Ch`＝0、`+14Dh`..`+150h` 是位移小、節區隨資料往前走的堆積指標，照 PC-98 版面讀會得到「一隻怪帶 12 件物品、節區永遠 `001E`」；`internal/monster` 的 `RawMonsterType` 改名 `RawItemCount`（恆為 0、沒有規則讀它，無行為變化）；第 637 輪：**把 spec 1164 那張表用到另外三份記錄上**（spec 1165）——★★ 22 bytes 戰鬥狀態 ＝ `COMBATVARREC` 19 欄，spec 806 的表補齊：**`+0Bh`／`+0Dh` 根本不是欄位**（是 `TARGET` 這個 4 bytes 遠指標的中間位元組，原本列在「還沒看到用途」）、`+11h` ＝ `TRIEDTOTURN` 是最後一格，而 `+05h` ＝ `ZEROLEVBLOWS` 與角色記錄 `+0DDh` ＝ `ZEROLEVELBLOWS` 兩份互證；★ `.SWG` ＝ `CHARITEMFILREC` 十四欄全對上（`+35h` ＝ `IDENTIFIED`，與 spec 1036 讀到的名字一致）、`.FX` ＝ `EFFECTREC` 五欄；⚠ 六處讀法待對集中列出，**只並列不覆蓋**——既有的 `decoded` 判讀是從程式碼讀出來的，光憑名字不足以推翻；其中 `internal/combat` 的 `AlreadyFled`（`+10h`）在 Pascal 那邊叫 `TURNED`（被牧師逼退的不死生物），可觀察行為相同但來源不是士氣，士氣那條是 `+14h` `ROUTING`；第 636 輪：**PC-98 除錯符號展得開 record 版面**（spec 1164）——型別記錄尾巴那三個位元組先前只當 `Detail` 收著，**後兩個是小端序的成員索引**（指標指到目標型別、record 指到第一個成員的 1-based 索引），欄位依序排列沒有對齊填塞；★★★ 角色記錄的 `unknown` 從 **24 變成 0**（422 ＝ decoded 299／documented 123）——`MINREST`／`SIZE`／`RAISED`／`MODIFIED`／`OLDCLASS`／`OLDLEVEL`／`RACETYPE`／`BASEATTBLOWS[1]`／`RANDOMID`／`BASEEXP`／`EXPPERHP`／`HEAD`／`BODY`／`COLORLIST`／`PDLNREMOVECURSE`／`DUM2`／`DUM3` 全部有名字；★★ DOS 的 422 比 PC-98 的 423 少一格，在 `+14Ch`..`+186h` 之間、之後整批往前挪一格——拿原版存檔對，挪一格之後 `MAXHP`＝`CURRENTHP`＝51、`BASEMOVE`＝`MOVE`＝12、`ENCUMBERANCE`＝300 全部成立；⚠ 三處與既有規格讀法不一致（`+0E6h`／`+11Ch`／`+192h`）只並列不覆蓋；★★ 另有 31 個具名 record 的逐欄版面一併產出（`WALLSET`／`TACTICALMAP`／`MAP3D`／`CHARITEMFILREC` ＝ `.SWG`／`COMBATVARREC` ＝ spec 806 那 22 bytes／`EFFECTREC` ＝ `.FX`），見 `docs/audit/pc98-record-layouts.md`；順手刪掉一份重複的覆蓋報告（`docs/audit/save-field-coverage.md`，正本是 `dos-save-field-coverage.md`）；第 635 輪：**SAVGAM 的前四塊就是 ECL 位址空間的前四區**（spec 1163）——`Area1`／`Area2`／`Runtime`／`ECL` ＝ 區 0／1／2／3，`ECL 位址 = 基底 + SAVGAM 位移 ÷ 2`；三重對齊：四塊大小與四區格數×寬度一一相符、spec 181 讀到的寫入順序與 spec 1096 讀到的 bank 指標順序一致、★★★ `internal/area` 那九個「未解讀」的具名位移套上算式之後**每一個都落在已知的 ECL 位址上**（`0x1E0`／`0x1E2`／`0x1E4` ＝ 移動前座標快照與上一段 ECL 編號、`0x1FA`／`0x1FC` ＝ 各段自己寫的天空色、`0x342` ＝ 世界路線查出來的城市、`0x5C2` ＝ 頭像、`0x624` ＝ `7F12h`）⇒ 那幾格從來不是「Area 記錄的未知欄位」，是 ECL 變數；★★ 兩側都接上了：存檔只動 VM 碰過的位址（沒建模的原版位元組原封不動）、讀檔不收 0 值、Area 編碼器那幾格以 `s.Area` 為準；⚠ 區 3 不寫回，自我修改過的 bytecode 不進原版版面的存檔；★★★ 順帶補掉第 634 輪自己開的洞：per-block 暫存**沒有進存檔**，存檔換段後讀回來會把別段的一次性旗標清掉（房間會再演一次）——`SessionSnapshot.BlockScratch` 補上，`internal/ecl` 與 `internal/game` 各一條兩面測試，都做過變異驗證；第 634 輪：**spec 1160 那六件事全部落地，`go test ./...` 全綠**；★★★ 路上解掉一個更根本的問題（spec 1162）：**`4C00h`..`4C0Fh` 是每一段自己的暫存**，不是整個 session 共用一份——判準是那 16 格**沒有任何唯讀的消費者**（會讀的段一定自己也寫），而 `4C10h` 以上滿是跨段交接；判定證據是眼魔洞穴的死精靈：世界地圖的旅行耗時暫存離開時把 `4C06` 留在 4，洞穴的 `0651h COMPARE 4C06 01` 因此走 `>` 那條並跳過選單，**袋子、毒氣陷阱與冒險手札第 59 條整串拿不到**；★★ spec 1142 那幾間「走到了卻沒有反應」的房間逐一復原，並校正兩處誤判（「下水道塌了」要從下水道進來才唸、停屍架那一格分派到一句 `EXIT`）；★★ 主線因此多演了好幾段（奧莉薇在路上就遇得到、火刀檢查哨要答否、立石群灰袍男子直接揭露烈焰之主）；新增 `standing-stone.seek-green`；★ 工具：`cmd/ecl-window -at <位移>`；第 633 輪：**GEO 目錄交給 `State` 了**（spec 1161 的 remake 段）——`SetGeoCatalog` ＋ `currentDungeonGrid` ＋ `refreshDungeonTerrainFromMap`，`projectFreshDungeonCoordinatesBeforeCall` 投影完座標就照原作重畫的做法把牆面／地形讀回 `C04E`／`C04F`；`cmd/azure-bonds-game` 載完目錄也交給 `State`，遊戲本體與測試同一條路徑；兩面測試 `TestRedrawRefreshesTerrainOnlyWhenTheScriptMovedTheParty` 釘住「有寫座標才重讀、沒寫就不動」——只釘一邊的話「每次重畫都重讀」與「永遠不重讀」各會過一邊；★ 效果實測：把 spec 1160 那六件事套上去之後，眼魔洞穴到站的 `C04E`／`C04F` **不再需要測試手動補**，直接就是 `8`／`0C0h`，而先前那三處手動補值（巫師塔塔頂、熔岩池、眼魔洞穴）也都不需要了；★★ 六件事＋九處斷言＋巫師塔走法全部套上之後，`internal/game` 只剩主線通關測試的眼魔洞穴那一段，而卡住的點已經定位到 `ECL4/0x22:0651h`：死精靈那個選單由 `4C06` 擋成**一次性**，投影生效之後主線會在更早的地方先走到那一格、選單就在那時被消費掉 ⇒ **剩下的是那一段測試的內容順序要重排，不是引擎問題**；六件事本身仍未推（缺一條主線就會停在某個門口），實作已還原、工作區全綠；第 632 輪：spec 1160 那六件事全部實作、九處原作行為的斷言全部改完，`internal/game` 從**四條紅收斂到只剩主線通關測試的眼魔洞穴那一段**——路上把提爾弗頓、盜賊公會、猶拉什、巫師塔、希爾斯法、贊提爾都走通了；★★★ 擋住最後一段的是**結構問題不是路線**（spec 1161）：原作 `2E10h` 重畫會**自己去地圖讀**目前這一格的牆面／地形寫進 `C04E`／`C04F`，而每格事件正是靠 `C04F` 分派；remake 的 `State` **手上沒有 GEO 目錄**（`grid` 一律由呼叫端傳進 `MoveDungeon`），所以腳本傳送（只寫 `C04B`／`C04C`）之後 `C04F` 還停在被搬走前那一格，接下來的事件就分派到錯的一支或什麼都不演——實驗裡巫師塔塔頂、熔岩池、眼魔洞穴各要手動補一次 `C04E`／`C04F` 才走得下去 ⇒ **下一步是把 GEO 目錄交給 `State`**；★★ 巫師塔的走法解出來了：`GEO5/0x33` 是五個互不相連的房間，樓梯朝向表在 `1C81h`（`090Ch` 用地形碼查，朝向不對直接 `EXIT`），⚠ 索引 1 給的是 `5` 不是合法朝向——`(7,15)` 不是樓梯而是出口，要朝東才演 `0811h` 那個「地道還是荒野」選單；★★ 眼魔洞穴 `(5,9)` 的傳送器把隊伍送到 `(13,1)` 朝西，而且死精靈那一段就在同一次執行裡印出來，pack 的 `set_map_position` 替身在位置那一半已經多餘；★★ 測試走訪器兩條規則：落點不是規劃那一格就把控制權交回呼叫端（否則症狀出現在終點、原因在中間某一步）、開鎖門子路徑也要記「被拒的邊」，而寫死的隨機格排除清單是**逐圖**的（`(8,15)`／`(12,10)` 落在巫師塔那張圖上）；整輪實作已還原、工作區全綠；第 631 輪：把「拿掉宣告 spawn 那道閘門」整套做了一遍再**還原**，推導成果全部留在 spec 1160——原本以為三件事，實際是**六件**：① 拿掉 `Spawn` early return、② `2E10h` 的守衛改成逐條看 `CallRequests[i].BlockID`（跨 block 的執行原本整批跳過）、③ `2Dh CALL C01Eh` 改讀腳本當下寫的 `C04D` 並把走完的座標寫回暫存器、④ 重畫要**消費**座標寫入（原作是髒旗標、重畫時清掉；沒有這條游標，走位迴圈裡第二次重畫會把進場 spawn 再投影一次、把中間走的步數整個抹掉）、⑤ 宣告的 spawn 改成「沒人指定時」的後備（否則會得到「X／Y 來自 pack、朝向來自腳本」的混合值）、⑥ 拿掉 `ECL2/0x02` 的座標位移；★★★ **那個位移是投影被壓掉時湊出來的**——公會進場寫 `(8,0)` 朝東，`0D44h` 的五次迴圈照方向表 `1,2,2,2,2` 走成 `(9,3)` 朝南，而 `(9,3)` 正是 `ECL2/0x02` 每格分派表裡公會主人那一格（「BEFORE YOU STANDS A BURLY MAN…」）⇒ 腳本的暫存器本來就是 GEO 格子（與 spec 1158 的哈普村同一個形狀），spec 292 記的到站暫存器 `(1,12,0)` 是套過位移之後的值；★★ 九處原作行為逐處推導完成（賢者／武器店／神殿／高階祭司離場退回門外、訓練所往南一格、酒館走位到 `(7,12)`、城門衛兵送回 `(2,0)`、公會走到 `(9,3)`、猶拉什進場走到 `(0,3)` 朝西且收尾朝向由 `0185h` 的 `SAVE 4C00 C04D` 決定），十個測試斷言也都重新推導過；★★ 實驗裡卡在巫師塔塔頂，但**出口已經找到**：`ECL5/0x33:0811h` 那段的選單選 `CAVES` 走 `088Bh` ＝ `SAVE 06 C04B; SAVE 0F C04C; NEWECL 32h`——⚠ 那兩句座標寫入**後面沒有 `CALL 2E10h`**，交接靠 `NEWECL`，remake 這一側要由 `syncDungeonStateFromECLRegisters` 接；而塔頂 `(3,1)` 在 `GEO5/0x33` 上**不是孤立的**，走不出去是因為 `walkNormalDungeonTo` 那份寫死的排除清單（`(8,15)`／`(12,10)` 落在這張圖上，把第 15 列走廊切斷）⇒ 要處理的是走訪器，不是遊戲機制。因為六件事缺一條主線就會停在某個門口，本輪**不推半套行為改動**，整套實作已還原、工作區全綠；第 630 輪：**原作只有一條寫入路徑 `STOREVALUE`**（spec 1159）——所有會寫變數的 opcode 都走它，而它同時把隊伍座標鏡射到 `720Fh`／`7210h`／`7211h` 並立髒旗標，所以「哪些 opcode 寫過座標」在原作裡不是分類問題；remake 的 VM 先前只在 `09h SAVE`／`35h SAVE TABLE` 記 `SaveWrites`，算術／`08h RANDOM`／`2Ah GETTABLE` 的寫入等於沒寫，已改成全部走同一支 `recordStore`；★★ 分類數字跟著改：「明確移動」是 **12 處不是 8**，多出來的四處全是 `GETTABLE` 的假門傳送（兩層查表），先前被歸到「沒有座標寫入」——**工具與 VM 同一個成因的假零**；新工具 `cmd/ecl-redraw-sites` 是這份分類的可重生清冊（125 = facing 67／move 12／restore 15／picture 3／none 28）；★★★ 12 處裡有 8 處還被兩道閘門擋著：地圖宣告了 `Spawn` 就整張跳過（提爾弗頓／贊提爾暗黑神殿／巫師塔），跨 block 的執行整批跳過（盜賊公會）；★★ 本輪拿掉第一道閘門量過一次（已還原）：提爾弗頓一次浮出六個原作行為——賢者菲拉妮／科米爾武器店／剛德神殿／高階祭司離場都**退回進門前那一格**（`(5,5)`／`(3,12)`／`(1,7)`／`(2,10)`）、酒館走出門後有一段**走位動畫**停在建築側邊 `(7,12)`、城門衛兵第一次會把隊伍**送回 `(2,0)`**；⚠ 但不能就這樣拿掉——`2Dh CALL C01Eh`（`MOVEFORWARD`）用的是 `s.DungeonDirection` 而不是腳本當下寫的 `C04D`，盜賊公會抓捕動畫在 `0D90h` 先 `SAVE 7F7B C04D` 指定方向，朝向沒同步就會走錯方向（spec 292 量到到站暫存器是 `(1,12,0)`，朝西走會變成 `(13,0)`）；第 629 輪：**`C04D` 條件拿掉了**——只寫 `C04B`／`C04C` 的重畫就是真實移動、朝向不變，那 23 處全部生效（spec 1158）；擋著的四條路線測試逐條重新推導，順帶推翻一個地圖範圍上的斷言：★★★ `GEO5/0x32` 同時裝兩張圖，**哈普村只有 x 0..5**（地形碼 `< 0x80`，`ECL5/0x31`），東半是黑暗精靈／古熔岩洞（`ECL5/0x32`），分界寫在 `03D0h` 的 `COMPARE C04F 80h; IF <`；每格分派器 `AND C04F 7Fh` 把最高位遮掉，所以 `ecl-cell-events.md` 兩個 block 的格子欄長得一樣**不代表兩邊都到得了**——舊路線從 `(5,12)` 一路往東走到 `(15,5)`，等於在村子的 ECL 底下逛另一張圖；村子出口是「地形碼＋朝向」成對（`01`/北、`03`/西、`02`/東），`(5,12)` 往東是唯一東側出口，答 `NO` 走 `0E9Fh` 退回上一格；★★ `4C02` 是「這一步已經演過一場」的閂（`03D7h` 每格清 0、四支場景演完寫 1），舊斷言 `4C02 == 1` 成立只因為隊伍停在東半、清零那句被跳過，現在改成「剛演完是 1、停在 `(4,12)` 是 0」兩個斷言；★★ 另外兩處玩家選項會退格：`ECL3/0x11:0589h` 的 `FLEE`（改挑 `WAIT`，走 `1075h` 跳過那兩句 `SAVE`）、`ECL6/0x40:097Fh` 的 `GO`（改挑 `REBURY SKELETON`）——改完之後隊伍第一次真的穿過去，**多走到以前走不到的格子**（該段 19s→44s，墓園 `(6,12)` 的骸骨選單就是這樣冒出來的）；★★ 尋路要記「被拒的邊」：走完一步座標沒變就把 `{地圖,格子,方向}` 記進觀察器繞開，否則症狀出現在終點、原因在中間某一步；★★ 工具盲點：查「誰跳到這個位址」要用 `cmd/ecl-window -into`（走 `TraceGraph`，跟 `ON GOTO` 的邊），`-opcode 24 -operand` 只比對指令運算元——`160Ch` 用 `-operand` 只算得到 3 個入口、`-into` 是 6 個，而玩家實際踩到的三個全在漏掉的那一半；新增兩面測試 `TestFireKnifeFrozenRoomRetreatReturnsToPreviousCell`（`RETREAT` 退回 `(5,2)`／`INTERROGATE` 留在 `(4,2)`），並把原本釘著「沒有 `C04D` 就不搬」的那條單測改成釘相反面；第 628 輪：那 23 處逐處分類完成（spec 1157）——只有兩種：8 處明確移動、15 處 `SAVE 4BF0 C04B; SAVE 4BF1 C04C`「退回上一格」，**兩類都是真實的隊伍移動**，所以 `C04D` 條件擋掉的是玩家看得見的位移；★ 那 15 處的守衛**各不相同**（`4C10`／`7ED5`／`4C06`／`4C44`／`7F79`／`4CE4`／`7F82`），只有一處用 `7EC7h`——上一輪寫的「等 `7EC7h` 有 producer」不是共同前提；★★ `ECL2/0x04:160Ch` 是從**六個**地方跳進去的共用退格出口，先前寫「只有戰後走得到」是只看了一個入口；★★★ 實跑探針（已還原）定案火刃巢穴三條路線測試會紅的原因：投影發生在退格處理之後的 `161Ah`，而 `COMPARE 7EC7 80h` 讀到的確實是 `0 > 80h`＝假 ⇒ 走的是另外五個入口之一——**那三條路線是照著被 `C04D` 壓掉之後的行為寫出來的**，要換忠實模型得連同重新推導路線一起做；★★ 順帶抓到第二處被 IDA 切錯的共用 handler：`2Fh`／`30h` 共用 `0DA4h`..`0E12h`（43 條，IDA 拆成 11＋32），audit 表原本連 `2Fh` 那一列都沒有，兩列都補上並在表頭寫了邊界警告；★★ 差點照著一行錯註解改壞程式：`AND`／`OR` 之後的比較是 `compare_variables(0, 結果)`，**`0` 是左運算元**（`overlay-02:0DF3h` 先 push 0），remake 的程式碼本來就對、錯的是註解——而全 corpus 174 處 `AND`／`OR` 沒有一處接排序型 `IF`，所以對調了**所有測試照樣全綠**；第 627 輪：`7EC7h` 命名 ＋ 修掉「拿 PC 當執行順序」那個代理（spec 1156）——`7EC7h` ＝ `bank1^[58Eh]`，寫入端全在 `overlay-05`（POSTCOM）四處，`0817h` 寫 **`81h`**、`0B52h`／`185Dh` 寫 `80h`、`173Fh` 寫 `0`；腳本一律 `COMPARE 7EC7 80h / IF >`，只有 `81h` 成立 ⇒ **`81h` ＝ 這一場沒有打贏**（spec 303 的勝利線、1145 的重打迴圈、318 的退回三處用法一致，三份都刻意沒命名它）；★ `ECL2/0x04` 三處座標寫入全部列出，其中 `02D7h`「YOU ARE BEING TAKEN TO THEIR LEADER.」是**沒有 `C04D` 的真實移動**，remake 現在整個跳過——玩家看得見；★★★ 順帶抓到一個結構性問題：`projectFreshDungeonCoordinatesBeforeCall` 用 `write.PC < call.PC` 篩「這條寫入在 CALL 之前嗎」，但**反向跳躍時 PC 小不等於先發生**；已改成 VM 記執行序（`MemoryWrite.Sequence` 與 `CallRequest.Sequence` 共用同一條計數，所以序號會跳號——死精靈那段是 1、2、3、**5**，中間的 4 是那條 `CALL`，這本身就是「兩份清單在同一條時間軸上」的證據）；⚠ 這個修法**單靠第一版回歸測試抓不到**：那一版把後執行的那一輪放在比 `CALL` 小的 PC，但兩種比法給的答案相同，改回 PC 版仍然全綠——要真正的反向跳躍（`CALL` 之後跳回小 PC）才區分得開；⚠ `C04D` 條件仍在，行為在真實 corpus 上沒有改變；第 626 輪：**`4BF0`／`4BF1` 的來源解出來了**（spec 1155 補完，spec 1045 加上連結）——用 spec 1098 的區 0 換算 `bank0 + (位址−4B00h)×2` 把兩個格子對回 `bank0^[1E0h]`／`[1E2h]`，而 spec 1045 的地城主迴圈（`overlay-02:3984h`）在呼叫 `MOVEPARTY` 之前就把當時的 X／Y 存進去、`MOVEPARTY` 回來再比對「隊伍到底有沒有移動」⇒ **那兩格是「這一步移動之前的座標」**，腳本那 15 處 `SAVE 4BF0 C04B; SAVE 4BF1 C04C` 是**把隊伍退回上一格**；★ 缺的從來不是逆向——spec 1045 記著「記下座標」、spec 1098 記著換算公式，兩份都 `exact`，缺的是把它們兜起來的那一句；⚠ 而 `overlay-02:3984h` 那一段 **IDA 沒有認成程式碼**，是位元組掃描找到的，只查函式清單會落空（這條也是 spec 515／516 從第 515 輪留到現在的未決項）；remake 的 `MoveDungeon` 已接上同一個時機並用兩個變異釘住（存成移動**後**的座標會讓那 15 處等於什麼都沒做而且測試全綠）；⚠ `C04D` 條件**還沒拿掉**：補上 producer 之後再試一次，失敗座標從 `(0,1)`（垃圾值）變成 `(5,3)`（真座標）——資料對了，剩下的差異是「哪些 `CALL` 該投影」，那要另一輪；第 625 輪：把 `2Dh CALL 2E10h` 的缺口從「模型不對」收斂成一個具體的擋路點（spec 1155）——125 處可達的 `CALL 2E10h` 逐處分類：67 處寫了 `C04D`（remake 會投影座標）、**23 處只寫 `C04B`／`C04C`**（remake 整個跳過，而原作 `STOREVALUE` 當場就寫了）、3 處只有 `PICTURE`、32 處前六條沒有座標寫入；★ 那 23 處裡有 **15 處是同一個慣用法** `SAVE 4BF0 C04B; SAVE 4BF1 C04C`（還原一組存起來的座標），而 `4BF0`／`4BF1` 在整個 ECL corpus 裡**只被常數寫過兩次**、remake 這一側沒有任何 producer；★ 做了一次**已還原**的對照實驗：把那個 `C04D` 條件改成原作模型之後四個測試立刻失敗，隊伍被丟到 `(0,1)`——⇒ **那個條件不是把 `2E10h` 讀錯了，是在補 `4BF0`／`4BF1` 沒有 producer 這個洞**；就算把五個髒旗標全部實作出來，那 15 處還原一樣會把隊伍丟到錯的格子。spec 1150 與 `effects.go` 的 `partial` 理由都改寫成這一句；⚠ 這一輪**沒有改行為**——照 WORKLIST 自己寫的警告，先建重現材料再談換模型，而重現材料證明了不該換；第 624 輪：`38h PROGRAM` 由 `partial` 轉 `done`（spec 1154）——四個分派值 0／3／8／9 全部讀完，corpus 13 處四個值都用得到、沒有一處落在什麼都不做的預設值；★ **打通關看得到結局了**：原作 `PROGRAM 8` 先跑 `overlay-18:10FFh` 的結局過場才回主選單問存檔，而 remake 先前直接跳到存檔詢問，一句結局都看不到。spec 1082 有全部台詞但只寫「…印一段…」沒給邊界——把字串位移與等鍵呼叫依序取出來之後，分段是**五頁、四次等鍵**，而且**第 4 頁是 8 行、其餘四頁各 4 行**（它把「枷印消失」與「騎士抵達」合在同一頁）；第 5 頁演完沒有等鍵，直接落回存檔詢問。主線通關測試會逐頁走過去，順便驗那五頁在真實玩家路徑上到得了而且是中文；⚠ **spec 1082 的中文草稿與專案 glossary 不一致**（提蘭斯拉克蘇斯／密斯卓諾／影谷／艾米斯特／莫安德／萊桑德／光輝之池），譯文一律照 `coab-glossary.md`（提朗瑟克斯／迷斯卓諾／暗影谷／伊爾明斯特／摩安德護手／洛山達護符／光芒之池）；⚠ **spec 1087 把 `4FC7h` 說成「訓練不收費」旗標是錯的**——那是 spec 1045／1095 的離開旗標，也是 spec 1152 讀到的「全隊倒下」那一格；訓練所的免費旗標是 `7587h`／**`4FC8h`**／`8B6Fh`，差一個位址。所以 `PROGRAM 3` ＝ 宣告隊伍全滅，remake 本來就是對的；`partial` 由 10 個 1,037 條降為 **9 個 1,024 條**；第 623 輪：`37h LOAD PIECES` 收尾（spec 1153）——★ 它與 `21h` 共用的那一支**函式邊界是錯的**：IDA 只認到 `0D4Ah`（104 條），真正的 `retf` 在 `0DA3h`（131 條），被切掉的正是收尾那段重繪判斷（audit 表已改）；★ 三組牆面參數的寫入端找到了——`LOADWALLSET` 的**收尾六條**寫 `[7210h＋槽×4] := 片`、`[7212h＋槽×4] := 槽`（第二個 word 是**槽號本身**，兩個運算元都取 `arg_0`），運算元是 `0FFh` 的槽則由 handler 自己寫 `0FFFFh`；這三格就是存檔第 9..14 欄，而 **remake 從來沒有寫過它們**，存出來的檔一律是零——已接上並釘住三個方向；★ 順帶定案 `21h LOAD FILES` 的**第一個**運算元是**要載的地圖 block**（全 22 處掃過；通常等於所在 block 但不一定——三處載的是別張）；⚠ **spec 1087 的運算元順序寫反了**——它照 IDA 的 `var_N` 標籤抄，而那是「離 `bp` 幾個位元組」不是運算元編號，反過來讀會得到「`LOAD3DMAP` 全遊戲一次都不執行」這個自洽但錯的結論；PC-98 側的 spec 587 一直是對的，corpus 是第二個證人。正文已改寫，並把這個錯誤寫成規則進 `AGENTS.md` §3（連同「下結論前先 grep 那個位址」——這一輪和上一輪各踩了一次）；`37h` 仍 `partial`，剩兩條 corpus 走不到的路（`o[1] = 7Fh`、兩槽分支）；第 622 輪：`2Eh DAMAGE` 整支 305 條讀完（spec 1152），連 `MAKESAVE`／`TRYTOHIT` 也各自讀完——★ **旗標 bit 7 清空時整個 byte 是次數**：連打 N 下、每下隨機挑一名隊員、用第五個運算元當**攻擊值**（不是豁免種類）擲 `TRYTOHIT`，而且**每下之間重擲傷害**；bit 7 設定才是旗標（bit 6 全隊、bit 5 不擲豁免、bit 4 豁免成功仍吃**全額**、bit 0..4 豁免調整值，⚠ bit 4 與調整值欄位重疊，但 corpus 24 處低 5 位全 0 所以實務上只是旗標）；★ 目標三選一，而「目前角色」那一路的**豁免種類要減一且 `0` 代表不擲**，另外兩路不減一、由 bit 5 決定——兩種讀法在 corpus 上結果完全相同，只驗行為分不出來；★ 「單體但隨機挑一名」在原作是活著的程式碼但 corpus 24 處**走不到**；remake 的規則本來就對，缺的是接線——正式路徑先前只結算全隊那 14 處，另外 10 處（8 處目前角色、2 處連打）永遠留在 pending，現在都結算了；★ 目標改由**封包自己**帶著發出當下選的角色，因為腳本會把 `LOAD CHARACTER` ＋ `DAMAGE` 包在走過整隊的迴圈裡（`ECL5.DAX/0x32:0223h` 配 `7F3Eh` 隊伍人數），拿聚合值會把整批傷害算到同一位身上；⚠ 結算的呼叫點**維持**在 `applyECLLoadCharacterSignals` 之前——看起來該移到後面，但沒帶選定角色的封包代表 VM 到那一刻沒人被選過，該退回的是這一次執行**之前**的值；順帶答掉 spec 582 的未定項之一（豁免種類由呼叫端夾成 0..7），並補上兩句只有 DOS 有的英文（`The entire party is killed!`／`press <enter>/<return> to continue`）；第 621 輪：隨機戰利品接上原作的造物品常式 `CREATERNDTREASURE`（`overlay-21:0FEDh`，spec 1036）——`ItemBlock > 80h` 擲出類別之後，加值、名稱三段索引、重量、價值、卷軸法術與特殊物品範本都照原作補齊，撿東西畫面上顯示得出 `＋N` 與名稱修飾；加值分布寫進 spec 1036（`sub_FAF` 擲 1d20，1..14 給 ＋1、15..20 給 ＋2）——那一項原本列在「明確不宣稱」，但**函式台帳早就記著**，這是補上規格不是新發現；★ 抓到 spec 1036 重量表的一格死分支——`149Fh` 寫「`2Fh` 或 `5Dh` ⇒ 1」，但 `2Fh` 在 `13DBh`（80）就被同一條 if-else 鏈攔下，照抄那一列會和原版分岔；擲骰順序（`plus` 先擲、`15h` 的 1d5 後擲、`47h` 的 1d8 在範本選擇裡擲）用 `scriptedRolls` 逐顆釘住，十二個變異只有補完卷軸環表與價值係數表之後才全部被抓到；`27h` 的缺口從兩項收斂成一項（多筆 TREASURE 的覆寫 vs 相加）；第 620 輪：`27h TREASURE` 整支讀完（spec 1151）——戰利品池是覆寫、物品鏈是前插所以清單反序、隨機表每一段兩端都夾（修掉 `{48,49}` 會給劍的 bug）；第 619 輪：`2Dh CALL` 七支分派的名字與內容（spec 1150）——corpus 其實用了四個運算元不是兩個、`B200h` 的第二個音效走不到、`6803h` 是圖片序列的下一格；第 618 輪：`24h` 的三選一順序反了，有怪就該直接打（spec 1149）；第 617 輪：`PICTURE 0FFh` 是關閉而 remake 先前什麼都不做（spec 1148）；第 616 輪：`33h` 是硬換行、缺口在 UI 行模型（spec 1147），並定案 `65A0h` 是欄、`65A1h` 是列；第 615 輪：遭遇距離模型與 opcode→handler 對照表，APPROACH 的減一是演出不是選單條件（spec 1146）；第 614 輪：`1Ch` 連戰利品一起丟掉（spec 1145），火刀首領重打迴圈會多領；第 613 輪：遭遇選單的三句旁白依距離挑，16／20 處原本演錯句子（spec 1144）；第 612 輪：段內支線在主線進度下逐格取樣，與遭遇選單那批沒被數過的旁白；第 611 輪：段界快照讀回來還走得動——`eventReturnMode` 沒進存檔，欄位全對但按下一步卡住；第 610 輪：199 處 24h 逐處分類；第 609 輪：24h 的三選一與營地旗標；第 608 輪：`+07h` 是假缺口，防禦存得下去讀得回來；第 607 輪：把剩餘工作清單重量一次，三個數字過期；第 606 輪：怪物記錄傷害 vs 裝備中的武器；第 605 輪：怪物的物品鏈接進 Fighter；第 604 輪：選曲逐格對回 PC-98 的 selector 表（SEG-33 後半）；第 603 輪：攜帶物的跨段連續性（SEG-31 後半）；第 602 輪：世界地圖從 13 個地點各掃一次、762 個分支；第 601 輪：世界地圖路段編號 `4C9D` 與世界地圖的語系缺口；第 600 輪：分派器配對條件修正與查表分派解讀；第 599 輪：旗標寫入端追溯與 spec 1142 的成因收斂；第 598 輪：全遊戲逐格實測（194 個索引、落回原文 0 格）與朝向刻度；第 597 輪：全遊戲每格事件對照表與古熔岩洞逐格；第 596 輪：第六章三區逐格盤點 41 格；第 595 輪：墓園逐格盤點與語系閘門的 `Prompt` 路徑；第 594 輪：內城一樓房間與 map-local bank（spec 1142）；第 593 輪：跨段不變量（語系／隊伍／音樂）；第 592 輪：**開場到結局的完整主線跑通**；第 591 輪：主線接到第六章密斯卓諾；第 590 輪：整條主線跑拆成 12 個段測試與段界快照；第 589 輪：25 段接線盤點；第 588 輪：`SEG-12` 47 條邊逐條交接；第 587 輪：`SEG-11` 前半，段落快照往返與交接；第 586 輪：`SEG-04` 統一直入旗標，25 段逐段進得去；第 585 輪：`SEG-03` 段落 id 與標籤；第 584 輪：`SEG-02` block ↔ 地圖對照；第 583 輪：`SEG-01` ECL block 轉移機制；第 582 輪：乾淨 clone 的 engine bootstrap；第 581 輪：力量調整表與原版存檔 oracle；第 580 輪：面向／護甲刻度等七個契約搬進共用 engine；第 579 輪：機會攻擊四道閘、隊員命中與 AC 表；第 578 輪：AC 與命中的刻度統一；第 577 輪：第二個 AC 的算式；第 573–576 輪：回合開始生命週期、突襲遮罩判定死碼、面向與背後攻擊、90° 扇形與機會攻擊面向閘；第 572 輪：第一人稱畫面與原版逐格比對）

本檔只保留**目前有效的現況與入口**。歷史敘述已分冊到
[`docs/context/`](docs/context/)，逐行保留原文，不再放在這裡。

## 真相來源優先序

同一件事若多份文件說法不同，依這個順序判定，並主動修掉被推翻的舊斷言：

1. 目前 worktree 的程式碼、JSON 與測試。
2. 原始 bytes、IDA database 與可重現的 runtime trace。
3. 標為 `READY` 且限定範圍明確的 `docs/spec/`。
4. [`docs/knowledge/coab-re-coverage-matrix.md`](docs/knowledge/coab-re-coverage-matrix.md)（系統層完整度）
   與 [`docs/audit/`](docs/audit/) 的可重生清冊（模組／函式層完整度）。
5. [`WORKLIST.md`](WORKLIST.md)（執行順序）。
6. `docs/context/` 分冊與 `docs/project-status.md`（歷史，只作追溯用）。

工作規則的單一權威入口是 [`AGENTS.md`](AGENTS.md)。

## 目前階段：先把反組譯盤點做完，再談語意與實作

2026-08-13 使用者指示停止「邊做邊分析」：先徹底完成 CoAB 的反組譯分析，
再繼續 remake。同日的盤點證實這個轉向有具體理由——

**先前的 RE 是問答式的**：`scripts/ida/` 的 64 支腳本每支只回答當時那一題，
沒有任何一份全模組函式清冊。因此「還剩多少沒看」在此之前無法回答。

**overlay 程式碼幾乎沒被分析過**：DOS `GAME.OVR` 直接載入 IDA 只得到
**1 個函式**；PC-98 同樣。原因是 raw TPOV overlay 沒有 MZ header 也沒有
entry point，自動分析無從開始。真正的 entry point 全在 resident executable 的
`CD 3F` five-byte stub 裡（第 412 輪已證明格式）。

把那份格式知識接成工具鏈之後，DOS 側從 1 個函式變成 **1,320 個**
（36 段 overlay ＋ resident，871 個 entry stub 當種子）。這不是新的語意成果，
是把先前看不見的程式碼變成可盤點的對象。

新的驗收口徑（使用者 2026-08-13 決定）：

- **全函式覆蓋台帳**：兩平台每個函式都要在台帳裡有狀態
  （`已解讀`＋推論等級／`不阻塞`＋理由／`待解讀`），不再只有系統層百分比。
- **兩平台都全掃**：DOS 是行為 oracle，PC-98 有 Borland symbols 可當語意
  交叉驗證；同一機制在兩邊的證據要分開記錄，不得因數字相同就合併。

方法、產物與批次順序見
[`docs/spec/559-full-module-re-sweep.md`](docs/spec/559-full-module-re-sweep.md)。

## 現有能力（可驗證，不等於整作完成）

**正常新遊戲 session 已經可以從開場一路打到結局**：下水道 → 火刀據點 →
戰後世界 → 阿沙本福德 → 立石群 → 艾森布拉城外 → Hap／熔岩洞／巫師塔 →
散提爾堡 → 眼魔洞穴（手札 59、Dexam 雙戰）→ 猶拉什 → 希爾斯法 →
立石群灰袍男子 → 密斯卓諾墓園（紅網、黛米爾公主）→ 外城遺跡 → 內城遺跡 →
儀式與爪牙戰 → 二樓東北角 → **擊敗提朗瑟克斯的結局選單**。
這條路拆成 23 個段 subtest，每段結束存一份快照並整批驗往返
（`TestRealNewGameRunsToTheEnding`）。整條主線的 260 句話**沒有一句落回原文**（訊息與提示兩條路徑都記）；
同一個角色的經驗值不會倒退、隊伍變動的位置是宣告好的；22 個段界都有一首在
game pack 曲目表裡的曲子。

原版的四段建角（種族 → 性別 → 職業 → 陣營 → 擲屬性 → 名字 → `Save?`）已完整
重現，資料全部取自原作資料段並轉成 `gamepack/rules/character-tables.json`。

主線分段有直入的入口了：25 段全部可用 `-segment ECL{成員}/0x{block}` 進去，
註冊表在 `internal/segment`，逐段一條測試。`EnterFrom` 一律要是轉移圖上真的
存在的一條邊（`0x00` ＝ 全新開局，spec 1141）。`ECL5/0x30` 是過場 block——
進去就轉走，停不在自己身上。⚠ 正常新遊戲流程走 `0x50 → 0x01`，**沒有經過開場
block `0x52`**；那一段的原作序幕目前只有 `-segment ECL1/0x52` 進得去。
段的邊界狀態存得下去也讀得回來（25 段的往返閘），`-segment-snapshot` 寫出快照、
`-party-load` 接下一段的交接實際跑通了。⚠ **存檔沒有獨立的 GEO 檔集欄位**，
讀檔時是拿 `Area.GameArea` 重建的——兩者留下不同的值，快照就往返不回來。
47 條 `NEWECL` 邊也逐條有交接測試（存快照 → 讀回 → 帶著來源 block 當 `LastECL`
進目的段）。⚠ 這些測試的來源都是**段的入口**狀態，不是段真的走完的結束狀態。

接線的現況由 `cmd/segment-coverage` 量出來（`docs/audit/segment-coverage.md`）：
**22 段的入口有中文劇情、3 段的入口不出文字、0 段落回原文**。不出文字的三段
（`ECL2/0x02`、`ECL3/0x12`、`ECL4/0x21`）是從別段被帶進來的，劇情在前一段的
轉移與每回合／搜尋生命週期裡，不在 `initial`。剩下的接線工作在**段內**。

整條主線跑拆成 **23 個段 subtest**，覆蓋的步數一步沒少（拆的時候用正規化逐行
比對驗過），每段結束存一份快照並整批驗往返。⚠ 這些段共用同一條 session，
前一段紅了後面就不跑；段界快照不進 repo（存檔格式一改整批失效）。

第六章的三個轉移是**讀 bytecode**找出來的，不是在地圖上撞出來的：墓園 → 外城
與外城 → 內城都是邊界處理的 `ON GOTO C04D`（面向÷2，0 起算，分別要朝東與朝北），
內城的每格事件則是 `ON GOTO (C04F & 0x7F)` 用地形碼分派（樓梯 `0x97`＝`(10,7)`、
最終戰 `0x9A`＝`(6,1)`）。工具是 `cmd/ecl-window`。

第六章三區的每格事件都由地形碼分派（`ON GOTO (C04F & 0x7F)`），所以「哪一格演
哪一場」是從 bytecode 反推的：`cmd/ecl-window -table` 拆表、`cmd/geo-probe` 反查
地形碼。全遊戲的對照表由 `cmd/ecl-cell-events` 一次產完（`docs/audit/ecl-cell-events.md`）：
25 個 block 裡 **14 個**用地形碼分派、**234 個場景對到格子**；另 5 個走 `GETTABLE`
查表分派（尚未解讀）。⚠ **遮罩不是固定的**（`0x7F` 與 `0x3F` 都有），寫死一種會讓
另一半安靜地變成「沒有每格事件」。

**逐格實測也是全遊戲一次跑完**（`cmd/cell-sweep` → `docs/audit/cell-sweep.md`，27 秒）：
每個分派索引直接進段、站到那個地形碼的第一格、跑一次生命週期，記玩家真的看到的字。
14 個 block、**250 個索引**，**235 格中文、落回原文 0 格**，15 格沒演出來且每一格都
印得出處理常式的守衛（12 格是 spec 1142 的 `4C00` 區）。兩份對照表不一致時**以實測
為準**：`ecl-cell-events` 的「第一句」是依位址往後找的推測，處理常式開頭是 `GOTO`
就會撈到隔壁那支的台詞。

世界地圖那一半用的是路段編號 `4C9D ＝ 出發地 × 4 ＋ 選的方向`（spec 1143），
四張表分別給旅行天數、走法數、到達地點與**旅途場景**。走得到的分支由
`cmd/route-sweep` 逐條驗語系：從宣告的 14 個地點各掃一次（13 個進得去），
**762 個分支、敘述與選項都沒有一條落回原文**。

⚠ **選項要單獨判語系**：溫拉爾軍隊那一場的敘述早就中文化了，三個選項卻還是英文
——只看敘述的閘門會說那一場已經接好。**缺口會躲在沒被量的那一維。**

⚠ **`24h COMBAT` 沒有運算元**（arity 0）——它是**三選一的服務分派點**（商店／營地／
戰鬥，spec 1095），不是「打一場」。199 處已逐處分類（`cmd/ecl-combat-sites`）：
**153 處真的要打、46 處走服務分派**，後者後面接的是「GOOD QUALITY TAVERN.」這類
商店／神殿台詞，而且每一處往回走都碰得到 `CLEARMONSTERS`。

⚠ **要沿控制流往回走時，`TraceGraph` 的 `Edges` 只記跳躍、不記循序。** 只用那些邊
做反向可達，多數指令找不到任何前驅——第一版因此得到「199 處裡 0 處擺過怪」的假零。
循序前驅要自己補（前一條會 fall through、而且它的 `Next` 正好是下一條可達指令）。
營地那一支的旗標 `0x7EE2` 在正式程式碼裡**沒有任何 producer**，所以它目前走不到；
remake 的營地走的是 lifecycle entry 2／3 這條等價路徑。

⚠ **修清單的時候會順手寫出新的假缺口。** 第 607 輪把三處過期數字改正，同一次卻在
P0 新寫了一條「`+07h`（保留的機會攻擊）沒有投影進 `Fighter`」——查下去才發現 remake
用 `CombatAction.Guarding` 表達同一個狀態，設定／消費／清除三端都對得上 spec 1136。
**照著 spec 的「明確不宣稱」抄，會把「沒有逐位元組鏡射」讀成「沒有做」。**

⚠ **路由文件的數字是手抄的，會過期。** `WORKLIST.md` 的「還開著的工作」逐列數字
抄自各支稽核工具；工具重跑之後沒人回頭改，就會出現「工作早就做完、清單還寫著開著」
——比沒有清單更糟，因為它把後面每一輪帶錯方向。2026-08-21 對帳抓到三處：回合生命
週期（spec 1135～1138 早就做完）、手札 producer（寫 30 則缺、實際 64 則全接）、
存檔 `unknown`（寫 29、實際 24）；**兩份稽核產物本身也過期沒重產**。
閘門是 `TestWorklistInventoryNumbersAreCurrent`。

⚠ **比對怪物的武器一定要過濾武器槽**（類別表 `+0` ＝ 0）。第一件裝備中的物品常常
是弓或彈藥（slot 10）——用它去比，`FIRE KNIFE` 會被判成「記錄 `1d8`、武器 `1d6`」
不一致，而牠 slot 0 的武器正好是 `1d8`。**槽位過濾漏掉會製造出假的不一致。**

⚠ **怪物的東西要依「怪物 ID 的命名空間」解析，不是依目前的 block。** 世界地圖的
派遣器會召喚別章的怪（`ECL1` 的 `0x50` 會叫出 `MON5` 的 `0x3C`），所以物品鏈、
效果鏈與怪物記錄三者都走同一條解析路徑。物品還要**每一隻各自一份**——共用同一個
slice 的話，一隻換裝會影響到全部，而症狀只在「同一種怪出現兩隻以上」時才看得到。

⚠ **「有值」不等於「值對」。** 選曲的閘門原本只驗「有一首在曲目表裡的曲子」，
那擋不住「每一段都播同一首」。PC-98 的 `BGMPLAY` 直接讀 `CURRENTECL`，所以哪一段
該播哪一首是**查得出來的**（spec 355 的精確表）——現在三層都驗：宣告對回原作的表、
段界播的是對的 selector、整條主線至少播出 4 首不同的曲子。

⚠ **驗「有沒有掉東西」的閘門要先擋非空。** 裝備／記憶法術／效果的跨段比對
（`SEG-31` 後半）如果只擋「沒有變動」，哪天攜帶物整批讀不到、每一段都是空的，
測試照樣全綠——**空的等於空的**。這條路線上真正帶東西的是 NPC 同伴
（阿卡巴的法術書、艾莉亞絲與龍餌的裝備與效果），玩家的單人戰士驗不出任何東西。

⚠ **走訪類的閘門要同時釘住「走到幾個」**。只驗語系的話，走訪壞掉（起點進不去、
選單認錯）會讓分支數塌下來而剩下的每一條還是中文——**全綠，但什麼都沒驗到**。

⚠ **分派器的配對條件是「`ON GOTO` 分派的那一格 ＝ `AND C04F` 寫進去的那一格」**，
不是「緊接在後面」。用相鄰去配同時會漏掉真的分派器（下水道中間隔了兩條指令）與
把不相干的兩條湊成一對（`ECL3/0x15` 那個 `ON GOTO` 分派的是 `4C08`）。
第二種形狀是 `GETTABLE <表>, <索引格>` ＋ 分派同一格的 `ON GOTO`，索引不是地形碼
——兩個世界地圖 hub 是「路段編號 `4C9D` → 14 個旅途場景」。

⚠ **掃描維度的刻度要從消費端讀**：邊界處理常式是 `COMPARE C04D, <方向>`，而
`C04D ＝ 朝向 ÷ 2`、朝向是 0..7。掃 0..3 只蓋得到 `C04D` 的 0 與 1，南、西兩面整批
落空——而落空的格子跟「這一格沒有內容」長得一模一樣。改掃 `{0, 2, 4, 6}` 之後，
猶拉什四個出城口才冒出來，其中三句還是沒接的原文。

⚠ 逐格盤點**每格都重新進段**——once-only 旗標與戰鬥結果會互相污染，塞進連續
session 會把別的段弄紅。

⚠ **`4C00` 區的旗標跨地圖互相污染**（spec 1142）。同一格在不同地圖是不同的東西：
`4C06` 在世界地圖是旅行時鐘（`ECL CLOCK 4C06 04`），在密斯卓諾內城是三間房共用的
once-only 標記。症狀不是崩潰也不是錯字，是**房間安靜地不演**——內城五間房加上
散提爾堡城區四格，守衛讀的旗標帶著別張地圖留下的值（`4C07=0x80` 是眼魔洞穴寫的，
而第四章之後沒有任何一處把它寫回 0）。

換 bank 的時機**不是** `NEWECL`、也**不是** `LOAD FILES`——兩個候選各有一個反例
（`ECL4/0x20` 前導讀的是第二章寫的 `4C01`；`ECL3/0x10` 在 `LOAD FILES` 之前寫
`4C05`／`4C0C`，之後整段都在用）。剩下兩種可能靜態讀碼分不出來，**要拿原版當 oracle**。

⚠ **查旗標的寫入端一律用 `cmd/ecl-cell-refs`，不要查事件目錄**：目錄的指令表有可達性
缺口，`4C06` 只列 18 處而走訪有 75 處，密斯卓諾廚房那支 `SAVE 01 4C06` 就不在裡面。
**拿目錄下「沒有人寫這個旗標」的結論會反過來。**

⚠ `zhentil-keep.beholder-cave` 的 `script_block 0x22` **是對的**：那一段跑的時候
`CurrentBlockID()` 就是 `0x22`，而迪姆斯華特那場戲跑在 `0x21`。
**字串出現在哪個 block ≠ 那場戲屬於哪個 block**——一個 block 裝得下不只一場戲。
未解的是幾何怎麼換成 `GEO4/0x25`（`0x22` 的 `LOAD FILES` 是 `21`）。

劇情文字的串接有可重生的分母了：`cmd/ecl-text-coverage` 依**控制流走訪**列出
原作的每一頁，控制流可達的 1022 頁中 `unmatched` 為 0（spec 1110）。
⚠ 這個數字在上一版是 197 頁——`ON GOTO` 沒被走訪、四個變長指令的長度沒算、
頁的歸屬用 offset 順序，三者疊起來讓當時的 `unmatched=0` 什麼都不代表。
剩下 `variable-insert` 16 頁（頁裡印的是執行期的值，靜態驗不到）要靠實機路徑驗。

**overlay 的 far call 現在解得出目標了**（spec 1112）。IDA 把 overlay 從位址 0
載入，於是 `call far ptr 014D:00B6` 被顯示成同一份 `.bin` 裡的 `loc_1584+2`——
規格裡那一票 `<far 1590h>` 都是這樣來的假位址。真正的目標是常駐段的 TPOV 控制
記錄：段選 overlay、位移選 entry。`cmd/far-call-map` 把段位移常數量出來
（DOS `7Bh`、PC-98 `9Ah`），**5,079 個 DOS 呼叫點 0 個解不出來**，並重現三筆
先前手工解出的目標。這把「這個 far call 做什麼」從逐支硬讀變成查表。

存檔那一項也有數字了（spec 1115）：DOS 角色記錄 422 bytes **逐位元組**都有狀態
（`decoded` 299／`documented` 99／`unknown` 24；解析器實際讀到 291／422），
而 `decoded` 是**量出來的**——
改一個位元組、重新解析、比對結果，有差別就是有讀；掃程式碼會漏掉算出來的索引。
⚠ 這個數字衡量的是**看得懂多少**，不是**存得住多少**：匯入保留整份原始記錄，
未知位元組原樣寫回。同一輪替 `Fighter` 建了「每個匯出欄位都要能存進讀回」的
反射閘，當場抓到一個真的 bug——新增 `StatusPartyFled` 之後，逃離結局的存檔
存得下去、讀不回來。

戰鬥系統依此接上三塊：**逃跑**（走出邊界即脫離，比雙方移動率，平手 1d2；
spec 1112／1113）、**AI 移動**（行為模式 1..6 決定五個候選方向、半格成本、
走到射程才攻擊；spec 1114）、**法術主表**（原作 100 筆全部匯出，16 個位元組
逐欄有出處；spec 1111）。

**回合開始那一段整段對回原作了**（spec 1135–1138）。先攻是
`敏捷遠程命中調整 + 1d6` 夾底 1，`20` 是快速戰鬥的哨兵值；
`CHECKFX(07h)` 移到分派 AI／玩家選單之前，先前玩家控制的隊員完全套不到。
**突襲遮罩（`Area2+596h`）是死碼**：全 build 只有一讀一清 0，先攻的 `−6`
走不到，CoAB 傳 0 是有證據的值（spec 1136）。保留中的機會攻擊 `+18Dh^[7]`
的設定端是 GUARD，設定／消費／清除三段接起來。面向、動作計數、累計轉向
三個欄位與背後攻擊的三道條件是**字面移植**（spec 1138）：`+09h` 的幾何意義
兩個寫入端互相矛盾，不解釋；第二個 AC 的值用 `ArmorClassFacingKnown` 留白。
90° 扇形判斷（`INARC`，spec 1002）八個方向讀完並接上機會攻擊的 180° 面向閘。
第二個 AC 也解出來了（spec 1000 §七）：`+19Bh ＝ +19Ah − 敏捷防禦調整 −
盾牌那一槽 − 2`，81 筆 `MON*CHA` 有 78 筆殘差為 0，另外三筆的殘差 1、1、2
正好是 ＋1／＋2 盾牌。

**AC 與命中的刻度也統一了**（spec 1139）。原作把兩者都存成 `60 − 顯示值`，
命中判定是 `d20 ＋ 攻擊者^[199h] ＋ v >= 目標 AC`（儲存值越大越難打）；
remake 一律用顯示刻度，換算後是 `d20 ＋ 命中加值 ＋ 目標 AC >= 20`。
FIRE KNIFE 自己打自己原作要 18，remake 也是 18。原作搬過來的算式
（`CanHitTarget`、ECL 隊伍戰力、`CHECKFX` 的護甲寫入）在邊界換回儲存刻度。

這一批機制**已經搬進共用 engine**：`combat/facing`（八扇形、90° 扇形、轉向記帳、
背後攻擊三條件、開場面向、180° 掃描）、`combat/armorclass`（刻度換算與命中門檻）、
`combat/ability`（敏捷防禦調整）、`combat/footprint`、`combat/dispel`、
`combat/aiscan`、`viewport.Camera`。CoAB 這一側只剩轉接與作品資料。
盤點與剩下該搬的見 [engine 抽取盤點](docs/knowledge/engine-extraction-review.md)。

戰鬥規則再往下三塊。**戰術地圖的兩種障礙格**（spec 1119）：`far 193Eh+1` 用 far call
對照表解出來是 `overlay-32 entry#19`，它把腳印四格各查一次，先前記成抽象旗標的
`b`／`c` 其實是兩個地形碼 `1Eh`（豁免得過就走）與 `1Ch`（`有號(+0E5h) ≥ 7` 就走），
兩張豁免效果清單只有部分重疊。**士氣崩潰**（spec 831）分四段各 10%／50%／20%／20%，
逃走那一段掛效果 `23h`、把 `+18Dh^[10h]` 設 1、士氣補滿、清掉目標。
**自動換裝的評分算式**（spec 1120）讀完了，但物品類別表 `DS:5CF6h` 在常駐資料段
是整片 `FF`——它是 BSS，所以缺的不是算式而是那張表的來源。

法術這一側改成兩張表撐起來。**效果修正表**（spec 1123）：`CHECKFX(timing)` 給
「這個時機有哪些效果要介入」，handler 給數字——141 個 handler 機械分類之後，
祝福是士氣 ＋5、緩速是移動折半、抗寒是冷傷害折半（**有條件**，看傷害屬性旗標）。
**傷害骰表**（spec 1124）：`+0Ah = 0` 的那一批骰子在各自的 handler 裡，
從法術分派表 `DS:72A0h` 一路解到 `NdM`，治療輕／中／重就是 `1d8`／`2d8`／`3d8`。

★ 覆蓋台帳的「已判讀」用最嚴的定義：碼要在某個 timing 裡、那個 timing remake
**真的有查**、它動的那一格 remake **真的有讀**。分母也修過——戰鬥可施放的 79 支
裡有 6 支職業模型不支援（德魯伊、表裡沒有職業），可宣告的是 **73**。
逐支讀完之後**宣告數 73／73，可宣告的分母歸零**（spec 1125）；`handler`／`visual`／
`sound` 三欄都是 observed。剩下的不是接線缺口：13 筆占位玩家取不到、8 支只能紮營、
6 支職業模型不支援。

原版存檔匯入（spec 1121）走上編碼分流：`CHRDAT?.sav`／`.swg` 的版面同時被原版與
remake 自己的槽使用，光看檔案分不出來源，所以由呼叫端明講
（`LoadOriginalSAVGAMSlot`、CLI `-savgam-import`）。⚠ **英文樣本測不出這件事**，
兩條路在 ASCII 上結果完全相同。

## 第一人稱畫面已經有逐格的原版對照（第 572 輪）

提爾佛頓（`GEO2` 區塊 `01h`）**五格 × 四個朝向、共二十張**畫面與原版逐格比對：
十九張完全相同，一張差一格。比的是 88×88 可見區在 EGA 量化之後的 16 色索引
——DOSBox 與 remake 對同一個 EGA 顏色算出的 RGB 差幾個階，直接比 RGB 會有九成
的格子「不同」，把真正的差異埋掉。

修的是天花板：`gamepack` 把 `tilverton.first-person` 的 `indoor_sky_color` 宣告成 0
（→ `skyPalette[0]` ＝ 黑），原版的 runtime 值是 **9**（→ `skyPalette[1]` ＝ 白），
`outdoor_sky_color` 3 → **11**。★ 兩個數字取自原版**在地圖上**紮營存檔寫出來的
`Area1`；從**選單**存的那一份兩個都是 0——**同一支存檔常式，存的時機不同，
欄位意義不同**。

取 oracle 的方式也換掉了：原本是「猜一整串定時按鍵、等一輪、看結果」，
任何一步的載入時間漂移都會讓後面每一個鍵錯位。現在容器常駐、逐步送鍵、
每一步把畫面上的 8×8 文字格讀成字串再決定下一鍵
（`tools/dos-oracle-session.sh` ＋ `tools/dos_screen.py`），一步約一秒。

順帶確定的三件事：`CURSE.CFG` 是四行純文字（顯示介面卡／音效／存檔路徑／第四行），
留著它開機三題整個跳過；DOS 存檔 13,149 bytes 與 PC-98 13,214 是平台差異，
兩份規格各自對自己的平台；存檔裡的角色檔是 `CHRDAT<槽><序>.SAV` ＋ `.FX`，
**檔名欄指到不存在的檔案時原版不報錯、直接跳過那名角色**，八筆全跳過就是空隊伍
——畫面上看起來與「拒絕讀檔」一模一樣。

還開著的：其餘 17 張 `first_person` 地圖的天空色仍是宣告值。那兩個欄位**不是
純存檔狀態**（清成 0 之後進地圖會被重算回來），而改存檔的 `Current3DMapBlockID`
到不了別的地圖（同樣被重算），所以要拿別張地圖的值只能真的推劇情，或用
runtime watchpoint 找出誰寫它。見 [spec 1134](docs/spec/1134-original-first-person-oracle.md)。

其餘系統的逐項狀態一律查完整度矩陣，不在本檔重述。

## 已被推翻的斷言

一筆紀錄只有在**錯誤版本還讀得到**時才值得留：來源文字沒改乾淨、或那個錯誤
容易被下一個人重新推導出來。兩種情況都有更好的處置——

- 來源沒改乾淨 ⇒ 直接改寫來源正文成正確答案，不要靠這張表補救。
- 容易重新推導 ⇒ 寫成規則放進 [`AGENTS.md`](AGENTS.md) §3「容易重犯的反組譯與
  實作錯誤」，或在該規格正文標成「這裡容易擺錯」，並儘量加一條回歸測試擋住。

兩者都做完之後，紀錄本身就沒有讀者了。**目前沒有需要保留的條目。**
逐輪的經過寫在當月分冊，那是追溯用的檔案，不是現況。

## 尚未完成（摘要）

完整 ECL 有序副作用與 external routine、全城市／全房間 coverage、完整戰鬥
規則與敵方 AI、原機音訊、全量繁中校對、完整存檔相容、開場到結局的同一
session，以及三平台發行。

## 歷史分冊

| 分冊 | 範圍 |
|---|---|
| [`10-architecture-and-boundaries.md`](docs/context/10-architecture-and-boundaries.md) | 獨立 engine／JSON game pack 的分工與早期確認事項 |
| [`20-rounds-through-347.md`](docs/context/20-rounds-through-347.md) | 第 1–347 輪工作序列與成果 |
| [`30-log-2026-07.md`](docs/context/30-log-2026-07.md) | 2026-07-28 至 07-31 |
| [`40-log-2026-08-01-03.md`](docs/context/40-log-2026-08-01-03.md) | 2026-08-01 至 08-03 |
| [`50-log-2026-08-09-13.md`](docs/context/50-log-2026-08-09-13.md) | 2026-08-09 至 08-13 |
| [`60-log-2026-08-14-18.md`](docs/context/60-log-2026-08-14-18.md) | 2026-08-17 至 08-18（第 565–572 輪）|

分冊只作追溯：裡面的「目前」「下一步」都是當時的狀態，不得當成現況引用。
新的輪次紀錄一律寫進當月分冊，本檔只更新現況段落。
