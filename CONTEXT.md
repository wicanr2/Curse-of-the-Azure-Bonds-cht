# 專案現況

更新日期：2026-08-23（第 730 輪：**「實跑踏到」本來漏掉大半的步——聯集 210 → 214**（`cmd/route-cells`／spec 1193）——★★★ 那一欄的來源是**觀測迴圈**不是每一步移動，走過去又馬上被劇情推走的格子從來沒被取樣；報表自己標明是下界，但一直沒有辦法補；★★ 上一輪為了按鍵重放而錄的 `COAB_DECISION_LOG` 剛好就是 `MoveDungeon` 的**每一次**呼叫 ⇒ `cmd/route-cells` 把 824 步移動的起點換算成 (ECL 段, 地形碼)、得到 119 個組合併進「實跑踏到」；⇒ 實跑 191 → **198**、聯集 210 → **214**、未達成 48 → **44**、覆蓋率 81% → **83%**；★ **同一條路跑同一次，只是記得比較細**——取樣密度也是覆蓋率的一部分；⚠ 錄的是**起點**座標，最後一步的終點會漏掉（仍是下界，只是密很多）；⚠ `0x04`／`0x12`／`0x21` 沒有地形分派，那 154 步換算不出地形碼（本來就不在分母裡）；第 729 輪：**視窗要算「往前看幾個移動」不是「幾筆紀錄」——83 格／66 句**（spec 1191）——★★★ 路線裡 `select` 佔了一半（783／1607），照**筆數**算視窗的話那些「請按任意鍵繼續」會把視窗塞滿、下一個 `move` 被擠到範圍外 ⇒ **看起來像跟丟，其實只是沒看到**；★★ 改成只算 `move` 之後視窗才開得大，實測 3→82／57、8→82／57、**20→83／66**、40→83／66、80→43／10（退回只有 `0x01`）⇒ 取 20；⚠ **照路線按的次數變少（123→76）但走得更遠**——次數本身不是目標，它只是「跟得上路線的程度」的一個側面；★ 試過而**沒有**用的：跟丟時用廣度優先**朝下一個路點走過去**——只觸發 4 次，而且格子從 82 掉到 71（`routeMove` 的視窗與重新對齊已經涵蓋絕大多數情況，真的兩者都失敗時那一段的路點通常也用完了）⇒ 記進 spec 免得下次再實作一次；⇒ 連續按鍵那一場定案在 **83 格／66 句／2 段／落回原文 0**；第 728 輪：**重放跟丟時的重新對齊——三種寫法差很多**（spec 1191）——★★ 主線與按鍵這一場走的路不完全一樣，視窗一過就對不上；視窗裡找不到時拿「**現在在哪一段的哪一格**」去整條路線裡找同樣的起點；⚠ **一定要帶段號**：不同段的地圖上都有 (7,13)，只比座標會對齊到完全不相干的地方⇒ `move` 紀錄改成連 ECL 段一起錄；★★★ 三種寫法實測（1500 幀）：**不對齊** 76 格／63 句／104 步；**對齊＋把指標跳過去** 41／18／34（中間步驟被整批丟掉）；**對齊＋指標不動** 57／47／759（同一格一直對齊到同一步，原地震盪）；**對齊＋用過就記** **82／57／123** ← 目前這一版，兩個問題都避開；⚠ 代價是話少了 6 句（63 → 57）——**兩邊都不是免費的**，換走法要重新量；第 727 輪：**路線知識用錄的不用手抄——按鍵那一場第一次走出開場段**（`COAB_DECISION_LOG`／spec 1191）——★★★ 上一輪確認「限制是路線知識」，而那份知識散在戰役測試三千多行裡，手抄只會抄錯；★★★ 改成**錄**：`State.Select` 與 `MoveDungeon` 是所有玩家層級動作的唯一入口，主線跑一次就錄下 **1607 步（783 選擇 ＋ 824 移動）**，重放端用**按鍵**把游標挪到那一項、把身體轉到那個方向；⚠ 錄的是**選項文字**與**起點座標＋方向**不是索引與終點（索引會隨選單錯位，樓梯要站對方向踏上去才觸發）；★★★ **只錄選單是不夠的**：第一版 783 個選擇只用得到 13 步——**主線的決策點要走到那裡才會出現**，而「走到哪裡」本身就是路線知識的一半；補上移動之後 28 → **76 格**、37 → **63 句**、ECL 段 1 → **2**（第一次走出 `0x01`）、照路線按 **104／1607** 步；★★ `routeLookahead` 是**量出來的**：2→完全跟不上、4→16 格、**8→76 格**、12→72、24→41、100→48⇒ 兩邊都不是線性的，改動走法之後要重新量；⇒ 量測列格式加上「走到幾個 ECL 段／照路線按了幾步」；⚠ 仍**不是**「按鍵玩到結局」：104／1607 步之後就跟丟了；第 726 輪：**確認按鍵那一場的限制不在走法也不在選單策略上**（spec 1191）——★ 卡住偵測的簽章原本含訊息，而商店每按一項就換一句回應 ⇒ 計數永遠歸零、**卡住偵測等於沒有**，而「離開商店」是最後一項，整場困在店裡；改成只看選項後 36 → 37 句；★★★ 然後把三個方向都試到底：**幀數 600→4000 不變、卡住門檻 6→3→2 不變、改成輪流選變差**（28→27 格、37→16 句）⇒ **限制不在走法也不在選單策略上**，缺的是**路線知識**（戰役測試裡逐段寫死的那一份）；★ 這一條寫進 `AGENTS.md`：三個方向都試過而且都不動，才有資格說「不是這裡的問題」——只試一個就換方向會把時間花在調沒有用的旋鈕上；⇒ 按鍵那一場定案在 **28 格／37 句／4 種畫面／落回原文 0**，只到 ECL `0x01`；第 725 輪：**「卡住才換選項」讓按鍵那一場 23 → 36 句，並挖出第二個玩家看得到的缺陷：游標沒有夾範圍**（spec 1191）——★★ 「永遠按第一項」會**卡在同一個選單上**（世界地圖的「進入城市｜繼續旅程｜紮營」永遠選第一項，隊伍就離不開開場那座城）；折衷是**同一個選單看到第 6 次還在原地才往下挪一項**——預設仍是第一項，所以不會像輪流選那樣自己切斷路線 ⇒ 記到的話 **23 → 36 句**；★★★ **第二個缺陷**：`choiceCursor` 是前端自己的狀態、選項是 ECL 換頁時整批換掉的，商店選單五十幾項挪到第 8 項之後換到只有三項的荒野選單，游標還是 8 ⇒ `Select` 回 `choice 8 is invalid in mode 1`，**玩家看到的是一行錯誤訊息**；原本的重設只發生在幾條特定路徑（選完一項、載入存檔），涵蓋不了所有換頁 ⇒ `Update()` 每一幀先 `clampChoiceCursor()`，並加了四個方向的迴歸測試；⚠ 走過的格子仍是 28、走到過的 ECL 段仍只有 `0x01` ⇒ **要真的離開開場那一段，還是得把戰役測試那份路線知識搬過來**；第 724 輪：**按鍵那一場 19 → 28 格，並確認「多策略」不能搬到連續 session 上**（spec 1191）——★★ 前幾輪修好的「邊界要記（格子, 進入方向）」也適用按鍵那一場：只記格子的話，第一次從錯的方向踏上樓梯格就把那一格封死 ⇒ 改掉之後 **19 格 → 28 格、15 句 → 23 句**，而且第一次走進了**場所**（城裡的店家）；★★★ **反過來的教訓**：試著把冷走那邊很有效的「多策略選單」搬過來，結果**變差**——走過的格子 28 → 27、記到的話 23 → 16，因為輪到「離開」那一項就真的離開了，整條路被自己切斷；⇒ 冷走多策略有效是因為它**每一種策略各跑一趟再取聯集**，而按鍵這一場是**一條連續的 session**，換選項不是多一條路是**換**一條路——**同一個技巧在兩種量測上效果相反，不要因為在一邊管用就搬到另一邊**；★ 600 幀就到頂（跑到 3000 幀還是 28 格），所以不為了「看起來跑得久」拖慢測試套件；第 723 輪：**輸入層從開場擴到整條主線：114／114 推得動、落回原文 0，並修掉一個玩家看得到的缺陷**（`TestKeysDriveEveryCampaignSnapshot`／spec 1191）——★★ 開場那條只證明遊戲開得起來、前幾段按得動；主線後面那些段前端的 `Update()` **從來沒有在那些狀態下被跑過**；把主線各段的快照逐份**讀進真的 app** 用按鍵推，就補上了那一半；★★★ **第一次跑：114 份裡有 104 份按什麼都沒反應**——成因是 `LoadPartyFile` 設了 `GeoMapSet`／`GeoMapBlock` 卻沒設 `geoMapPending`，而前端的 `geoGrid` 只有收到 `ConsumeGeoMapRequest` 才會換 ⇒ 讀檔之後前端拿的還是**上一段的地圖**，牆、門、能不能走全部對不上而且**不報錯**；一開始沒有地圖時更直接：`moveDungeonPreview` 看到 `geoGrid == nil` 就 return，**按什麼都沒反應**；★ 兩個把「沒反應」判錯的陷阱：簽章**要帶朝向**（面對牆按 M 只轉身，少了朝向會把最常見的那一步判成沒反應）、以及**測試自己少載 `ITEM*.DAX`** 會讓 `TREASURE` 直接回錯、看起來像按不動；★★ 順帶接上巫師塔光球決鬥：距離那一頁（`0x1642`）**只有數字是變數** ⇒ 逐個數字寫規則就接上（10..70 呎），又一次「列舉不到的東西用走的走得到」；⚠ 另一頁（`0x147B`）印的是**玩家自己取的隊員名字**，固定譯文會把名字吃掉 ⇒ 規則層故意不接，放進一份故意很短、不會自動長大的豁免清單；⇒ 量測列 25 → **26**；⚠ 明確不宣稱：這**不是**「按鍵從頭玩到結局」——每一份是各自載入的，證明的是那些狀態下輸入層是活的，不是一路按過去到得了；第 722 輪：**一段要存好幾份快照——聯集 204 → 210**（`captureInsideSegment`／spec 1193）——★★★ 從**同一個起點**走，走得到的就是那一塊連通分量；`ECL4/0x22`（眼魔洞穴）那種段主線是靠劇情事件一路被帶進去的，只存「第一次進段」那一份等於只拿到入口那一小塊（16 格／2 個索引）；★★ 改成沿路每隔幾次觀測存一份（每段最多 6 份、至少隔 6 次觀測）⇒ 冷走 194 → **205**、聯集 204 → **210**、未達成 54 → **48**、覆蓋率 79% → **81%**；`ECL2/0x02` 3 → **7（滿）**、`ECL4/0x22` 7 → 9；★ 份數不能無上限：走訪時間是「份數 × 選單策略數」，而同一塊裡多存幾份走出來的東西幾乎一樣；★ 主線不經過的段（提爾佛頓那三段）要在**那條路線的測試**裡也存，否則 `cmd/cell-sweep -snapshots` 對那些段沒有備用入口；第 721 輪：**分母本來漏了兩段——因為它們「冷進不去」**（`cmd/cell-sweep -snapshots`／spec 1193）——★★★ 逐格實測每一段都**冷開一支隊伍**走進去，而有些段的入口是**一整段過場**（巫師塔是德拉坎德羅斯那一串、`ECL4/0x25` 是魔法商店那一段），冷走完會停在**世界地圖**而不是地城 ⇒ 那些段整段被記成「進不去」、**格子從來不在分母裡**——而玩家真的走得到；★★ 修法是冷進不去時改從**主線的段內快照**進（上一輪剛做的 `captureInsideSegment`）⇒ 分母 **250 → 259**、聯集 **195 → 204**、覆蓋率 78% → **79%**；⚠ 從快照進的段隊伍**帶著主線那一刻的旗標**，報表在那一段標明，不要讀成「冷開就走得到」；★★★ **一個安靜的坑**：第一版把來源註記寫進 `blockSweep.note`，但 `note` 的語意是**這一段掃不了**、渲染時看到就整段跳過 ⇒ 一段**掃得好好的**段從報表裡消失、分母從 259 掉回 250，**而且沒有任何錯誤訊息**；註記要用自己的欄位（`fromSnapshot`）；第 720 輪：**三支走訪器都有同一個結構性缺陷：邊界只記格子不記方向**（spec 1193）——★★★ 上一輪的結論是「剩下的是幾何，房間要靠樓梯事件才進得去」，追下去發現**問題出在我自己的走法**：樓梯事件是**站對方向踏上去**才觸發的（`ECL5/0x33:090Ch` 查表拿方向，朝向不對就直接 `EXIT`、畫面上什麼都不會發生），而三支走訪器都只用**格子**當 `seen` 的鍵 ⇒ 第一次從錯的方向踏上去就把那一格封死，另外三個方向永遠不會再試，**靠樓梯才進得去的樓層永遠走不到**——而報表只顯示「那幾格走不到」，看不出是走法的問題；★★ 改成邊界記 `(格子, 進入方向)`、「去過沒有」另外記之後：巫師塔 `ECL5/0x33` 從 **15 格／2 個地形碼 → 207 格／22 個**、冷走索引 198 → **220**、實跑踏到 177 → **182**；⚠ **但聯集仍是 195**：多出來的大部分落在 `0x33`／`0x25` 這些**不在分母裡**的段，分母內多出來的又已被另一把尺涵蓋 ⇒ **修正是對的和指標會動是兩件事**，分開講才不會下次以為「走法沒問題，是內容不夠」；第 719 輪：**補上段內快照之後，「門要旗標」的否證才站得住——仍然只有 4 個**（`captureInsideSegment`／spec 1193）——★★★ 上一輪用**段界**快照否掉「門要劇情旗標」這個假設，但**證據不夠**：23 份裡有 7 份讀回來停在荒野／地圖模式（`ECL4/0x22` 眼魔洞穴就是一份）⇒ **走不了的那幾段根本沒被驗到**，結論看起來一樣但撐不住；★★ 於是讓主線在**第一次踏進每一段的地城時**也存一份，拿到 13 份段內快照，涵蓋 12 個有地形分派的 block **全部**，這一次每一份都讀得回來也走得動（只有 `0x50` 世界地圖沒有地形分派，如實報出）；★★★ 結果**仍然只有 4 個**索引是非得帶旗標才走得到的 ⇒ 結論不變而且證據變強：剩下的絕大部分不是門是幾何——房間要靠樓梯／傳送事件才進得去，而事件要在**劇情的正確位置**才觸發，**不是「有沒有旗標」是「當下在不在那個位置」**；⇒ 聯集 194 → **195**、未達成 55 → **54**；★ 段內快照每段只存第一次進地城那一刻（存太多份會讓走訪時間乘上份數，而段內旗標差異對「走得到哪些格子」影響很小）；第 718 輪：**「門要劇情旗標」這個假設被自己的工具否掉了——只解釋 3 個**（`cmd/campaign-snapshot-walk`／spec 1193）——★★ 未達成的最大一類是「站得上去但從段入口走不到」，直覺成因是**門要旗標才開得了**（冷走每段都開新隊伍、沒有旗標）；★★★ 新工具直接驗這個假設：拿主線在各段存下的 **23 份快照**（就是一般存檔格式、帶著那一刻的旗標）在那一段做同樣的多策略走訪 ⇒ **只有 3 個索引是非得帶旗標才走得到的**；⇒ 所以剩下的絕大部分**不是門是幾何**：那些房間要靠樓梯／傳送事件才進得去，而事件要在劇情的正確位置才觸發——快照停在段落**結束**處，往往已經離開那一塊了；★ 要再往前推得讓主線在**段落中間**也存快照；★★ **讀存檔取段號的陷阱**：`Area.LastECLBlockID` 是存檔格式裡的欄位但**遊戲跑的時候從來沒有人寫它**，讀出來是 0——而 0 是**合法的 block 編號**，查下去會拿到完全不相干的地圖而且不報錯 ⇒ 段要跟 ECL session 拿（新增 `State.CurrentECLBlockID`）；⇒ 聯集 193 → **194**、未達成 56 → **55**；⚠ 23 份裡有 7 份讀回來停在荒野／地圖模式（`ECL4/0x22` 眼魔洞穴就是一份），報表逐份列出停在哪裡，**沒有靜默跳過**；第 717 輪：**同一招套到實跑路線上，一次抓出 15 句沒翻譯的英文**（`internal/game/tilverton_route_test.go`／spec 1193）——★★★ 上一輪的「多策略取聯集」對冷走有效，套到實跑路線上**立刻抓到 15 句落回原文**：13 則酒館傳聞（第 3／9／13／15／17／20／24／31／43／45／52／56／59 則）、酒保的「那，要為各位送上什麼？」、以及羅剎妖收買路財那一段——**全部都是單一策略永遠走不到的岔路**（選第一項會被收費關卡與「不喝」擋掉）；★★ 傳聞內容取自冒險手札（Adventurer’s Journal）的 TAVERN TALES 段，逐則翻譯並沿用既有譯名（蔓生怪／深坑／摩安德／散提爾堡／尤拉什／特什河／羅剎妖）；★★★ **順序是正確性的一部分**：`all_contains` 是子字串比對而 `MatchText` 先命中先贏，而 `TAVERN TALE 3` 是 `TAVERN TALE 31` 的子字串 ⇒ 兩位數的規則一定要排在一位數前面，否則第 31 則會被第 3 則攔走，**而且兩邊都是合法的傳聞、畫面上看不出錯**；`TestTavernTaleRulesDoNotStealEachOther` 釘住它，並且做過正對照（把第 3 則搬到前面 → 測試如預期紅在「第 31 則被 world.tavern-tale-3 攔走」）；⇒ 實跑踏到 **153 → 177**、text run matched 1199 → 1200、orphan 仍 0；⚠ 聯集不動（193）：那 24 個索引冷走本來就走得到 ⇒ **這一輪的價值不在聯集，在於實跑那一欄帶著語系不變量**——冷走不看文字，抓不到這 15 句；第 716 輪：**走訪聯集 178 → 193，成因是冷走只跑了一種策略**（`cmd/dungeon-walk-probe`／spec 1193）——★★★ 上一輪把 72 個未達成拆成成因之後，下一步本來以為是「補路線」，結果**真正的槓桿在冷走自己**：它只跑一種策略，而**單一策略的結果看起來都很合理**；★★★ 三個各自獨立的擋點：(a) 選單挑第一項會被**收費關卡**擋在外面（下水道的奧提尤格要食物）、(b) 挑最後一項會在「要離開嗎」那種提示上直接走人、(c) **踏上去被事件搬走時不從落點繼續**——等於把「走上樓」記成「走到樓梯口」，靠樓梯才進得去的樓層永遠走不到（巫師塔每層是獨立房間，spec 1161）；★★ 改成跑 8 種（選第 1／2／3／最後項 × 跟不跟傳送）取聯集 ⇒ 冷走 153 → **179**、聯集 178 → **193**（天花板 249 的 78%）、未達成 72 → **57**（站得上去但走不到 56／四面不通 1／死索引 0）；★★ **跟傳送單獨看反而更差**：`ECL6/0x43` 的格子 191 → 235，但順序一變 once-only 事件換地方觸發，索引反而少兩個 ⇒ 所以是**取聯集不是換一種**；★ 再加第 4 項一個索引都沒多、跑一趟多兩分半 ⇒ 收斂了，就停在 8 種；★ 同一個「丟掉落點」的洞在 `tilverton_route_test.go` 也修了（實跑 152 → 153）；★ 順帶把報表裡兩個**寫死的例子**改成算出來的——走法一改它們就變了，而寫死的數字不會有任何東西提醒你它已經不對；第 715 輪：**走訪的 72 個未達成拆成了成因——而且死索引是 0，所以全部都是真的待辦**（`cmd/cell-reachability`／spec 1193）——★★ 可達性報表說 250 個分派索引、聯集 178，剩下的 72 只是一個數字，**看不出要補路線、要補劇情旗標、還是根本補不了**；★★★ 新增兩份純幾何的輸出（`-walkable-json` 站得上去、`-on-map-json` 圖上有），把 72 拆成 **71 站得上去但從入口走不到／1 四面都不通／0 圖上根本沒有**；★★★ **死索引是 0 這件事本來只能猜**——它成立才表示未達成全部都是真的待辦、不是分母灌水；★★ 順帶算出**走路的上限 249**（250 減掉那個四面不通的）：**那才是天花板不是 250**，把分派索引總數當目標會讓永遠差的那一格看起來像還有事情可做，目前 178 ＝ 天花板的 71%；★ 「站得上去」要另外算的理由是**一張圖常常不是一整片**：巫師塔每一層在 GEO 上是獨立房間、只靠樓梯**事件**接（spec 1161）⇒ 「從段入口走得到」是下界、「站得上去」是上限，差額就是要靠事件才進得去的房間；⚠ 分類只看幾何不跑 ECL——走過去又被劇情推回來那種不算「走不到」，那是內容不是幾何；⚠ 所以 71 裡還混著兩種（幾何斷開 vs 門擋著），要再分得看門的旗標；第 714 輪：**原機音訊從「只有分母」變成有覆蓋率——而缺的兩種都卡在共用 engine**（`cmd/audio-lifecycle-audit`／spec 1192）——★★ `cmd/dseg-writers` 早就盤出六格音訊狀態的 17 處寫入，但那一列自己寫著「⚠ 還沒做的是**下一步**：把這些寫入點對回遊戲事件。分母有了，對照還沒做」——這一輪把它做掉；★★★ **`stop-track` 沒有自己的格，它是一個「值」**：`MUSICNUM := 255`（沒有曲子）——只按位址分類會把「停止」算成「載入」，於是原作明明會停音樂、報表卻說這一類已經接上了；改成看值之後停止的證據有三處（`2F46h` `MUSICSW:=0`、`2F5Ah`／`9451h` `MUSICNUM:=255`），而 `9451h` 落在 `9400h..9520h`——同一段裡有全部 7 處 `MUSICNO` 寫入 ⇒ 那是**派曲常式**：挑不到就寫 255；★★★ **「發得出來」與「有東西會發」要分兩欄問**：規則層有這個動作是能力，有哪一段劇情會發是有沒有用到，混一格會讓「寫了但沒人呼叫」看起來像做完了；⇒ 原作 7 種動作，規則層發得出 7 種、game-pack 真的會發 **5** 種；★★★ **缺的兩種都是「停」，而卡點不是「還沒寫那條 binding」是「寫不出來」**：共用 engine 的 pack 驗證把 `track_id` 空的 binding 判成 `references unknown track ""` ⇒ 「這裡不放音樂」在 pack 裡沒有任何寫法，要動共用 repo 才解得開；★ 這兩種卡點處置差很多（補一行 JSON vs 動共用 repo），所以報表那句話是**跑出來的**不是手打的，`TestEnginePackCannotExpressStopYet` 把 engine 的限制釘住——**它一旦紅代表鬆綁了**，那時要補 binding 不是刪測試；★ 順帶修掉一個潛在缺陷：空的 `TrackID` 本來會發成 `play`，然後在 adapter 查無此曲**只留一行 log**，音樂繼續放著、測試照樣綠；⇒ 量測列 24 → **25**；⚠ 明確不宣稱：「接上了」只表示 remake 有對應動作，**不表示在正確的時機發**（那要實機比對）；PC-98 版面，DOS 沒有音樂、位址不能套；第 713 輪：**輸入那一層第一次被跑到——按鍵從標題玩到開場，「開場到結局」有了實測的一半**（`cmd/azure-bonds-game/keys.go`／spec 1191）——★★★ 那一列缺的不是劇情也不是畫面，是**中間那一層**：戰役測試直接呼叫 `state.X()`、spec 1188 讓前端把檢查點畫了一張，但前端的 `Update()` 在整條戰役裡**一次都沒被跑到**，所以「這段劇情按得出來嗎」在報表上一直是空的；★★ 前端 152 處直接呼叫 `inpututil.IsKeyJustPressed`／`ebiten.IsKeyPressed`，全部改走 `keySource` 介面（`a.keys` 是 nil 就退回真的 ebiten，正式路徑不變）；★★★ **接縫要有守門員**：繞過去的那一行在按鍵驅動的測試裡永遠不會觸發，而測試會照樣綠——它根本走不到那裡，所以 `TestFrontendReadsKeysOnlyThroughTheSeam` 把新的直接呼叫變成紅燈；⇒ `TestKeysDriveARealSessionFromTheTitle` 從標題開始**只按鍵**：Enter 進角色建立、Enter×6 建隊、D 完成、Enter 推開場劇情（醒來→枷印→旅店老闆娘→手札第 31 條）、M 轉向 ＋ ↑ 前進走 **19 格**，沿路套與戰役同一條語系不變量，**落回原文 0 句**；★ 走不遠踩了兩個坑，都已定位：(a) 挑方向要**兩輪**（先找沒去過的、找不到才退回走得通就好），只有第一輪會在只剩回頭路的死巷**原地轉圈**；(b) **門在 `CanMoveDungeon` 眼裡是牆**，只挑「走得通」的方向就永遠不會去碰門，而玩家的做法是**撞上去**（`moveDungeonPreview` 發現是門就開選單）——而且「撞門」不能掛在「四面都是牆」底下，那個條件永遠不成立，要掛在「沒有沒去過的格子可走」時；⇒ 7 格 → **19 格**、6 句 → **15 句**；★★ **停在 19 格的原因已查明不是選項**：整場沒出現任何真的選單（只有「按任意鍵繼續」）、也一次都沒撞到門 ⇒ 是幾何上到不了，不是選錯——這兩種成因處置完全不同；⇒ 量測列 23 → **24**；⚠ 明確不宣稱：這**不是**「按鍵玩到結局」，是開場那一段；7 格是這一場的實測不是上限（同一段幾何走訪找得到 83 格，差別在門與選項）；落回原文 0 只涵蓋這一場的 6 句；第 712 輪：**這一類規則錯法不必再靠運氣撞到了——覆蓋率改以執行路徑計，一次補掉 29 條規則**（`cmd/ecl-text-coverage`／spec 1190）——★★★ 前兩輪各靠實跑撞到一句英文，成因都一樣：規則的 `all_contains` 含了**兄弟句**的片段，兩句一起印時命中、單獨印時永遠命中不了；★★★ 關鍵發現是 **`ecl-text-coverage` 對這一類是全盲的**——把那兩條修好的規則從 pack 拿掉重跑，`unmatched` **仍然是 0**；成因是處置判在**頁**上，而一頁只要曾被某一條命中的 run 經過就算接上，**玩家走的卻是一條 run 不是所有 run 的聯集**；★★ 於是改成以 **run**（一次交給 `MatchText` 的那份文字）計，豁免沿用同一套只是提升到 run，再加一類 `truncated`（停在句子中間或是別條的前綴 ⇒ 走訪器在 `IF` 分岔上切出來的半截，依據是原作只在**選單／戰鬥／寶物／輸入／換 block／離開**才把文字交出去，那些點都在一句講完之後）；⇒ orphan run **93 → 0**，中間一次補上 **29 條規則 ＋ 兩個語系譯文**，涵蓋立石群、檢查哨、酒館與小酒鋪、競技場三種怪、絲綢分開買黑心與黑卵、少女答謝、藤蔓只纏住龍餌（愛麗雅絲不在隊上）、紅羽衛陷阱警告等；★★ **這個零撐得住**：拿掉任一條規則 `runs_orphan` 立刻變成拿掉的條數並逐條指名，而逐頁的 `unmatched` 在每一種情況下都還是 0 ⇒ 量測列 22 → **23**；★ 寫規則的順序是硬要求：新規則一律加在檔案最後（`MatchText` first-match-wins，既有的多片段規則才會先命中），新規則彼此之間片段多的排前面；⚠ 明確不宣稱：`orphan = 0` 只表示「有一條規則命中」，**不等於譯文正確**，也不等於那一頁的事件副作用已還原；剩下 62 條 `truncated` 是走訪器的產物不是待辦；★ 順帶把只問「單句有沒有規則」的靜態稽核工具丟掉——它產出 236 條候選卻**驗不出**那兩個已知缺陷，比 run 級的弱且會誤導；第 711 輪：**實跑路線擴到 12 段，再抓到一句沒翻譯的英文——同一種規則錯法**（`internal/game/tilverton_route_test.go`）——★★ 上一輪的提爾佛頓路線一跑就抓到 bug，於是把同一套走法擴到 12 段（開場三段 ＋ 猶拉什 ＋ 散提爾堡 ＋ 眼魔洞穴 ＋ 哈普村 ＋ 古熔岩洞 ＋ 密斯卓諾三段），沿路記到 263 句話；★★★ 又抓到一句：巫師塔的 `AN EFREET LEADS A BAND OF DARK ELVES AGAINST YOU.`——**和上一輪完全同一種錯法**：規則 `wizard-tower.sucked-into-sphere` 的 `all_contains` 把它和「被光球吸走」那句綁在一起，而那是**兩條分開的 ECL 字串**；★ 判定它是規則寫錯而不是片段的依據是**兄弟句**：同一組的 `A SULFEROUS SMELL…` 與 `A PARTY OF DARK ELVES…` **各有一條獨立規則**，就它沒有 ⇒ 補一條 `wizard-tower.efreet-band`；★ 另外兩句（`WHAT DO YOU DO ?`、`DOES ANYONE WANT TO GO AND OPEN ONE?`）判為**問句／子程式片段**：實機會被併進呼叫端那一頁，兄弟句也都是併在前一頁的規則裡；`ecl-text-coverage` 明文說**不要**替它們寫規則（短片段的 `all_contains` 會攔截別的文字），所以放進一份**故意很短、不會自動長大**的 `routeKnownFragments`，⚠ 任何**新的**英文落回照樣讓測試紅；⇒ 實跑踏到 101 → **152**，⚠ 但**不要讀成「玩到六成」**：那條路線用的是廣度優先走訪、方法與冷走相同，所以這一欄往冷走收斂而**聯集幾乎不動（177 → 178）**；★★ **它的價值不在數字，在於它是帶著語系不變量的測試**——兩輪各抓到一句玩家看得到的英文，而 `ecl-text-coverage` 看到規則在、譯文在就算「已接上」，**結構上看不到這一種洞**；第 710 輪：**把提爾佛頓的實跑路線補上了，第一次跑就抓到一句沒翻譯的英文**（`internal/game/tilverton_route_test.go`）——★★★ 上一輪證明提爾佛頓「走得進去、只是主線不經過」之後，剩下的就不是盤點而是**把路線補上**；`TestTilvertonRouteIsWalkableAndLocalized` 真的走了 `ECL2/0x01`／`0x02`／`0x03` 三段（83＋65＋69 ＝ 217 格），並套用與主線同一條語系不變量；★★★ **第一次跑就抓到一句沒翻譯的英文**：下水道那句 `THEY COLLECT MONEY AND EQUIPMENT…`；成因不是漏譯——譯文早就寫好了，是規則 `tilverton.sewers.spoils-and-master` 的 `all_contains` **要求了一段屬於隔壁訊息的字**（`A SOUND, SUDDENLY CUT OFF` 是另一條 ECL 字串），所以那兩句合併出現時才對得上，單獨出現時**永遠對不上**；★ 修法是**加一條只要求本句兩個片段的規則，放在合併版之後**——`MatchText` 是 first-match-wins，放前面會把合併版的文字搶走；兩個語系檔都補了對應的譯文；★ 順帶修掉開場那一段被跳過的原因：`ECL2/0x01` 走的是 `BeginAdventure`，它要的是**角色名冊**不只是戰鬥員，少了名冊會回「adventure requires a created or loaded party」，整段被 skip 掉而測試照樣綠；⇒ 實跑踏到 **81 → 101**、聯集 **176 → 177**，而且**「一格都沒踏到的段」歸零**；⚠ 明確不宣稱：這**不是**「從新遊戲玩到提爾佛頓」（進段是直接進的，那三段本來就是開場），走法是廣度優先的幾何走訪不是劇情路線，隊伍也是撐起來的 ⇒ 仍然是下界；第 709 輪：**提爾佛頓不是走不進去，是主線不經過——覆蓋率 32% → 70%**（`dungeon-walk.md`／`cell-reachability.md`）——★★★ 上一輪的「主線一格都沒踏到提爾佛頓」有**兩種完全不同的成因**：(a) 主線不經過那裡（路線的選擇）或 (b) 那些格子走不進去（缺陷）——而逐格實測分不出來，因為它把隊伍**直接放**到目標格上；★★ 新的 `cmd/dungeon-walk-probe` 從段入口出發，只用 `CanMoveDungeon`／`MoveDungeon` 廣度優先地走：**提爾佛頓三段冷走走得到 25 個索引**（主線 0）⇒ **是 (a) 不是 (b)**；★★★ 而且兩把尺**互不涵蓋**：主線有劇情旗標、開得了冷走打不開的門（`ECL4/0x22` 主線 6 冷走 1），冷走沒有劇情擋路、走得到主線繞過的地方（`ECL6/0x40` 主線 2 冷走 22）⇒ **只報其中一個都會低估**，要看聯集：主線 81 ＋ 冷走 153 ⇒ **聯集 176／250 ＝ 70%**（上一輪報的是 32%）；⚠ 三個數都是**下界**：「沒到過」不等於「到不了」；冷走會被事件擋在半路，主線的記錄點是觀測迴圈不是每一步移動；冷走也**不是**「從新遊戲玩到那裡」（進段是直接進的），它答的是**段內**可達性；⇒ 走訪那一列的分母**全部補齊**，剩下的是覆蓋率本身——要提高得補走那些地方的**實跑路線**，那是新的測試工作不是新的盤點；`remake-status` 量得到的列維持 22（可達性那一列從一個數字變成三個）；第 708 輪：**走訪可達性有數字了——81/250，而提爾佛頓整段沒有任何實跑路徑經過**（`cell-reachability.md`）——★★★ 走訪最後缺的是可達性，而我上一輪寫的是「要實跑資料，靜態答不了」——**實跑資料就在手上**：`TestRealNewGameRunsToTheEnding` 從角色建立打到提朗瑟克斯就是；在觀測迴圈記下當時的 (ECL block, 地形碼)，整條主線記到 **127** 組；★★ `cmd/cell-sweep` 把隊伍**直接放**到目標格上、還把隊伍撐起來，所以它答的是「這一格演不演得出來」**不是「走不走得到」**；兩者對照 ⇒ **250 個分派索引裡主線實際踏到 81 個（32%）**；★★★ 分母第一版是自己掃全圖地形算的，得到 **299** 而實測是 **250**——**兩個數都自洽，放在一起就是假的覆蓋率**；改成由 `cell-sweep -index-json` 輸出、可達性只消費不重算，分母就結構上不可能漂（`cell-sweep` 走的是 `dispatch.Indexes`、跳過索引 0、只算地圖上有的，而且「進不去」的段整段不計——這四條規則自己重寫一遍就會漏掉其中幾條）；★★ 順帶看見的事實：**提爾佛頓（`ECL2/0x01`–`0x03`，48 個索引、占 19%）整段沒有任何實跑路徑經過**——逐格實測驗過它們演得出來，但主線測試從艾森布拉開始，序章不在路線上；⚠ 明確不宣稱：**「沒被踏到」不等於「走不到」**（主線只走它要走的路，支線與可選分歧本來就不會踏到），記錄點是觀測迴圈不是每一步移動 ⇒ 兩層都是**下界**；⇒ 走訪那一列的分母**全部補齊**，剩下的是覆蓋率本身——要提高得**補走那些地方的實跑路線**，那是新的測試工作不是新的盤點；`remake-status` 量得到的列 21 → 22；第 707 輪：**失敗分支也有下界了；量測本身錯了三次才對**（`cell-sweep.md`）——★★ 走訪缺的另一個分母是**失敗分支**（豁免沒過、技能檢定失敗、遭遇表擲到別側）：主掃描**找到第一顆演得出來的種子就回來**，所以另一側**結構上不會被看到**——報表是滿的，而那一半從來沒跑過；改成同一格同一組條件把每顆種子各站一次、數相異的 `Message`；⇒ **240 個有內容的格子裡，11 格換種子會演出不同敘述**；★★★ 這一輪的量測本身錯了三次，每次數字都「看起來完全合理」：(1) 忘了先把事件推回地城就再跑一次生命週期 ⇒ 221/240 記成「跑不動」；(2) 用 `playerText()`（Message 空就退回 Prompt）當重訪指標 ⇒ 74 格「演出別的字」，拆開來 53 格是「請按任意鍵繼續」、12 格是撿寶物提示、5 格是 HUD，**真的只有 3 格**；(3) 數骰子分歧時把 `playerText` 的值當集合第一個元素、後面卻放 `Message`，**每一格都多算一種**（3 種其實是 2 種）——⚠ **集合的鍵只能有一種來源**；★ 修完之後：重訪 只演一次 **184**／每次都演 **44**／有不同敘述 **3**／推不回地城 9（合計 240 ✓），骰子分歧 **11**；顯示端也修了——兩種結果第一句相同時印「（差異在第一句之後）」，照樣印第一句會看起來像判定壞掉；⚠ 明確不宣稱：兩個都是**下界**——重訪量的是「同一格再跑一次」不是「走開再回來」，骰子分歧量的是「換種子敘述會不會變」不是分支數（一格可能三條分支只打到兩條，也可能只有數值不同）；⇒ 走訪那一列還缺的只剩**可達性**（要實跑資料，靜態答不了）；`remake-status` 量得到的列 20 → 21；第 706 輪：**重訪分支有分母了，而第一版的數字灌水了二十倍**（`cell-sweep.md`）——★★ 「全城市／全房間走訪」缺的分母之一是**重訪分支**：逐格實測本來每一格都重新進段（once-only 旗標會互相污染，那個設計是對的），副作用是**第二次踏上同一格從來沒被觀察過**，而原作大量用 `SAVE <旗標>` ＋ `IF <旗標>` 讓事件只演一次；★ 做法：同一次進段裡把事件推回地城再跑一次生命週期（⚠ 少了「推回地城」這一步，`RunDungeonLifecycle` 會正確地拒絕，而那個拒絕被記成「跑不動」——221/240 格，**看起來像 remake 有問題，其實是量法錯了**）；★★★ 第一版用 `playerText()`（`Message` 沒有就退回 `Prompt`）當指標，得到「74 格第二次演出別的字」；拆開來 **53 格是「請按任意鍵繼續」、12 格是撿寶物提示、5 格是地城 HUD，真的有新敘述的只有 3 格**——**分母灌水近二十倍，而每一格都「確實不一樣」**；改成只看 `Message` 之後：只演一次 **184**、每次都演 **44**、第二次有不同敘述 **3**、跑不動 9（合計 240 ✓）；★ 那 3 格逐一看過都是真的：酒館第二次是「酒保有個黑眼圈，一隻手還吊著」、`(12,0)` 第二次檸檬味從「一絲」變「濃烈」、`(7,12)` 龍巫妖之後才看到「法球試煉」的門牌；⚠ 明確不宣稱：量的是「同一格再跑一次」不是「走開再走回來」（**代理指標**），而且只看 `Message` 會把「旁白放在 `Prompt`」的那一類算成沒有新敘述 ⇒ **這一欄是下界**；「只演一次」**不是缺陷**，原作本來就有大量一次性事件；⇒ 走訪那一列還缺的只剩**失敗分支**與**可達性**；`remake-status` 量得到的列 19 → 20；第 705 輪：**輸入那一層也盤點了，抓到「按 I 永遠跳出第一則的圖」**（spec 1189）——★★ 補的是「開場到結局」剩下的那半個缺口：spec 1188 讓真的前端把檢查點**畫**了，但戰役直接呼叫 `state.X()`、玩家按的是鍵盤，**前端 `Update()` 到不了的方法就沒有按鍵組合到得了**；用 `go/ast` 做可達性：戰役 57 個 `State` 進入點，分成「按得出來／觀測點／啟動接線／讀取器差異／硬缺口」五類，別名、會不會改狀態、可達展開**全部自動判**（手打表這個 session 已經咬過三次）；★★★ 硬缺口從 **29 → 28 → 7 → 2 → 1**，每一次都是**我自己的掃描面比實際窄**：只收 `CallExpr` 的 Fun（漏掉 `a.combatAction(a.state.CombatAct)` 這種把方法當值傳的）、閉包停在第一個 `state.X`（漏掉 `Select()`→`selectCamp()`→`AdvanceGameTimeHours()`）、把讀取器與啟動接線都算成缺口；⚠ **中間每一個數字看起來都像真的結論**——**假缺口比假零更難察覺，因為它會給你一份待辦清單**，而假零會被別的證據撞到；★★★ 剩的那 1 個 `NextJournalPage` 不是漏接，是前端**刻意自己管翻頁**——而那正是缺陷的成因：翻頁走 `a.journalDisplayPage`、看圖走 `state.JournalMessageID()`（讀 `State.JournalPage`，永遠 0）⇒ **玩家翻到第 5 則按 `I`，跳出來的是第 1 則的圖**，連「有沒有圖」的鍵判斷也是拿第 1 則算的；★ 兩個頁碼**不是同一個粒度**（一則手札可能佔好幾個顯示頁），不能直接同步兩個計數器；修法：分頁時一併回傳「每個顯示頁來自第幾則」，再用新的 `JournalMessageIDAt(index)` 查，`journalDisplayPages` 保留成薄包裝所以既有測試不動；⚠ 明確不宣稱：靜態可達性**方向不對稱**——證明得了「前端沒有那條路」，證明不了「有路所以玩得到」；「會不會改狀態」是近似且**寧可少報缺口**；`remake-status` 量得到的列 18 → 19；第 704 輪：**把戰役檢查點交給真的前端畫一張，第一次跑就抓到兩個缺陷**（spec 1188）——★★★ `remake-status` 的「開場到結局的同一 session」寫著「端到端測試跑得到結局，但那是**測試路徑**」，那句話是準確的：`TestRealNewGameRunsToTheEnding` 從角色建立一路打到提朗瑟克斯、24 段全綠，但它驅動的是 `*State`，**畫面那一層一次都沒被跑到**；★★ 做法：戰役本來就會存段界快照（只是寫到 `t.TempDir()`），加一個環境變數導出來，再讓 `cmd/azure-bonds-game -party-load -screenshot` 逐一畫；23 個檢查點**全部畫得出來、沒有一張空白**；⚠ 第一次跑就踩到：字型在 repo 外，沒掛進容器的話**每個字都是豆腐框而畫面其他部分完全正常**；★★★ 抓到兩個 `LoadPartyFile` 的缺陷，都是「存檔保存了狀態、但沒保存畫面上的字，讀檔路徑用建檔當下的值填掉」：(1) 最後**無條件**設世界地圖 hub ⇒ 存在事件畫面上的檔讀回來，人在密斯卓諾墓園、畫面卻是「隊伍已建立．準備開始冒險．」＋「進入城市／繼續旅程／紮營」（F5 快存**沒有限制模式**，玩家走得到）；(2) `Location` 有還原、**`LocationName` 沒有** ⇒ 標題永遠是建檔當下的「荒野」；修法只寫 `LocationName` 不碰 `Location`，所以原註解警告的「用 `CurrentCity` 覆寫會蓋掉 `Location`」那個 hazard 在結構上不成立；兩條回歸測試都**先反向驗過會紅**；★ 這兩格 `*State` 層的測試**結構上看不到**——讀完檔就做下一個動作，欄位在被觀察到之前就被覆蓋掉了；⚠ 同一個函式上面幾行就有 `eventReturnMode` 的註解在講同一類問題，當時只補了「按下一步會不會卡住」——**同一類缺口補了一半會看起來像補完了**；★ 一個**不是**缺陷的觀察也記下來了：三對檢查點畫出來位元組相同，其中兩對存檔的座標／朝向／`InDungeon` 都不同，看起來像「畫面沒跟著位置變」——實際上世界地圖那張只由 `Area.CurrentCity` 決定，而那兩對的 `CurrentCity` 相同⇒ 本來就該一樣；報表因此把 `CurrentCity` 一起印出來；⚠ 明確不宣稱：只證明 `Draw` 畫得出非空白畫面，**輸入那一層（鍵盤 → `Update`）仍然沒有自動化證明**，像素分佈也驗不到字型有沒有載入或畫的對不對；`remake-status` 量得到的列 17 → 18；第 703 輪：**戰鬥音樂找到了，而 DOS 根本沒有音樂**（spec 1186／1187 增補）——★★★ `pc98-music-triggers.md` 一直寫著「兩首（戰鬥、地城二）沒有立即數換曲點選到，**不能讀成用不到**」，那句警告是對的但只能停在那裡——因為掃描器只認 `mov byte [MUSICNO], imm` **一種形狀**；真正的形狀是 `INITCOMBAT`（COMPREP ＝ overlay-10）**把曲號直接推給 `MSCPLAY`**、完全不碰 `MUSICNO`：`cmp byte [LOADMONNUM], 47h` → 相等放曲目 11（地城二）、否則放曲目 7（戰鬥）⇒ **戰鬥開始換哪一首取決於載入了哪一組怪物**；補上第二種形狀之後宣告的 12 首**全部有落點**（未被選到 2 → **0**）；★★ 同一輪還修掉兩個同源的量測洞：`soundCallSites` 仍走 far-call 對照表（`MSCPLAY` 少 2 處、`SOUNDFX` 少 18 處，少掉的正是那兩處戰鬥音樂），以及**第三張手打對照表**——`soundRoutines` 只有四格，於是 `015Eh` 被印成「符號表沒有這一格」，而它就在符號表裡叫 **`MSCSTOP`**；⚠ 這是同一個 session 裡**第三次**踩到「手打對照表漏格 → 印出一句看起來很有資訊量的『查無』」；★★ 報表裡那句寫死的交叉印證（「`MSCPLAY` 正好落在那五個改寫 `MUSICNO` 的 overlay 上」）在資料變寬之後就成了假的，已改成**算出來**（5 個模組重合）——**寫死的句子不會跟著資料變**；★★★ 音樂那一半的 DOS 答案是「**沒有那一半**」：同一套指紋認法拿 `MSCPLAY` 去比 DOS，最接近的只對上 7 處裡的 2 處而且一整排並列（對照 `SOUNDFX` 的 1 比 29 ＝ 找不到，不是找到比較像的）；DOS 音效段 `0713h` 只有兩個入口、image 裡沒有任何音樂資料檔、game pack 早就標著 `source_platform: pc-9801`——三件事互相印證 ⇒ **DOS 版沒有背景音樂**，那個待辦不是「還沒做」；第 702 輪：**DOS 側的音效格子認出來了——沒有符號，靠形狀**（spec 1187）——★★★ 音效語意全部來自 PC-98 的 Borland 符號，而**行為 oracle 是 DOS**；「PC-98 的東西不能直接套 DOS」這句警告一直寫在各份規格裡，但**從來沒有人做過那個「另外認一次」的動作**；★★ 認法是兩個**互相獨立**的訊號：(1) 呼叫點分佈當指紋——PC-98 `SOUNDFX` 在 overlay 側 42 處跨 8 個模組，拿去比 DOS 全部 **432** 個相異 far-call 目標，第一名 `0713:0020` 差 **1**、第二名差 **29**（**差距本身就是證據**，咬得近就是巧合不是指紋）；(2) 表的位置——描述子都是 `基底 ＋ 選擇子×2`，⚠ 但**整張表平移一格之後每一列還是說得通**（位址連續、名字一一對應），所以每一個觀察到的描述子都試過一次當錨點，最佳 `25AAh` 對上 **14** 格、第二名只對上 **2** 格；⇒ 結果：DOS 基底 `25AAh`、PC-98 `4838h`（差 `228Eh`），**14 格的 overlay 呼叫點分佈逐模組完全相同**——兩支各自編譯、沒有共用符號的執行檔，每一格落在哪些模組各幾次全部一致；★ 唯一的差是 `CRASHFX`：PC-98 在 `INTERPET` 有一處、**DOS 整支 0 處**（正對照做過：同一種掃法在 DOS 找得到 `PADFX` 7 處、`SOUNDOFF` 4 處，所以那個 0 是真的）；remake 在 `PROGRAM 3`（全隊陣亡）發 `SoundCrash` 與 PC-98 對得上，⚠ DOS 版沒有這一聲、DOS 資源表裡 `Crash` 也沒有 WAV ⇒ 目前靜音，但那是**巧合不是設計**；★ 兩版的常駐都**只**放 `SOUNDHALT`／`SOUNDOFF`／`SOUNDON`，一個玩法音效都沒有——這是 spec 1186「`SoundStop` 是引擎內務」的**第二次獨立印證**；⚠ 明確不宣稱：只證明**呼叫介面**對得上，沒有證明波形資料或常式內部相同（DOS 走 PC 喇叭、PC-98 走軟體發聲），逐處「哪一支常式在放」在 DOS 只到模組層級；`remake-status` 量得到的列 16 → 17；第 701 輪：**投射武器的第二聲接上了，順手撞到一個與音效無關的缺陷**（spec 1186 增補）——★★ `SHOWARROW` 的 `+56h` 就是 **`CHARITEMREC.ITEMPTR`**（物品類別，remake 的 `ItemRecord.Type`），兩份獨立證據：PC-98 記錄版面表在 `CHARITEMREC+056h` 列著 `ITEMPTR`（檔案版 `CHARITEMFILREC` 是 `+02Eh`，正是 remake 讀 `Type` 的位移），以及 `USINGMISSLEWEAPON` 的雙重間接讀的是同一欄；★★★ **remake 不必知道「架著的是哪一件彈藥」**——分歧鏈點名的 14 個類別裡，箭（`49h`）與弩矢（`1Ch`）**落在同一個 ARROWFX 分支**，所以「要另外彈藥」的武器一律得到箭聲；投石索類／油瓶（`2Fh`／`65h`／`56h`）哨音、擲斧／棍／鎚（`02h`／`07h`／`14h`）揮擊、飛鏢／標槍／矛（`09h`／`15h`／`1Fh`）箭、其餘走 `2C48h` 的預設分支（弓與弩本身在這裡，**預設分支不是空的**）；14 個類別裡 12 個遊戲裡真的有物品、2 個一件都沒有 ⇒ 那兩條分支走不到；★ remake 這一側的掃描器也有同一種假零：只數 `requestSound(SoundXxx)` 會把 `SoundWhistle` 印成 0，因為它是 `missileImpactSound` 回傳的——**同一輪在原版與 remake 兩側各踩到一次「掃描面比實際窄」**；⇒ 「原版有而 remake 從沒發過」2 → **1**，剩的 `SoundStop` 是引擎內務、不必補；★★★ 順手撞到**與音效無關**的缺陷：`AmmunitionCount` 挑的是「類別表槽 11 或 12」的裝備，而這一款的**槽 11／12 是卷軸**，箭與弩矢在**槽 10**（還混著藥水、飾品）——來源是 spec 1120 **自己的自相矛盾**（§161 說槽 11／12、§199 說「彈藥（slot 10）」，兩句話並存幾百輪沒人發現）；成因是把「偏移量算出來的陣列第 11 格」當成「分類欄位是 11」；後果不會噴錯也不會讓測試變紅（`capByAmmunition` 把 0 當不設限），只是**箭的數量從此不再限制射擊次數**，反過來架著卷軸的角色會被卷軸張數限制；⚠ **本輪不修**（正確判斷式要先解出 `+17Dh`／`+181h` 由什麼決定），但 spec 1120 正文已改寫成正確說法，並加了一條會失敗的回歸測試擋住「再從槽 11／12 出發」；第 700 輪：**音效什麼時候響——54 處 `SOUNDFX` 的逐處語意，以及兩個把答案藏起來的洞**（spec 1186）——★★★ 修掉的第一個洞是**手打的描述子對照表漏了兩格**：`4840h`／`4842h` 就是 `MISSFX` 與 `SPELLHITFX`（描述子 ＝ `483Ah` ＋ 選擇子×2），漏掉不報錯、報表只是印成「符號表沒有」，下游的音效對照因此**少了兩列**，正好是 remake 用得最兇的 `SoundSpellHit`（29 處）與 `SoundMiss`（2 處）——那兩個事件從來沒被比對過；已改成從 `pc98sfx.Selectors()` 推，並加「每一個宣告過的 `SoundEvent` 都必須有一列，否則讓它紅」的護欄；★★★ 第二個洞是 **far-call 對照表是下界**：表列 36 處、位元組直掃 **54** 處（表是直掃的真子集，逐處驗過），而差的 18 處裡有一處**改結論**——`LIGHTNINGFX` 在表裡是 0，於是報表寫成「remake 有、原版沒有」，實際上它在 `CASTSPELL` 裡（`cmp al,2Fh`→FIREBALLFX、`cmp al,33h`→LIGHTNINGFX、其餘 CASTFX；2Fh＝47＝Fireball、33h＝51＝Lightning Bolt，與早先由另一條路解出的法術編號一致）；正對照：同一個位元組樣式在常駐掃得到 12 處 `SOUNDFX`＋3 處 `INITSOUND`＋1 處 `BGMPLAY`，所以某個音效的 0 才算數；★★ **`MISSFX` 不是近戰揮空的聲音**——它在整支執行檔裡唯一的呼叫點在 `TWINKLE`，與 `SPELLHITFX` 共用同一個 `if` ⇒ 是**法術**沒中；remake 原本在近戰揮空放 `SoundMiss`，等於在原作從不出聲的時機放法術音效，測試全綠也玩得動，只是聲音一直錯；★ 已修：近戰 travel 階段放 `SoundSwish`（原作 `SWISHFX` 在 `DESCRIBEWEAPONATTACK` 之前放、不看中不中）、命中補 `SoundHit`、**揮空不出聲**；`TWINKLE` 改成中了 `SoundSpellHit`／沒中 `SoundMiss`；新測試**先反向驗過會紅**才留下；⇒ 「原版有而 remake 從沒發過」3 種 → **2 種**（`SoundWhistle` 缺 `SHOWARROW` 的武器類別碼對照，`SoundStop` 是引擎內務——原作看 `SOUNDUP` 決定要不要停手上那一聲，remake 的逐事件播放器不需要它，理由寫進 spec 1186 免得下一輪又當成待辦）；「remake 有而原版沒有」3 → **0**；`remake-status` 量得到的列 15 → 16；第 659 輪：**`2Dh CALL` 七個選擇子的全量普查，以及一段過期的正文**（spec 1150 增補）——★ corpus 用四個（`2E10h` 125／`B200h` 19／`C01Eh` 13／`6803h` 11），`C018h`／`8000h`／`8001h` 三個都是 **0**，而且**沒有第八個**（認不得的選擇子一個也沒有）；⚠ 那三個零之所以算數，是因為測試**先做正對照**——四個有在用的必須掃到 125／19／13／11 才往下走；本輪已經被假零咬過兩次（`7EE2h` 的 producer、自己第一版的位元組掃描器），所以新測試一律這樣寫；⇒ **未接的三個目標都走不到**，`2Dh` 的 `partial` 只剩 `ViewMirror.Block`／`Written` 兩個 remake 側補丁；★★ spec 1150 正文那段「remake 沒有那五個髒旗標、改用回頭掃 `SaveWrites` 的啟發式」是**過期的**——第 642 輪（spec 1172）就把五個髒旗標建出來、啟發式整支拿掉了，正文已改寫；順帶關掉「不宣稱 `GODUEL` 做什麼」：它是決鬥，複製目前角色成叫 `ROLF`／`ロルフ` 的 NPC 掛進隊伍鏈並設 `8B56h`；第 658 輪：**`24h COMBAT` 轉 `done`，`partial` 4 → 3、686 → 487 條**（spec 1182 增補）——★★★ 卡住的是第一支的第二個條件 `DS:8B56h`，spec 1149 標著「不宣稱」——**而答案早就在自己的規格裡**：spec 917 判讀戰後結算措辭時就解出它是**決鬥旗標**（`8B56h <> 0` 印 `You have won the duel.`／`The duelist receives `），spec 626 記過唯一的 producer 是 `GODUEL`（分身叫 `ROLF`／`ロルフ`），spec 1150 記過 `GODUEL` 只有 `2Dh CALL` 的 `8000h`／`8001h` 到得了、而且**corpus 兩個都沒用到**；三份規格各有一半、從來沒被連起來 ⇒ **要說「解不出來」之前先 grep 自己的 docs**；★★ 連起來的結論：`8B56h` 那一支在 CoAB **走不到**，所以 remake 用「怪物鏈非空」代替 `8B69h` 已經覆蓋整個可達條件，四支各有實機路徑；★ 這一輪被假零咬過兩次之後，新測試一律**先做正對照**——`TestDuelCallSelectorsAreUnusedWhileTheOthersAreNot` 要先掃到那四個真的有在用的選擇子（125／19／13／11）才讓 `8000h`／`8001h` 的零算數；⚠ 決鬥本身仍是 `2Dh CALL` 的未接目標之一，那屬於 `2Dh` 的帳；第 657 輪：**「沒有 producer」是假零——商店與神殿的 `24h` 服務其實都走得到**（spec 1182 增補）——★★★ spec 1095 寫過「這兩格在 1,355 條 ECL 指令裡沒有任何一條寫過（已全掃確認）」，而 `cmd/ecl-cell-refs` 掃全 corpus 是 `7EE2h`（神殿）**4 處**、`7F6Ch`（商店）**9 處**，慣用法一模一樣：`CLEARMONSTERS`（保證「有怪就打」那一支不成立）＋ `SAVE 01 <旗標>` ＋ `COMBAT`；★★ 兩支服務**本來就接得起來**——VM 讀 `memory`、腳本用 `09h SAVE` 寫進同一張 map，已用原版資料（`ECL1/0x50:0822h` 的酒館段、`ECL4/0x25:009Ah`）實跑驗過，連「讀到就清零」也一起釘；★ 釘那個假零的 `TestCampRequestFlagHasNoProducer` 移除，換成**正對照** `TestServiceRequestFlagsHaveScriptProducers`：掃出 0 就直接說「那是假零，先確認掃描而不是下結論」，數字與 4／9 對不上也紅（突變驗過）；⚠ 第一版的掃描器自己寫位元組比對，結果剛好長成「一處都沒有」——變長指令要走控制流，這正是假零最典型的來源；`24h` 仍是 `partial`，原因收斂成 `8B69h`／`8B56h` 是引擎側旗標、remake 用怪物鏈非空代替；第 656 輪：**`24h` 的第四支接上了——那 46 處「沒擺過怪的戰鬥」其實是戰利品分配**（spec 1182 增補）——★★★ 原作的 `24h` **沒有「零隻怪的戰鬥」這種東西**：有怪才走 `sub_1956`，沒怪又沒商店沒神殿就走 `overlay-05` 的 `DOPOSTCOMBAT`。remake 先前一律發 `CombatRequested`，現在零隻怪改發 `PostCombatRequested`；★★ 改完之後 **10 個既有測試**從「零隻怪的戰鬥」變成「戰利品分配」，而每一個都是真的撿東西現場（外圍廢墟倉庫、逃亡者的暗格、賭場、內城臥房、埋葬谷的斯瑞克林營地、火刀辦公室、洞穴裡死去精靈的皮袋）——腳本的慣用法就是先 `27h TREASURE` 堆好、再 `24h` 開分配畫面，corpus 直接印證了模型；★ 地城裡「腳本把遭遇編成減到零再打 `24h`」那條續跑路徑跟著搬到新分支，原作那一支等於立刻結束並回到腳本；⚠ `24h` 仍是 `partial`：`8B69h`／`8B56h`／`7EE2h` 三個旗標在 remake 都沒有 producer；第 655 輪：**`overlay-04` 是神殿不是營地——兩份規格把 remake 對的名字改錯了**（spec 1182）——★★★ spec 1030 把 `dos overlay-04:00DD6h` 讀成「營地主選單」，spec 1095／1149 沿用，還因此說 remake 把 `7EE2h` 標成 `TempleRequested` 是「位址對、語意不符」：**反了**；兩份一手資料都說神殿——PC-98 的 Borland 符號只有 `LOADTEMPLE`／`GOTEMPLE`（營地在 `overlay-15`），而 DOS `overlay-04` 自己的字串是 `how can we help you?`／`Heal View Pool Appraise Exit`／`Raise Dead` 與「a priest says…」；spec 1030 讀到的 `H`/`V`/`T`/`P`/`S`/`A` 選單也是神殿的，不是營地的 Save／Rest／Magic／Alter；★★ **全部 36 個 overlay 的單元名一次認完**（`cmd/borland-symbols -overlay-names`）：Turbo Pascal 每個 overlay 的第一個 entry 一律是 `LOADxxx` ⇒ `xxx` 就是單元名。DOS 版沒有除錯符號，`overlay-NN` 一直只能靠內容推而**推錯不會有徵兆**；編號一致有兩處獨立印證（spec 1156 的 POSTCOM ＝ `overlay-05`、spec 1153 的 THREED ＝ `overlay-30`）；★★★ **`24h COMBAT` 是四選一不是三選一**：第四支 `sub_184B` 先前完全沒讀，讀完是「`bank1^[5C4h] = 1` ⇒ 神殿，否則 ⇒ **戰後處理**（`overlay-05` DOPOSTCOMBAT）」——這正是那 46 處「沒擺過怪的 `24h`」的去向：腳本先用 `27h TREASURE` 堆好戰利品，再用 `24h` 開分配畫面；★ 收尾重繪的閘門 `bank0^[1CCh]` 就是上一輪從 `21h LOAD FILES` 認出的 `4BE6h`，兩支 handler 互相印證；⚠ remake 只改名與註解（行為本來就發 `TempleRequested`），第四支仍走別的機制，`24h` 保持 `partial`；第 654 輪：**`21h LOAD FILES` 轉 `done`，`partial` 5 → 4、708 → 686 條**（spec 1181）——★★★ 它不只是「記下請求、上層去換檔」：載 3D 地圖那一路 handler **自己寫兩格 ECL 記憶體**（`bank0^[18Ah] := o[1]` ＝ 格 `4BC5h`、`bank1^[592h] := 0` ＝ 格 `7EC9h`），條件是 `o[1] <> 0FFh`、`<> 7Fh` 且 `bank0^[1CCh] <> 0`（格 `4BE6h`）；★★★ **`7EC9h` 腳本讀得到**——全 corpus 34 處存取，腳本一路寫 `FFh`，而唯一的讀取端 `ECL2/0x03:00F6h` 是 `COMPARE 7EC9 FF / IF = / SAVE 00 7ED5`，清掉它的**只有這個 handler** ⇒ remake 不寫的話，腳本設過 `FFh` 之後就永遠停在那個分支上，這是控制流分歧不是表現層問題；★ `4BE6h` 的 22 處寫入全部落在 ECL1 的三個世界地圖 block 且在 0／1 之間切，配上 handler 兩條互斥條件（非零載 3D 地圖、為零載大圖），形狀是「現在是不是第一人稱畫面」；兩格都走 `recordStore` 會進 `SaveWrites`——只改 map 不記錄的話存檔重載會不一致；⚠ 突變驗過（拿掉清 `7EC9h` 那兩行立刻紅）；第 653 輪：**`37h LOAD PIECES` 的三支分派全部照抄，`partial` 6 → 5、731 → 708 條**（spec 1153 增補）——★★★ **「沒列出來的槽這一次完全不動」是重點**：先前的實作一律三格寫滿，而 `o[1] = 7Fh` 那一支只呼叫 `LOADWALLSET(1, 0)`（片號是 **0** 不是 `7Fh`），槽 2／3 連哨兵都不寫 ⇒ 舊寫法會把既有的牆面組清掉；★★ 兩槽分支（`bank0^[1CEh]` 與 `[1D0h]` 都非零 ⇒ 只載槽 1／3）那一支**沒有 else**，所以 `0FFh` 是整個跳過而不是寫哨兵——三支各自對哨兵的處置都不一樣；兩個閘門的位址用 spec 1098／1155 的換算 `bank0 + (位址 − 4B00h) × 2` 對回 ECL 格 `4BE7h`／`4BE8h`；★ **分支放在 VM 這一層**（`ecl.WallSetAssignments`）而不是讓上層猜——handler 自己就是這樣分的，上層只接收「這一次要對哪些槽做什麼」；★★★ **而且兩槽分支不是死碼**：`cmd/ecl-cell-refs` 掃出兩個閘門各有 12／10 處 producer，其中**四個 block**（`ECL5/0x33`／`0x35`、`ECL6/0x40`／`0x45`）在自己的 `LOAD PIECES` 之前就把兩格都設成非零，寫法一模一樣（`SAVE 01 4BE7` ＋ `SAVE 01 4BE8` ＋ `LOAD FILES` ＋ `LOAD PIECES`）⇒ 這四處的**第二個運算元是死資料**，而 remake 先前把它載進槽 2 並寫進存檔（密林德藍諾與最終區的牆面組）；⚠ 這推翻了 spec 1153 自己標明「是推論不是觀測」的那一句（原本推論兩槽分支若是常態、槽 2 的片號會全是死資料），正文已改寫；`7Fh` 那一支 corpus 仍一次都沒出現，照 handler 寫；第 652 輪：**兩個 `partial` opcode 轉 `done`，8 → 6、818 → 731 條**——★★ `27h TREASURE`：原作那兩側是**兩套資料結構**，錢幣池是 `LongInt(DS:[6F70h+4i]) := 運算元`（**覆寫**），物品是前插進 `DS:6F8Ch` 的鏈（**累積**）；remake 先前把錢幣也當成累加 ⇒ 兩筆之間沒有 `1Ch` 的話金額會多算。測試兩側各釘一半、突變驗過（改回 `+=` 立刻紅：35 GP vs 10 GP）；★★ `2Eh DAMAGE` 的第三條路「單體但隨機挑一名」也接上了：擲一顆 `1..隊伍人數` 挑人，⚠ 這一路的豁免種類**不減一**、要不要擲由 `旗標` bit 5 決定——減一是**只有**「目前角色」那一路才有的事，兩種讀法在 corpus 上結果相同，只有讀 handler 才分得出來；★ corpus 24 處走不到這一路（單體封包的 `SaveFlags` bit 7 都是設定的），所以它沒有實機路徑背書，是照 handler 寫的，形狀與全隊那一路共用同一個結算器；兩條回歸都突變驗過；第 651 輪：**隊伍側的每回合攻擊次數與彈藥上限**（spec 1180 增補）——★★ 寫入端全在 `overlay-25`，門檻是**戰士 7 級／聖武士 7 級／遊俠 8 級** → 半次值 3；⚠ 聖武士**與 AD&D 規則書不同**（規則書是 8 級），照規則書填會錯一級；同一組門檻在現職業迴圈、前職業迴圈、直接查 `PREVIOUSLEVEL[2..4]` 三處完全一致；★★★ **整包 overlay 寫進 `+11Ch` 的立即數只有 `2` 與 `3`，沒有任何一處寫 `4`** ⇒ 這一款的隊員**永遠到不了一回合兩次**，AD&D 13 級那段沒有實作——已寫成測試擋住「照規則書補一個 4」的好意；★ 遠程的彈藥上限也照抄了，含那個看起來像疏漏的組合：`m := 1` 之後第二個條件是 `彈藥數 > 0`，所以**數量 0 不會把次數壓成 1**；彈藥數取的是**架著的那一件**的原始 `+39h`（類別表槽 11／12），不是全身合計；⚠ `PREVIOUSLEVEL`（`+111h`）remake 還沒有模型，雙職角色靠前職業拿到的一次半算不出來；第 650 輪：**68 種怪物全部每回合只打一下，因為讀錯了一個欄位**（spec 1180）——★★★ remake 把 `+0A1h` 當每回合攻擊次數，而那一格落在 `SPELLSKNOWN`（`+079h`，100 bytes）**裡面**、68 種怪物**全部是 0**、整包 overlay 反組譯**沒有任何一處讀它**（同一套掃描對 `+0DEh`／`+11Ch`／`+19Ch`／`+0DDh`／`+151h` 分別找到 10／11／10／9／14 處，所以不是掃描方法的問題——這是「查詢回空」的正對照）；★★★ 真正的欄位是 `+11Ch` ＝ **`BASEATTBLOWS[0..1]`**，單位是**半次**：分布 0×2／2×51／4×13／8×2 ⇒ **15 種怪物**（`TROLL`／`RAKSHASA`／`DRACOLICH`／`BLACK DRAGON`／`THRI-KREEN`）先前每回合少打一到三下；★★ 換算只有一個函式 `ADJUSTBLOWS`（DOS `overlay-13:0F12h`，PC-98 同名、**位元組完全相同**）：`(半次值 + (ROUND and 1)) div 2` —— 加的是**回合數最低位**，奇數半次值因此在回合之間交替（`3` → 2,1,2,1，平均 1.5 ＝ AD&D 的每兩回合三次攻擊），**寫成 `/2` 會每回合都少半次而且沒有徵兆**；★ 名字全是原作自己的 Borland 符號，`DS:758Dh` ＝ **`ROUND`**、`758Eh` ＝ `LASTROUND`、`75E6h` ＝ `SHOWDUDEMOVE`——用 spec 1019 早就量出的「PC-98 資料段 ＝ DOS ＋ `3292h`」對位，`758Dh+3292h = A81Fh` 正是符號表裡的 `ROUND`；★ 順帶關掉四份規格的待定項：spec 808 的「`0F12h` 怎麼換算」與「`+11Ch`／`+19Ch` 的量綱」、spec 771 的「沒讀消費端」、spec 1010 把 `+11Ch` 標成「陣營」（實際是 `BASEATTBLOWS[0]`，陣營是 `+11Bh`）、spec 1164 的 `+11Ch` 兩種讀法待對（`= 0 就用槽 2` 是它的一個**用法**不是另一種讀法，`GIANT SPIDER`／`PHASE SPIDER` 的 `{0, 2}` 正是那一條）；加速／緩速改成乘除**半次值再換算**；⚠ 彈藥數上限與隊伍側的 `BASEATTBLOWS` 都還沒接；第 649 輪：**戰鬥畫面上有 45 種怪物是英文，而且沒有任何徵兆**（spec 1179）——★★★ `LocalizeCombatantName` 查不到宣告時**原樣回傳**、`found` 回 false 而呼叫端只在 true 時覆寫，所以漏一種不會有錯誤、不會有 log、測試不會紅，**只有玩家看得到**；原版六章 `MON*CHA` 共 **68 種**相異名字，pack 先前只宣告 23 種；★★ 三條閘門各擋一個方向：原版有而 pack 沒有、宣告的 `source` 拼錯（那條規則永遠不生效）、兩種生物撞同一個譯名；第一條走 pack 的**執行時查詢**不是比對兩份 JSON 的鍵——宣告與翻譯各自對得起來但查詢路徑斷掉的話，比 JSON 一樣會過；突變驗過（`THRI-KREEN` → `THRI KREEN` 兩條同時紅、指出的問題不同）；★ 譯名不是自己決定的：`coab-glossary.md` 明文規定戰鬥怪物名的唯一來源是 `combatant_name_rules`、稽核時併入，於是 `glossary-audit` 一口氣抓到 3 條與手札既有譯名衝突（移位獸／奧提尤格／斯瑞克林，全部改採表裡的寫法）；★★ 另外三條 `duplicate_rendering` **不是重複是原文別名**——`MON*CHA` 的名字欄有長度上限，`AKABAR BEL AKAS`／`FIRE KNIFE`／`WORG` 是 `Akabar Bel Akash`／`Fire Knives`／`warg` 的截短或單複數寫法；譯名表加了「原文另作 `X`」的別名折疊，折疊後不但不再誤報，還**多一道一致性檢查**；⚠ 有 5 個測試在斷言**顯示名**（`fighter.Name == "FIRE KNIFE"`）——顯示名會被翻譯，身分要看 `SourceName`；順帶對齊 `item_name_E8`（火巨靈 → 伊弗利特，兩張表來源不同所以上一輪沒抓到）；第 648 輪：**物品名稱整組是錯的模型，而且看不出來**（spec 1178）——★★★ 名稱**完全不含物品類別**：三個 byte（`+2Fh`／`+30h`／`+31h`）各自去查 `DS:1040h + N×15h` 那張 255 筆的 Pascal 字串表，串起來就是全名；remake 先前拿 `item_type_XX` 當基底再貼成分，靠一條「和基底相同就不加」的守衛才沒穿幫，遇到類別名與中心語不同的（`Miscellaneous` 的伊奧恩石）就露餡；★★ **語系檔那 54 筆有一半編號是錯的**——第 260 輪照一份 reference 實作的陣列填，`24h`..`3Bh` 剛好對，`9Fh` 之後整段偏 3（檔裡 `item_name_9F` 寫 `+1`，原作 `+1` 是 `A2h`），`3Ch`..`3Eh` 是拿物品類別的編號填的 ⇒ **玩家看到的加值是錯的數字**；★ 基底怎麼證的：指令在 `overlay-24:063Bh`（`mul 15h` ＋ `add 1040h`），資料側再把 253 件物品整批組回去比字串——**208 件逐字元相同，基底位移一格或兩格都是 0 件**；★★ **`+32h`（Plus）與 `+36h`（Cursed）不進名字**：加值走名稱編號 `A2h`..`A6h` 那幾個**可以被藏起來**的成分，原版 72 件帶 Plus 的物品有 54 件從頭到尾不顯示加值，而 `+35h` 不是布林是三個 bit（出廠 `6h` ＝ 未鑑定）；⚠ 紀錄的 `+00h..+29h` 是**未鑑定名的快取**（`Deck`／`Beaker`），組名時會被覆寫，不是權威資料；`item_name_XX` 由 54 筆補成 252 筆，126 個用到的編號**缺譯 0**、115 種相異名稱逐件列在 `docs/audit/item-names.md`；翻譯層加一條「三成分而中間是連接詞就前後對調」讓 `Wand of Fireballs` 讀成「火球之魔杖」；第 647 輪：**逐格實測的「沒演出來」現在自動分得出成因**（spec 1177）——★★ 那先前是一個混在一起的桶子：**remake 沒接**與**缺前置劇情**處置相反，而分辨的成本是一格一格反組譯守衛；改法是把守衛的 `COMPARE <格子> <值> / IF <op> / EXIT` 拆成結構，把格子設成**避開 `EXIT`** 的值再站一次；★★ **不是「設成比對的值」就好**——`COMPARE 4C01 01 / IF >= / EXIT` 設成 1 反而保證離開，第一版忽略運算子只多救回 3 格，補上之後是 5 格；全遊戲 250 個索引：**中文 235 → 240、沒演出來 15 → 10、落回原文仍是 0**；★ 剩下那 10 格逐格看過**都是盤點的限制不是缺口**，四種形狀（守衛比移動前快照、旗標被該段前導覆蓋、擷取到的守衛跨過真正的處理常式、處理常式本來就不講話）寫進報表抬頭，下一個人不必重推；第 646 輪：**SAVGAM 的第四塊（ECL 位元組碼）終於寫得回去**（spec 1176）——★★ 匯出原版格式時前三塊都從 session 重編，第四塊卻只是把匯入來源那一份原封抄過去：**新開的遊戲沒有匯入來源，於是匯出的存檔那一塊整塊是 0**；匯入之後換過段再存檔，寫出去的也是匯入當時那一段的碼；★ 這一塊**不能沿用前三塊的編碼**——前三塊刻意跳過值為 0 的格子（原作分不出「寫過 0」與「沒寫過」），而位元組碼的 `00h` 是真的指令（`EXIT`），跳過會把程式打洞，所以第四塊有自己一對 codec、**0 照收**；★★ 順帶量出一件事：`cmd/ecl-cell-refs -range 8000-9DFF` 掃出 **3,012 處而 opcode 只有三種**（`GOTO`／`GOSUB` 的跳躍目標與 `GETTABLE` 的表），**沒有任何一處把值寫進區 3** ⇒ 「自我修改的碼會遺失」在這一款不成立——⚠ 但那是**這一款**的量測不是引擎的性質（`GETTABLE` 的表就住在位元組碼裡，表示引擎本來就當它是可定址記憶體），所以照通用做法存回去，不靠「反正沒人寫」這個前提；第 645 輪：**換裝重投影的漏欄位改成擋得住整類**，並把大／小切換的邊界讀完（spec 1175 增補）——★★ `TestReplaceFighterEquipmentCarriesEveryEquipmentDerivedField` 拿同一個角色**帶武器與不帶武器各投影一次**，有差的欄位就是裝備衍生的：名單由行為算出來，新增欄位不必記得更新任何清單；**突變驗過**——拿掉 `ReplaceFighterEquipment` 裡任何一行搬運都會紅；★ 整個戰鬥 overlay **只有一個**大／小切換點（另外兩處讀 `+151h` 的是背刺與射程），所以切換與近戰／遠程無關，第 644 輪那句「遠程沒讀」的保留可以收掉；⚠ 原作的 `+1A2h` 是**調整不是指派**（`− 小加值 ＋ 大加值`）⇒ 連續兩次大型目標攻擊之間沒有重算的話加值會漂；沒有找到復原的地方，也沒有證據說重算每次攻擊都跑，兩種可能靜態讀碼分不出來——remake 兩組三連都存著、只讀不寫，不會漂；第 644 輪：**大型目標的傷害切換接上了，怪物側的武器投影也補了**（spec 1175）——★★ `overlay-13:15EFh` 在**攻擊結算當下**切：只有攻擊者有槽 0 武器才切（天生攻擊不分大小）、切的是類別表 `+02h`..`+04h`、**加值是換掉不是相加**；★★★ 判準是 `+0DEh > 80h` **或** `and 7 > 1`，而六章 43 種怪物裡 `81h` 有四隻（`BEHOLDER`／`BUGBEAR`／`GIANT SPIDER`／`PHASE SPIDER`）——`81h and 7 = 1`，**只看低 3 位會把牠們判成小型**；bit 7 是獨立的「傷害算大型」旗標、低 3 位是佔格大小，remake 先前只留 `data[0xDE] & 7`，那個位元整個消失；★★ **怪物側的武器投影接上了**（`monster.ProjectMonsterWeapon`）：開戰掛完 `MON*ITM` 就照spec 1174 投影槽 0 的裝備中武器——那是 spec 1120 留了很久的「規則那一側還沒接」；★ `ReplaceFighterEquipment` 也補上新欄位，少了它換裝之後打大型目標用的還是上一把武器的骰；第 643 輪：**兩個掛了很久的未定項一起關掉**（spec 1173／1174）——★★★ **換武器之後傷害怎麼算**（spec 1120 留的問題）：派生值重算`overlay-24:0C28h` 先把記錄的基準骰整組抄進現值，再叫 `0025h` 做武器投影——`武器 := 角色^[151h]`（**槽 0**），為 NIL 就整支返回，否則把類別表的小型傷害三連（`+09h`／`+0Ah`／`+0Bh`）**無條件**寫進現值；⇒ 量到「記錄與武器都非 0 但不一樣」那 13 隻不是矛盾，`BUGBEAR` 的 `2d4` 是**放下武器時**的天生攻擊；★ 大／小體型在**戰鬥當下**才切（`overlay-13:1608h`），兩處都要有；★ remake 把 `FighterWithEquipment` 從「第一件有傷害骰的裝備中物品」改成**槽 0**——舊規則會被排在鏈前面的弓或彈藥搶走；★★ **`DS:7603h` 是下一批怪物的圖示槽**（`+143h` ＝ `ICONINDEX`，`overlay-33 entry#5` 與 `CPIC` 組檔名）：`inc` 在放置迴圈**結束後**才做一次 ⇒ 同一批共用一張圖，而 0..7 是隊伍成員的圖示，所以 `CLEARMONSTERS` 重設成 8 不是 0；它是**快取索引不是遊戲狀態**，remake 不需要對應動作，`1Ch CLEARMONSTERS` 轉 `done`（`partial` 剩 8 個 opcode／818 條指令）；第 642 輪：**`2E10h` 的髒旗標建進 VM，重畫不再靠「回頭掃寫入」猜**（spec 1172）——★★ spec 1150 讀完的模型終於接上：`STOREVALUE` 寫 `C04B`／`C04C`／`C04D` 時**當場**鏡射到 `720Fh`／`7210h`／`7211h` 並立 `8B68h`，`2Dh CALL 2E10h` 取快照後把五個旗標清掉；先前那個「回頭掃同一 block、執行序在前的 `SaveWrites`」啟發式整支拿掉；★★ **`CALL C01Eh` 要在 VM 裡走一步**——原作的 `MoveForward` 當場改地圖暫存器，而 remake 把 `CALL` 的副作用留到執行結束後才套用，鏡射不跟著走的話中間那些 `2E10h` 的快照會停在走之前，盜賊公會的走位動畫少掉第一步；⚠ 留下**兩個 remake 這一側的補丁**（原作沒有，都寫成可以拿掉的條件）：鏡射多記「三格是哪個 block 的腳本寫的」，以及 `Written` 遮罩每次頂層執行重新起算——兩者的成因是同一個：remake 改變隊伍位置的路徑不只一條、不是每一條都回寫 ECL 暫存器；★ 回歸擋板跑的是**真的 bytecode**，手寫 `RunResult` 會把「快照在什麼時候取」的答案寫進輸入裡；第 641 輪：**卷軸的 `USE` 也接上了，戰鬥裡的「使用」整條走得通**（spec 1171）——★★ 原作判「是不是卷軸」看的是**類別表的第一格（裝備槽）**落在 `0Bh`..`0Dh`，**不是物品類別**：`3Ch` 的名字叫 `Scroll` 但槽是 `0Ah`，原作把它當充能物品，所以正確的數字是**卷軸 12／充能 18**（先前寫成 15／15），連帶答掉 spec 1169 留的「`5Fh`／`60h` 沒有持有者」；★★ **`+35h` 一個位元組管兩件事**——它是隱藏名稱旗標，非 0 時連卷軸上有哪些法術都列不出來，清掉它同時把名字補成 `... With 3 Spells`，**辨識與命名是同一個欄位**；★★ **名字編號 `+30h` 就是剩餘支數**：一律 `0D4h`，每唸一支減一、`< 0D2h` 才丟掉 ⇒ 唸完第三支才消失；★★★ 失敗判定正是 AD&D 1e：牧師槽或法師槽有等級 → 一定成功、**盜賊 10 級以上 → 1d100 ≤ 75**、其餘一定失敗只印 `oops!`（失敗**不消耗**卷軸）；★ 選單本體是通用法術挑選器，七個標題字串把用途講死（USE 傳 `on Scroll`）；remake 唸卷軸與記憶施法**選目標的方式一樣**；⚠ 條件 B 的 `+111h > +0E6h` 沒讀出來，只實作「牧師槽有等級」那一半；第 640 輪：**充能物品的 `USE` 接進 remake，15 件全部活過來**（spec 1169／1170）——★★★ 突破口是**法術主表那 13 筆無名列不是占位，是物品效果列**：火球的 handler 第二條就是 `cmp ds:6F97h, 40h`，為飛彈項鍊分了 `(1d3×2＋1)d6` 的路，**如果玩家取不到，這段程式沒有存在的理由**；九支 handler 逐條讀完，四句訊息（`is Speedy`／`is stronger`／`is paralyzed`／`is invisible`）把語意釘死——巨人力量藥水把力量提到 **21**、時間 `(1d4＋4)×10`，**比克魔杖只打 `RACETYPE = 12h`**（資料裡那四種全是 Moander 的植物眷屬）；⚠⚠ 這個缺口**是自己的過濾器造成的**：`spell_damage_table.py` 的 `or spell["placeholder"]` 讓四列從來沒被讀過，而排除條件的來源是 spec 815 一句沒驗證過的「玩家取不到」——**排除條件也是斷言**，已寫進 `AGENTS.md` §3；★ 接線本身沒有新規則：分類用欄位不用類別（粉塵與項鍊的類別是 `46h`）、等級一律 6、目標形狀取自 `+6` 敵我借用 `+7`（⚠ 借用是推論，沒見過的組合一律回錯誤）、扣除順序「數量 > 1 就只扣數量」；`U` 使用、`Alt+U` 換一件；沒接的三件事（卷軸的 `sub_3474h`、`92h` 到期換回力量、選單裡的物品清單）寫在 spec 1170；第 639 輪：**戰鬥選單改成逐項串接，並把 `USE` 的缺口量成數字**（spec 1168）——★★ 原版六章 **253 件物品** ＝ 卷軸 15／充能 15／其他 223（`cmd/item-use-audit`）；⚠ 卷軸的判準（類別 `3Ch`..`3Eh`）是從資料推的，工具逐件檢查「名字含 Scroll」⟺「類別在這三個裡」，253 件全部成立、不成立就整支失敗；★ 選單照 spec 905 的形狀改成逐項串接，`使用` 誠實了（身上沒東西就不顯示）；`移動`／`施法` 還沒照條件收放，理由寫在 spec 1168；⚠ 新工具第一版走了 remake 的解碼路徑讀原版 DAX，被既有的閘門當場擋下來（spec 1121）；第 638 輪：**驅散不死接進 remake**（spec 1167）——規格（spec 834／905）早就 `READY`，缺的只是接線：戰鬥主選單先前七項少了 `退散`；★★ 矩陣逐 byte 取自資料段 dump，**`0` 與 `−1` 不能統一**——兩者驅離／摧毀行為相同，但額外配額那一條原作寫的是 `if 有號(v) < 0`，`0` 那幾格摧毀之後不加回配額；★ 選單改成動態（牧師槽等級 > 0 且 `TRIEDTOTURN` ＝ 0 才接上，用過就消失），⚠ 條件裡沒有「場上要有不死生物」；★★★ 順帶定案 spec 1164 留的未定項（spec 1166）：**DOS 少的那一格就是 `MONSTERTYPE`**——原版怪物資料的 `+14Ch`＝0、`+14Dh`..`+150h` 是位移小、節區隨資料往前走的堆積指標，照 PC-98 版面讀會得到「一隻怪帶 12 件物品、節區永遠 `001E`」；`internal/monster` 的 `RawMonsterType` 改名 `RawItemCount`（恆為 0、沒有規則讀它，無行為變化）；第 637 輪：**把 spec 1164 那張表用到另外三份記錄上**（spec 1165）——★★ 22 bytes 戰鬥狀態 ＝ `COMBATVARREC` 19 欄，spec 806 的表補齊：**`+0Bh`／`+0Dh` 根本不是欄位**（是 `TARGET` 這個 4 bytes 遠指標的中間位元組，原本列在「還沒看到用途」）、`+11h` ＝ `TRIEDTOTURN` 是最後一格，而 `+05h` ＝ `ZEROLEVBLOWS` 與角色記錄 `+0DDh` ＝ `ZEROLEVELBLOWS` 兩份互證；★ `.SWG` ＝ `CHARITEMFILREC` 十四欄全對上（`+35h` ＝ `IDENTIFIED`，與 spec 1036 讀到的名字一致）、`.FX` ＝ `EFFECTREC` 五欄；⚠ 六處讀法待對集中列出，**只並列不覆蓋**——既有的 `decoded` 判讀是從程式碼讀出來的，光憑名字不足以推翻；其中 `internal/combat` 的 `AlreadyFled`（`+10h`）在 Pascal 那邊叫 `TURNED`（被牧師逼退的不死生物），可觀察行為相同但來源不是士氣，士氣那條是 `+14h` `ROUTING`；第 636 輪：**PC-98 除錯符號展得開 record 版面**（spec 1164）——型別記錄尾巴那三個位元組先前只當 `Detail` 收著，**後兩個是小端序的成員索引**（指標指到目標型別、record 指到第一個成員的 1-based 索引），欄位依序排列沒有對齊填塞；★★★ 角色記錄的 `unknown` 從 **24 變成 0**（422 ＝ decoded 299／documented 123）——`MINREST`／`SIZE`／`RAISED`／`MODIFIED`／`OLDCLASS`／`OLDLEVEL`／`RACETYPE`／`BASEATTBLOWS[1]`／`RANDOMID`／`BASEEXP`／`EXPPERHP`／`HEAD`／`BODY`／`COLORLIST`／`PDLNREMOVECURSE`／`DUM2`／`DUM3` 全部有名字；★★ DOS 的 422 比 PC-98 的 423 少一格，在 `+14Ch`..`+186h` 之間、之後整批往前挪一格——拿原版存檔對，挪一格之後 `MAXHP`＝`CURRENTHP`＝51、`BASEMOVE`＝`MOVE`＝12、`ENCUMBERANCE`＝300 全部成立；⚠ 三處與既有規格讀法不一致（`+0E6h`／`+11Ch`／`+192h`）只並列不覆蓋；★★ 另有 31 個具名 record 的逐欄版面一併產出（`WALLSET`／`TACTICALMAP`／`MAP3D`／`CHARITEMFILREC` ＝ `.SWG`／`COMBATVARREC` ＝ spec 806 那 22 bytes／`EFFECTREC` ＝ `.FX`），見 `docs/audit/pc98-record-layouts.md`；順手刪掉一份重複的覆蓋報告（`docs/audit/save-field-coverage.md`，正本是 `dos-save-field-coverage.md`）；第 635 輪：**SAVGAM 的前四塊就是 ECL 位址空間的前四區**（spec 1163）——`Area1`／`Area2`／`Runtime`／`ECL` ＝ 區 0／1／2／3，`ECL 位址 = 基底 + SAVGAM 位移 ÷ 2`；三重對齊：四塊大小與四區格數×寬度一一相符、spec 181 讀到的寫入順序與 spec 1096 讀到的 bank 指標順序一致、★★★ `internal/area` 那九個「未解讀」的具名位移套上算式之後**每一個都落在已知的 ECL 位址上**（`0x1E0`／`0x1E2`／`0x1E4` ＝ 移動前座標快照與上一段 ECL 編號、`0x1FA`／`0x1FC` ＝ 各段自己寫的天空色、`0x342` ＝ 世界路線查出來的城市、`0x5C2` ＝ 頭像、`0x624` ＝ `7F12h`）⇒ 那幾格從來不是「Area 記錄的未知欄位」，是 ECL 變數；★★ 兩側都接上了：存檔只動 VM 碰過的位址（沒建模的原版位元組原封不動）、讀檔不收 0 值、Area 編碼器那幾格以 `s.Area` 為準；⚠ 區 3 不寫回，自我修改過的 bytecode 不進原版版面的存檔；★★★ 順帶補掉第 634 輪自己開的洞：per-block 暫存**沒有進存檔**，存檔換段後讀回來會把別段的一次性旗標清掉（房間會再演一次）——`SessionSnapshot.BlockScratch` 補上，`internal/ecl` 與 `internal/game` 各一條兩面測試，都做過變異驗證；第 634 輪：**spec 1160 那六件事全部落地，`go test ./...` 全綠**；★★★ 路上解掉一個更根本的問題（spec 1162）：**`4C00h`..`4C0Fh` 是每一段自己的暫存**，不是整個 session 共用一份——判準是那 16 格**沒有任何唯讀的消費者**（會讀的段一定自己也寫），而 `4C10h` 以上滿是跨段交接；判定證據是眼魔洞穴的死精靈：世界地圖的旅行耗時暫存離開時把 `4C06` 留在 4，洞穴的 `0651h COMPARE 4C06 01` 因此走 `>` 那條並跳過選單，**袋子、毒氣陷阱與冒險手札第 59 條整串拿不到**；★★ spec 1142 那幾間「走到了卻沒有反應」的房間逐一復原，並校正兩處誤判（「下水道塌了」要從下水道進來才唸、停屍架那一格分派到一句 `EXIT`）；★★ 主線因此多演了好幾段（奧莉薇在路上就遇得到、火刀檢查哨要答否、立石群灰袍男子直接揭露烈焰之主）；新增 `standing-stone.seek-green`；★ 工具：`cmd/ecl-window -at <位移>`；第 633 輪：**GEO 目錄交給 `State` 了**（spec 1161 的 remake 段）——`SetGeoCatalog` ＋ `currentDungeonGrid` ＋ `refreshDungeonTerrainFromMap`，`projectFreshDungeonCoordinatesBeforeCall` 投影完座標就照原作重畫的做法把牆面／地形讀回 `C04E`／`C04F`；`cmd/azure-bonds-game` 載完目錄也交給 `State`，遊戲本體與測試同一條路徑；兩面測試 `TestRedrawRefreshesTerrainOnlyWhenTheScriptMovedTheParty` 釘住「有寫座標才重讀、沒寫就不動」——只釘一邊的話「每次重畫都重讀」與「永遠不重讀」各會過一邊；★ 效果實測：把 spec 1160 那六件事套上去之後，眼魔洞穴到站的 `C04E`／`C04F` **不再需要測試手動補**，直接就是 `8`／`0C0h`，而先前那三處手動補值（巫師塔塔頂、熔岩池、眼魔洞穴）也都不需要了；★★ 六件事＋九處斷言＋巫師塔走法全部套上之後，`internal/game` 只剩主線通關測試的眼魔洞穴那一段，而卡住的點已經定位到 `ECL4/0x22:0651h`：死精靈那個選單由 `4C06` 擋成**一次性**，投影生效之後主線會在更早的地方先走到那一格、選單就在那時被消費掉 ⇒ **剩下的是那一段測試的內容順序要重排，不是引擎問題**；六件事本身仍未推（缺一條主線就會停在某個門口），實作已還原、工作區全綠；第 632 輪：spec 1160 那六件事全部實作、九處原作行為的斷言全部改完，`internal/game` 從**四條紅收斂到只剩主線通關測試的眼魔洞穴那一段**——路上把提爾弗頓、盜賊公會、猶拉什、巫師塔、希爾斯法、贊提爾都走通了；★★★ 擋住最後一段的是**結構問題不是路線**（spec 1161）：原作 `2E10h` 重畫會**自己去地圖讀**目前這一格的牆面／地形寫進 `C04E`／`C04F`，而每格事件正是靠 `C04F` 分派；remake 的 `State` **手上沒有 GEO 目錄**（`grid` 一律由呼叫端傳進 `MoveDungeon`），所以腳本傳送（只寫 `C04B`／`C04C`）之後 `C04F` 還停在被搬走前那一格，接下來的事件就分派到錯的一支或什麼都不演——實驗裡巫師塔塔頂、熔岩池、眼魔洞穴各要手動補一次 `C04E`／`C04F` 才走得下去 ⇒ **下一步是把 GEO 目錄交給 `State`**；★★ 巫師塔的走法解出來了：`GEO5/0x33` 是五個互不相連的房間，樓梯朝向表在 `1C81h`（`090Ch` 用地形碼查，朝向不對直接 `EXIT`），⚠ 索引 1 給的是 `5` 不是合法朝向——`(7,15)` 不是樓梯而是出口，要朝東才演 `0811h` 那個「地道還是荒野」選單；★★ 眼魔洞穴 `(5,9)` 的傳送器把隊伍送到 `(13,1)` 朝西，而且死精靈那一段就在同一次執行裡印出來，pack 的 `set_map_position` 替身在位置那一半已經多餘；★★ 測試走訪器兩條規則：落點不是規劃那一格就把控制權交回呼叫端（否則症狀出現在終點、原因在中間某一步）、開鎖門子路徑也要記「被拒的邊」，而寫死的隨機格排除清單是**逐圖**的（`(8,15)`／`(12,10)` 落在巫師塔那張圖上）；整輪實作已還原、工作區全綠；第 631 輪：把「拿掉宣告 spawn 那道閘門」整套做了一遍再**還原**，推導成果全部留在 spec 1160——原本以為三件事，實際是**六件**：① 拿掉 `Spawn` early return、② `2E10h` 的守衛改成逐條看 `CallRequests[i].BlockID`（跨 block 的執行原本整批跳過）、③ `2Dh CALL C01Eh` 改讀腳本當下寫的 `C04D` 並把走完的座標寫回暫存器、④ 重畫要**消費**座標寫入（原作是髒旗標、重畫時清掉；沒有這條游標，走位迴圈裡第二次重畫會把進場 spawn 再投影一次、把中間走的步數整個抹掉）、⑤ 宣告的 spawn 改成「沒人指定時」的後備（否則會得到「X／Y 來自 pack、朝向來自腳本」的混合值）、⑥ 拿掉 `ECL2/0x02` 的座標位移；★★★ **那個位移是投影被壓掉時湊出來的**——公會進場寫 `(8,0)` 朝東，`0D44h` 的五次迴圈照方向表 `1,2,2,2,2` 走成 `(9,3)` 朝南，而 `(9,3)` 正是 `ECL2/0x02` 每格分派表裡公會主人那一格（「BEFORE YOU STANDS A BURLY MAN…」）⇒ 腳本的暫存器本來就是 GEO 格子（與 spec 1158 的哈普村同一個形狀），spec 292 記的到站暫存器 `(1,12,0)` 是套過位移之後的值；★★ 九處原作行為逐處推導完成（賢者／武器店／神殿／高階祭司離場退回門外、訓練所往南一格、酒館走位到 `(7,12)`、城門衛兵送回 `(2,0)`、公會走到 `(9,3)`、猶拉什進場走到 `(0,3)` 朝西且收尾朝向由 `0185h` 的 `SAVE 4C00 C04D` 決定），十個測試斷言也都重新推導過；★★ 實驗裡卡在巫師塔塔頂，但**出口已經找到**：`ECL5/0x33:0811h` 那段的選單選 `CAVES` 走 `088Bh` ＝ `SAVE 06 C04B; SAVE 0F C04C; NEWECL 32h`——⚠ 那兩句座標寫入**後面沒有 `CALL 2E10h`**，交接靠 `NEWECL`，remake 這一側要由 `syncDungeonStateFromECLRegisters` 接；而塔頂 `(3,1)` 在 `GEO5/0x33` 上**不是孤立的**，走不出去是因為 `walkNormalDungeonTo` 那份寫死的排除清單（`(8,15)`／`(12,10)` 落在這張圖上，把第 15 列走廊切斷）⇒ 要處理的是走訪器，不是遊戲機制。因為六件事缺一條主線就會停在某個門口，本輪**不推半套行為改動**，整套實作已還原、工作區全綠；第 630 輪：**原作只有一條寫入路徑 `STOREVALUE`**（spec 1159）——所有會寫變數的 opcode 都走它，而它同時把隊伍座標鏡射到 `720Fh`／`7210h`／`7211h` 並立髒旗標，所以「哪些 opcode 寫過座標」在原作裡不是分類問題；remake 的 VM 先前只在 `09h SAVE`／`35h SAVE TABLE` 記 `SaveWrites`，算術／`08h RANDOM`／`2Ah GETTABLE` 的寫入等於沒寫，已改成全部走同一支 `recordStore`；★★ 分類數字跟著改：「明確移動」是 **12 處不是 8**，多出來的四處全是 `GETTABLE` 的假門傳送（兩層查表），先前被歸到「沒有座標寫入」——**工具與 VM 同一個成因的假零**；新工具 `cmd/ecl-redraw-sites` 是這份分類的可重生清冊（125 = facing 67／move 12／restore 15／picture 3／none 28）；★★★ 12 處裡有 8 處還被兩道閘門擋著：地圖宣告了 `Spawn` 就整張跳過（提爾弗頓／贊提爾暗黑神殿／巫師塔），跨 block 的執行整批跳過（盜賊公會）；★★ 本輪拿掉第一道閘門量過一次（已還原）：提爾弗頓一次浮出六個原作行為——賢者菲拉妮／科米爾武器店／剛德神殿／高階祭司離場都**退回進門前那一格**（`(5,5)`／`(3,12)`／`(1,7)`／`(2,10)`）、酒館走出門後有一段**走位動畫**停在建築側邊 `(7,12)`、城門衛兵第一次會把隊伍**送回 `(2,0)`**；⚠ 但不能就這樣拿掉——`2Dh CALL C01Eh`（`MOVEFORWARD`）用的是 `s.DungeonDirection` 而不是腳本當下寫的 `C04D`，盜賊公會抓捕動畫在 `0D90h` 先 `SAVE 7F7B C04D` 指定方向，朝向沒同步就會走錯方向（spec 292 量到到站暫存器是 `(1,12,0)`，朝西走會變成 `(13,0)`）；第 629 輪：**`C04D` 條件拿掉了**——只寫 `C04B`／`C04C` 的重畫就是真實移動、朝向不變，那 23 處全部生效（spec 1158）；擋著的四條路線測試逐條重新推導，順帶推翻一個地圖範圍上的斷言：★★★ `GEO5/0x32` 同時裝兩張圖，**哈普村只有 x 0..5**（地形碼 `< 0x80`，`ECL5/0x31`），東半是黑暗精靈／古熔岩洞（`ECL5/0x32`），分界寫在 `03D0h` 的 `COMPARE C04F 80h; IF <`；每格分派器 `AND C04F 7Fh` 把最高位遮掉，所以 `ecl-cell-events.md` 兩個 block 的格子欄長得一樣**不代表兩邊都到得了**——舊路線從 `(5,12)` 一路往東走到 `(15,5)`，等於在村子的 ECL 底下逛另一張圖；村子出口是「地形碼＋朝向」成對（`01`/北、`03`/西、`02`/東），`(5,12)` 往東是唯一東側出口，答 `NO` 走 `0E9Fh` 退回上一格；★★ `4C02` 是「這一步已經演過一場」的閂（`03D7h` 每格清 0、四支場景演完寫 1），舊斷言 `4C02 == 1` 成立只因為隊伍停在東半、清零那句被跳過，現在改成「剛演完是 1、停在 `(4,12)` 是 0」兩個斷言；★★ 另外兩處玩家選項會退格：`ECL3/0x11:0589h` 的 `FLEE`（改挑 `WAIT`，走 `1075h` 跳過那兩句 `SAVE`）、`ECL6/0x40:097Fh` 的 `GO`（改挑 `REBURY SKELETON`）——改完之後隊伍第一次真的穿過去，**多走到以前走不到的格子**（該段 19s→44s，墓園 `(6,12)` 的骸骨選單就是這樣冒出來的）；★★ 尋路要記「被拒的邊」：走完一步座標沒變就把 `{地圖,格子,方向}` 記進觀察器繞開，否則症狀出現在終點、原因在中間某一步；★★ 工具盲點：查「誰跳到這個位址」要用 `cmd/ecl-window -into`（走 `TraceGraph`，跟 `ON GOTO` 的邊），`-opcode 24 -operand` 只比對指令運算元——`160Ch` 用 `-operand` 只算得到 3 個入口、`-into` 是 6 個，而玩家實際踩到的三個全在漏掉的那一半；新增兩面測試 `TestFireKnifeFrozenRoomRetreatReturnsToPreviousCell`（`RETREAT` 退回 `(5,2)`／`INTERROGATE` 留在 `(4,2)`），並把原本釘著「沒有 `C04D` 就不搬」的那條單測改成釘相反面；第 628 輪：那 23 處逐處分類完成（spec 1157）——只有兩種：8 處明確移動、15 處 `SAVE 4BF0 C04B; SAVE 4BF1 C04C`「退回上一格」，**兩類都是真實的隊伍移動**，所以 `C04D` 條件擋掉的是玩家看得見的位移；★ 那 15 處的守衛**各不相同**（`4C10`／`7ED5`／`4C06`／`4C44`／`7F79`／`4CE4`／`7F82`），只有一處用 `7EC7h`——上一輪寫的「等 `7EC7h` 有 producer」不是共同前提；★★ `ECL2/0x04:160Ch` 是從**六個**地方跳進去的共用退格出口，先前寫「只有戰後走得到」是只看了一個入口；★★★ 實跑探針（已還原）定案火刃巢穴三條路線測試會紅的原因：投影發生在退格處理之後的 `161Ah`，而 `COMPARE 7EC7 80h` 讀到的確實是 `0 > 80h`＝假 ⇒ 走的是另外五個入口之一——**那三條路線是照著被 `C04D` 壓掉之後的行為寫出來的**，要換忠實模型得連同重新推導路線一起做；★★ 順帶抓到第二處被 IDA 切錯的共用 handler：`2Fh`／`30h` 共用 `0DA4h`..`0E12h`（43 條，IDA 拆成 11＋32），audit 表原本連 `2Fh` 那一列都沒有，兩列都補上並在表頭寫了邊界警告；★★ 差點照著一行錯註解改壞程式：`AND`／`OR` 之後的比較是 `compare_variables(0, 結果)`，**`0` 是左運算元**（`overlay-02:0DF3h` 先 push 0），remake 的程式碼本來就對、錯的是註解——而全 corpus 174 處 `AND`／`OR` 沒有一處接排序型 `IF`，所以對調了**所有測試照樣全綠**；第 627 輪：`7EC7h` 命名 ＋ 修掉「拿 PC 當執行順序」那個代理（spec 1156）——`7EC7h` ＝ `bank1^[58Eh]`，寫入端全在 `overlay-05`（POSTCOM）四處，`0817h` 寫 **`81h`**、`0B52h`／`185Dh` 寫 `80h`、`173Fh` 寫 `0`；腳本一律 `COMPARE 7EC7 80h / IF >`，只有 `81h` 成立 ⇒ **`81h` ＝ 這一場沒有打贏**（spec 303 的勝利線、1145 的重打迴圈、318 的退回三處用法一致，三份都刻意沒命名它）；★ `ECL2/0x04` 三處座標寫入全部列出，其中 `02D7h`「YOU ARE BEING TAKEN TO THEIR LEADER.」是**沒有 `C04D` 的真實移動**，remake 現在整個跳過——玩家看得見；★★★ 順帶抓到一個結構性問題：`projectFreshDungeonCoordinatesBeforeCall` 用 `write.PC < call.PC` 篩「這條寫入在 CALL 之前嗎」，但**反向跳躍時 PC 小不等於先發生**；已改成 VM 記執行序（`MemoryWrite.Sequence` 與 `CallRequest.Sequence` 共用同一條計數，所以序號會跳號——死精靈那段是 1、2、3、**5**，中間的 4 是那條 `CALL`，這本身就是「兩份清單在同一條時間軸上」的證據）；⚠ 這個修法**單靠第一版回歸測試抓不到**：那一版把後執行的那一輪放在比 `CALL` 小的 PC，但兩種比法給的答案相同，改回 PC 版仍然全綠——要真正的反向跳躍（`CALL` 之後跳回小 PC）才區分得開；⚠ `C04D` 條件仍在，行為在真實 corpus 上沒有改變；第 626 輪：**`4BF0`／`4BF1` 的來源解出來了**（spec 1155 補完，spec 1045 加上連結）——用 spec 1098 的區 0 換算 `bank0 + (位址−4B00h)×2` 把兩個格子對回 `bank0^[1E0h]`／`[1E2h]`，而 spec 1045 的地城主迴圈（`overlay-02:3984h`）在呼叫 `MOVEPARTY` 之前就把當時的 X／Y 存進去、`MOVEPARTY` 回來再比對「隊伍到底有沒有移動」⇒ **那兩格是「這一步移動之前的座標」**，腳本那 15 處 `SAVE 4BF0 C04B; SAVE 4BF1 C04C` 是**把隊伍退回上一格**；★ 缺的從來不是逆向——spec 1045 記著「記下座標」、spec 1098 記著換算公式，兩份都 `exact`，缺的是把它們兜起來的那一句；⚠ 而 `overlay-02:3984h` 那一段 **IDA 沒有認成程式碼**，是位元組掃描找到的，只查函式清單會落空（這條也是 spec 515／516 從第 515 輪留到現在的未決項）；remake 的 `MoveDungeon` 已接上同一個時機並用兩個變異釘住（存成移動**後**的座標會讓那 15 處等於什麼都沒做而且測試全綠）；⚠ `C04D` 條件**還沒拿掉**：補上 producer 之後再試一次，失敗座標從 `(0,1)`（垃圾值）變成 `(5,3)`（真座標）——資料對了，剩下的差異是「哪些 `CALL` 該投影」，那要另一輪；第 625 輪：把 `2Dh CALL 2E10h` 的缺口從「模型不對」收斂成一個具體的擋路點（spec 1155）——125 處可達的 `CALL 2E10h` 逐處分類：67 處寫了 `C04D`（remake 會投影座標）、**23 處只寫 `C04B`／`C04C`**（remake 整個跳過，而原作 `STOREVALUE` 當場就寫了）、3 處只有 `PICTURE`、32 處前六條沒有座標寫入；★ 那 23 處裡有 **15 處是同一個慣用法** `SAVE 4BF0 C04B; SAVE 4BF1 C04C`（還原一組存起來的座標），而 `4BF0`／`4BF1` 在整個 ECL corpus 裡**只被常數寫過兩次**、remake 這一側沒有任何 producer；★ 做了一次**已還原**的對照實驗：把那個 `C04D` 條件改成原作模型之後四個測試立刻失敗，隊伍被丟到 `(0,1)`——⇒ **那個條件不是把 `2E10h` 讀錯了，是在補 `4BF0`／`4BF1` 沒有 producer 這個洞**；就算把五個髒旗標全部實作出來，那 15 處還原一樣會把隊伍丟到錯的格子。spec 1150 與 `effects.go` 的 `partial` 理由都改寫成這一句；⚠ 這一輪**沒有改行為**——照 WORKLIST 自己寫的警告，先建重現材料再談換模型，而重現材料證明了不該換；第 624 輪：`38h PROGRAM` 由 `partial` 轉 `done`（spec 1154）——四個分派值 0／3／8／9 全部讀完，corpus 13 處四個值都用得到、沒有一處落在什麼都不做的預設值；★ **打通關看得到結局了**：原作 `PROGRAM 8` 先跑 `overlay-18:10FFh` 的結局過場才回主選單問存檔，而 remake 先前直接跳到存檔詢問，一句結局都看不到。spec 1082 有全部台詞但只寫「…印一段…」沒給邊界——把字串位移與等鍵呼叫依序取出來之後，分段是**五頁、四次等鍵**，而且**第 4 頁是 8 行、其餘四頁各 4 行**（它把「枷印消失」與「騎士抵達」合在同一頁）；第 5 頁演完沒有等鍵，直接落回存檔詢問。主線通關測試會逐頁走過去，順便驗那五頁在真實玩家路徑上到得了而且是中文；⚠ **spec 1082 的中文草稿與專案 glossary 不一致**（提蘭斯拉克蘇斯／密斯卓諾／影谷／艾米斯特／莫安德／萊桑德／光輝之池），譯文一律照 `coab-glossary.md`（提朗瑟克斯／迷斯卓諾／暗影谷／伊爾明斯特／摩安德護手／洛山達護符／光芒之池）；⚠ **spec 1087 把 `4FC7h` 說成「訓練不收費」旗標是錯的**——那是 spec 1045／1095 的離開旗標，也是 spec 1152 讀到的「全隊倒下」那一格；訓練所的免費旗標是 `7587h`／**`4FC8h`**／`8B6Fh`，差一個位址。所以 `PROGRAM 3` ＝ 宣告隊伍全滅，remake 本來就是對的；`partial` 由 10 個 1,037 條降為 **9 個 1,024 條**；第 623 輪：`37h LOAD PIECES` 收尾（spec 1153）——★ 它與 `21h` 共用的那一支**函式邊界是錯的**：IDA 只認到 `0D4Ah`（104 條），真正的 `retf` 在 `0DA3h`（131 條），被切掉的正是收尾那段重繪判斷（audit 表已改）；★ 三組牆面參數的寫入端找到了——`LOADWALLSET` 的**收尾六條**寫 `[7210h＋槽×4] := 片`、`[7212h＋槽×4] := 槽`（第二個 word 是**槽號本身**，兩個運算元都取 `arg_0`），運算元是 `0FFh` 的槽則由 handler 自己寫 `0FFFFh`；這三格就是存檔第 9..14 欄，而 **remake 從來沒有寫過它們**，存出來的檔一律是零——已接上並釘住三個方向；★ 順帶定案 `21h LOAD FILES` 的**第一個**運算元是**要載的地圖 block**（全 22 處掃過；通常等於所在 block 但不一定——三處載的是別張）；⚠ **spec 1087 的運算元順序寫反了**——它照 IDA 的 `var_N` 標籤抄，而那是「離 `bp` 幾個位元組」不是運算元編號，反過來讀會得到「`LOAD3DMAP` 全遊戲一次都不執行」這個自洽但錯的結論；PC-98 側的 spec 587 一直是對的，corpus 是第二個證人。正文已改寫，並把這個錯誤寫成規則進 `AGENTS.md` §3（連同「下結論前先 grep 那個位址」——這一輪和上一輪各踩了一次）；`37h` 仍 `partial`，剩兩條 corpus 走不到的路（`o[1] = 7Fh`、兩槽分支）；第 622 輪：`2Eh DAMAGE` 整支 305 條讀完（spec 1152），連 `MAKESAVE`／`TRYTOHIT` 也各自讀完——★ **旗標 bit 7 清空時整個 byte 是次數**：連打 N 下、每下隨機挑一名隊員、用第五個運算元當**攻擊值**（不是豁免種類）擲 `TRYTOHIT`，而且**每下之間重擲傷害**；bit 7 設定才是旗標（bit 6 全隊、bit 5 不擲豁免、bit 4 豁免成功仍吃**全額**、bit 0..4 豁免調整值，⚠ bit 4 與調整值欄位重疊，但 corpus 24 處低 5 位全 0 所以實務上只是旗標）；★ 目標三選一，而「目前角色」那一路的**豁免種類要減一且 `0` 代表不擲**，另外兩路不減一、由 bit 5 決定——兩種讀法在 corpus 上結果完全相同，只驗行為分不出來；★ 「單體但隨機挑一名」在原作是活著的程式碼但 corpus 24 處**走不到**；remake 的規則本來就對，缺的是接線——正式路徑先前只結算全隊那 14 處，另外 10 處（8 處目前角色、2 處連打）永遠留在 pending，現在都結算了；★ 目標改由**封包自己**帶著發出當下選的角色，因為腳本會把 `LOAD CHARACTER` ＋ `DAMAGE` 包在走過整隊的迴圈裡（`ECL5.DAX/0x32:0223h` 配 `7F3Eh` 隊伍人數），拿聚合值會把整批傷害算到同一位身上；⚠ 結算的呼叫點**維持**在 `applyECLLoadCharacterSignals` 之前——看起來該移到後面，但沒帶選定角色的封包代表 VM 到那一刻沒人被選過，該退回的是這一次執行**之前**的值；順帶答掉 spec 582 的未定項之一（豁免種類由呼叫端夾成 0..7），並補上兩句只有 DOS 有的英文（`The entire party is killed!`／`press <enter>/<return> to continue`）；第 621 輪：隨機戰利品接上原作的造物品常式 `CREATERNDTREASURE`（`overlay-21:0FEDh`，spec 1036）——`ItemBlock > 80h` 擲出類別之後，加值、名稱三段索引、重量、價值、卷軸法術與特殊物品範本都照原作補齊，撿東西畫面上顯示得出 `＋N` 與名稱修飾；加值分布寫進 spec 1036（`sub_FAF` 擲 1d20，1..14 給 ＋1、15..20 給 ＋2）——那一項原本列在「明確不宣稱」，但**函式台帳早就記著**，這是補上規格不是新發現；★ 抓到 spec 1036 重量表的一格死分支——`149Fh` 寫「`2Fh` 或 `5Dh` ⇒ 1」，但 `2Fh` 在 `13DBh`（80）就被同一條 if-else 鏈攔下，照抄那一列會和原版分岔；擲骰順序（`plus` 先擲、`15h` 的 1d5 後擲、`47h` 的 1d8 在範本選擇裡擲）用 `scriptedRolls` 逐顆釘住，十二個變異只有補完卷軸環表與價值係數表之後才全部被抓到；`27h` 的缺口從兩項收斂成一項（多筆 TREASURE 的覆寫 vs 相加）；第 620 輪：`27h TREASURE` 整支讀完（spec 1151）——戰利品池是覆寫、物品鏈是前插所以清單反序、隨機表每一段兩端都夾（修掉 `{48,49}` 會給劍的 bug）；第 619 輪：`2Dh CALL` 七支分派的名字與內容（spec 1150）——corpus 其實用了四個運算元不是兩個、`B200h` 的第二個音效走不到、`6803h` 是圖片序列的下一格；第 618 輪：`24h` 的三選一順序反了，有怪就該直接打（spec 1149）；第 617 輪：`PICTURE 0FFh` 是關閉而 remake 先前什麼都不做（spec 1148）；第 616 輪：`33h` 是硬換行、缺口在 UI 行模型（spec 1147），並定案 `65A0h` 是欄、`65A1h` 是列；第 615 輪：遭遇距離模型與 opcode→handler 對照表，APPROACH 的減一是演出不是選單條件（spec 1146）；第 614 輪：`1Ch` 連戰利品一起丟掉（spec 1145），火刀首領重打迴圈會多領；第 613 輪：遭遇選單的三句旁白依距離挑，16／20 處原本演錯句子（spec 1144）；第 612 輪：段內支線在主線進度下逐格取樣，與遭遇選單那批沒被數過的旁白；第 611 輪：段界快照讀回來還走得動——`eventReturnMode` 沒進存檔，欄位全對但按下一步卡住；第 610 輪：199 處 24h 逐處分類；第 609 輪：24h 的三選一與營地旗標；第 608 輪：`+07h` 是假缺口，防禦存得下去讀得回來；第 607 輪：把剩餘工作清單重量一次，三個數字過期；第 606 輪：怪物記錄傷害 vs 裝備中的武器；第 605 輪：怪物的物品鏈接進 Fighter；第 604 輪：選曲逐格對回 PC-98 的 selector 表（SEG-33 後半）；第 603 輪：攜帶物的跨段連續性（SEG-31 後半）；第 602 輪：世界地圖從 13 個地點各掃一次、762 個分支；第 601 輪：世界地圖路段編號 `4C9D` 與世界地圖的語系缺口；第 600 輪：分派器配對條件修正與查表分派解讀；第 599 輪：旗標寫入端追溯與 spec 1142 的成因收斂；第 598 輪：全遊戲逐格實測（194 個索引、落回原文 0 格）與朝向刻度；第 597 輪：全遊戲每格事件對照表與古熔岩洞逐格；第 596 輪：第六章三區逐格盤點 41 格；第 595 輪：墓園逐格盤點與語系閘門的 `Prompt` 路徑；第 594 輪：內城一樓房間與 map-local bank（spec 1142）；第 593 輪：跨段不變量（語系／隊伍／音樂）；第 592 輪：**開場到結局的完整主線跑通**；第 591 輪：主線接到第六章密斯卓諾；第 590 輪：整條主線跑拆成 12 個段測試與段界快照；第 589 輪：25 段接線盤點；第 588 輪：`SEG-12` 47 條邊逐條交接；第 587 輪：`SEG-11` 前半，段落快照往返與交接；第 586 輪：`SEG-04` 統一直入旗標，25 段逐段進得去；第 585 輪：`SEG-03` 段落 id 與標籤；第 584 輪：`SEG-02` block ↔ 地圖對照；第 583 輪：`SEG-01` ECL block 轉移機制；第 582 輪：乾淨 clone 的 engine bootstrap；第 581 輪：力量調整表與原版存檔 oracle；第 580 輪：面向／護甲刻度等七個契約搬進共用 engine；第 579 輪：機會攻擊四道閘、隊員命中與 AC 表；第 578 輪：AC 與命中的刻度統一；第 577 輪：第二個 AC 的算式；第 573–576 輪：回合開始生命週期、突襲遮罩判定死碼、面向與背後攻擊、90° 扇形與機會攻擊面向閘；第 572 輪：第一人稱畫面與原版逐格比對）

本檔只保留**目前有效的現況與入口**。歷史敘述已分冊到
[`docs/context/`](docs/context/)，逐行保留原文，不再放在這裡。

## 真相來源優先序

同一件事若多份文件說法不同，依這個順序判定，並主動修掉被推翻的舊斷言：

1. 目前 worktree 的程式碼、JSON 與測試。
2. 原始 bytes、IDA database 與可重現的 runtime trace。
3. 標為 `READY` 且限定範圍明確的 `docs/spec/`。
4. [`docs/knowledge/coab-re-coverage-matrix.md`](docs/knowledge/coab-re-coverage-matrix.md)（系統層完整度）
   與 [`docs/audit/`](docs/audit/) 的可重生清冊（模組／函式層完整度）。
5. [`WORKLIST.md`](WORKLIST.md)（執行順序）。
6. `docs/context/` 分冊與 `docs/project-status.md`（歷史，只作追溯用）。

工作規則的單一權威入口是 [`AGENTS.md`](AGENTS.md)。

## 目前階段：先把反組譯盤點做完，再談語意與實作

2026-08-13 使用者指示停止「邊做邊分析」：先徹底完成 CoAB 的反組譯分析，
再繼續 remake。同日的盤點證實這個轉向有具體理由——

**先前的 RE 是問答式的**：`scripts/ida/` 的 64 支腳本每支只回答當時那一題，
沒有任何一份全模組函式清冊。因此「還剩多少沒看」在此之前無法回答。

**overlay 程式碼幾乎沒被分析過**：DOS `GAME.OVR` 直接載入 IDA 只得到
**1 個函式**；PC-98 同樣。原因是 raw TPOV overlay 沒有 MZ header 也沒有
entry point，自動分析無從開始。真正的 entry point 全在 resident executable 的
`CD 3F` five-byte stub 裡（第 412 輪已證明格式）。

把那份格式知識接成工具鏈之後，DOS 側從 1 個函式變成 **1,320 個**
（36 段 overlay ＋ resident，871 個 entry stub 當種子）。這不是新的語意成果，
是把先前看不見的程式碼變成可盤點的對象。

新的驗收口徑（使用者 2026-08-13 決定）：

- **全函式覆蓋台帳**：兩平台每個函式都要在台帳裡有狀態
  （`已解讀`＋推論等級／`不阻塞`＋理由／`待解讀`），不再只有系統層百分比。
- **兩平台都全掃**：DOS 是行為 oracle，PC-98 有 Borland symbols 可當語意
  交叉驗證；同一機制在兩邊的證據要分開記錄，不得因數字相同就合併。

方法、產物與批次順序見
[`docs/spec/559-full-module-re-sweep.md`](docs/spec/559-full-module-re-sweep.md)。

## 現有能力（可驗證，不等於整作完成）

**正常新遊戲 session 已經可以從開場一路打到結局**：下水道 → 火刀據點 →
戰後世界 → 阿沙本福德 → 立石群 → 艾森布拉城外 → Hap／熔岩洞／巫師塔 →
散提爾堡 → 眼魔洞穴（手札 59、Dexam 雙戰）→ 猶拉什 → 希爾斯法 →
立石群灰袍男子 → 密斯卓諾墓園（紅網、黛米爾公主）→ 外城遺跡 → 內城遺跡 →
儀式與爪牙戰 → 二樓東北角 → **擊敗提朗瑟克斯的結局選單**。
這條路拆成 23 個段 subtest，每段結束存一份快照並整批驗往返
（`TestRealNewGameRunsToTheEnding`）。整條主線的 260 句話**沒有一句落回原文**（訊息與提示兩條路徑都記）；
同一個角色的經驗值不會倒退、隊伍變動的位置是宣告好的；22 個段界都有一首在
game pack 曲目表裡的曲子。

原版的四段建角（種族 → 性別 → 職業 → 陣營 → 擲屬性 → 名字 → `Save?`）已完整
重現，資料全部取自原作資料段並轉成 `gamepack/rules/character-tables.json`。

主線分段有直入的入口了：25 段全部可用 `-segment ECL{成員}/0x{block}` 進去，
註冊表在 `internal/segment`，逐段一條測試。`EnterFrom` 一律要是轉移圖上真的
存在的一條邊（`0x00` ＝ 全新開局，spec 1141）。`ECL5/0x30` 是過場 block——
進去就轉走，停不在自己身上。⚠ 正常新遊戲流程走 `0x50 → 0x01`，**沒有經過開場
block `0x52`**；那一段的原作序幕目前只有 `-segment ECL1/0x52` 進得去。
段的邊界狀態存得下去也讀得回來（25 段的往返閘），`-segment-snapshot` 寫出快照、
`-party-load` 接下一段的交接實際跑通了。⚠ **存檔沒有獨立的 GEO 檔集欄位**，
讀檔時是拿 `Area.GameArea` 重建的——兩者留下不同的值，快照就往返不回來。
47 條 `NEWECL` 邊也逐條有交接測試（存快照 → 讀回 → 帶著來源 block 當 `LastECL`
進目的段）。⚠ 這些測試的來源都是**段的入口**狀態，不是段真的走完的結束狀態。

接線的現況由 `cmd/segment-coverage` 量出來（`docs/audit/segment-coverage.md`）：
**22 段的入口有中文劇情、3 段的入口不出文字、0 段落回原文**。不出文字的三段
（`ECL2/0x02`、`ECL3/0x12`、`ECL4/0x21`）是從別段被帶進來的，劇情在前一段的
轉移與每回合／搜尋生命週期裡，不在 `initial`。剩下的接線工作在**段內**。

整條主線跑拆成 **23 個段 subtest**，覆蓋的步數一步沒少（拆的時候用正規化逐行
比對驗過），每段結束存一份快照並整批驗往返。⚠ 這些段共用同一條 session，
前一段紅了後面就不跑；段界快照不進 repo（存檔格式一改整批失效）。

第六章的三個轉移是**讀 bytecode**找出來的，不是在地圖上撞出來的：墓園 → 外城
與外城 → 內城都是邊界處理的 `ON GOTO C04D`（面向÷2，0 起算，分別要朝東與朝北），
內城的每格事件則是 `ON GOTO (C04F & 0x7F)` 用地形碼分派（樓梯 `0x97`＝`(10,7)`、
最終戰 `0x9A`＝`(6,1)`）。工具是 `cmd/ecl-window`。

第六章三區的每格事件都由地形碼分派（`ON GOTO (C04F & 0x7F)`），所以「哪一格演
哪一場」是從 bytecode 反推的：`cmd/ecl-window -table` 拆表、`cmd/geo-probe` 反查
地形碼。全遊戲的對照表由 `cmd/ecl-cell-events` 一次產完（`docs/audit/ecl-cell-events.md`）：
25 個 block 裡 **14 個**用地形碼分派、**234 個場景對到格子**；另 5 個走 `GETTABLE`
查表分派（尚未解讀）。⚠ **遮罩不是固定的**（`0x7F` 與 `0x3F` 都有），寫死一種會讓
另一半安靜地變成「沒有每格事件」。

**逐格實測也是全遊戲一次跑完**（`cmd/cell-sweep` → `docs/audit/cell-sweep.md`，27 秒）：
每個分派索引直接進段、站到那個地形碼的第一格、跑一次生命週期，記玩家真的看到的字。
14 個 block、**250 個索引**，**235 格中文、落回原文 0 格**，15 格沒演出來且每一格都
印得出處理常式的守衛（12 格是 spec 1142 的 `4C00` 區）。兩份對照表不一致時**以實測
為準**：`ecl-cell-events` 的「第一句」是依位址往後找的推測，處理常式開頭是 `GOTO`
就會撈到隔壁那支的台詞。

世界地圖那一半用的是路段編號 `4C9D ＝ 出發地 × 4 ＋ 選的方向`（spec 1143），
四張表分別給旅行天數、走法數、到達地點與**旅途場景**。走得到的分支由
`cmd/route-sweep` 逐條驗語系：從宣告的 14 個地點各掃一次（13 個進得去），
**762 個分支、敘述與選項都沒有一條落回原文**。

⚠ **選項要單獨判語系**：溫拉爾軍隊那一場的敘述早就中文化了，三個選項卻還是英文
——只看敘述的閘門會說那一場已經接好。**缺口會躲在沒被量的那一維。**

⚠ **`24h COMBAT` 沒有運算元**（arity 0）——它是**三選一的服務分派點**（商店／營地／
戰鬥，spec 1095），不是「打一場」。199 處已逐處分類（`cmd/ecl-combat-sites`）：
**153 處真的要打、46 處走服務分派**，後者後面接的是「GOOD QUALITY TAVERN.」這類
商店／神殿台詞，而且每一處往回走都碰得到 `CLEARMONSTERS`。

⚠ **要沿控制流往回走時，`TraceGraph` 的 `Edges` 只記跳躍、不記循序。** 只用那些邊
做反向可達，多數指令找不到任何前驅——第一版因此得到「199 處裡 0 處擺過怪」的假零。
循序前驅要自己補（前一條會 fall through、而且它的 `Next` 正好是下一條可達指令）。
營地那一支的旗標 `0x7EE2` 在正式程式碼裡**沒有任何 producer**，所以它目前走不到；
remake 的營地走的是 lifecycle entry 2／3 這條等價路徑。

⚠ **修清單的時候會順手寫出新的假缺口。** 第 607 輪把三處過期數字改正，同一次卻在
P0 新寫了一條「`+07h`（保留的機會攻擊）沒有投影進 `Fighter`」——查下去才發現 remake
用 `CombatAction.Guarding` 表達同一個狀態，設定／消費／清除三端都對得上 spec 1136。
**照著 spec 的「明確不宣稱」抄，會把「沒有逐位元組鏡射」讀成「沒有做」。**

⚠ **路由文件的數字是手抄的，會過期。** `WORKLIST.md` 的「還開著的工作」逐列數字
抄自各支稽核工具；工具重跑之後沒人回頭改，就會出現「工作早就做完、清單還寫著開著」
——比沒有清單更糟，因為它把後面每一輪帶錯方向。2026-08-21 對帳抓到三處：回合生命
週期（spec 1135～1138 早就做完）、手札 producer（寫 30 則缺、實際 64 則全接）、
存檔 `unknown`（寫 29、實際 24）；**兩份稽核產物本身也過期沒重產**。
閘門是 `TestWorklistInventoryNumbersAreCurrent`。

⚠ **比對怪物的武器一定要過濾武器槽**（類別表 `+0` ＝ 0）。第一件裝備中的物品常常
是弓或彈藥（slot 10）——用它去比，`FIRE KNIFE` 會被判成「記錄 `1d8`、武器 `1d6`」
不一致，而牠 slot 0 的武器正好是 `1d8`。**槽位過濾漏掉會製造出假的不一致。**

⚠ **怪物的東西要依「怪物 ID 的命名空間」解析，不是依目前的 block。** 世界地圖的
派遣器會召喚別章的怪（`ECL1` 的 `0x50` 會叫出 `MON5` 的 `0x3C`），所以物品鏈、
效果鏈與怪物記錄三者都走同一條解析路徑。物品還要**每一隻各自一份**——共用同一個
slice 的話，一隻換裝會影響到全部，而症狀只在「同一種怪出現兩隻以上」時才看得到。

⚠ **「有值」不等於「值對」。** 選曲的閘門原本只驗「有一首在曲目表裡的曲子」，
那擋不住「每一段都播同一首」。PC-98 的 `BGMPLAY` 直接讀 `CURRENTECL`，所以哪一段
該播哪一首是**查得出來的**（spec 355 的精確表）——現在三層都驗：宣告對回原作的表、
段界播的是對的 selector、整條主線至少播出 4 首不同的曲子。

⚠ **驗「有沒有掉東西」的閘門要先擋非空。** 裝備／記憶法術／效果的跨段比對
（`SEG-31` 後半）如果只擋「沒有變動」，哪天攜帶物整批讀不到、每一段都是空的，
測試照樣全綠——**空的等於空的**。這條路線上真正帶東西的是 NPC 同伴
（阿卡巴的法術書、艾莉亞絲與龍餌的裝備與效果），玩家的單人戰士驗不出任何東西。

⚠ **走訪類的閘門要同時釘住「走到幾個」**。只驗語系的話，走訪壞掉（起點進不去、
選單認錯）會讓分支數塌下來而剩下的每一條還是中文——**全綠，但什麼都沒驗到**。

⚠ **分派器的配對條件是「`ON GOTO` 分派的那一格 ＝ `AND C04F` 寫進去的那一格」**，
不是「緊接在後面」。用相鄰去配同時會漏掉真的分派器（下水道中間隔了兩條指令）與
把不相干的兩條湊成一對（`ECL3/0x15` 那個 `ON GOTO` 分派的是 `4C08`）。
第二種形狀是 `GETTABLE <表>, <索引格>` ＋ 分派同一格的 `ON GOTO`，索引不是地形碼
——兩個世界地圖 hub 是「路段編號 `4C9D` → 14 個旅途場景」。

⚠ **掃描維度的刻度要從消費端讀**：邊界處理常式是 `COMPARE C04D, <方向>`，而
`C04D ＝ 朝向 ÷ 2`、朝向是 0..7。掃 0..3 只蓋得到 `C04D` 的 0 與 1，南、西兩面整批
落空——而落空的格子跟「這一格沒有內容」長得一模一樣。改掃 `{0, 2, 4, 6}` 之後，
猶拉什四個出城口才冒出來，其中三句還是沒接的原文。

⚠ 逐格盤點**每格都重新進段**——once-only 旗標與戰鬥結果會互相污染，塞進連續
session 會把別的段弄紅。

⚠ **`4C00` 區的旗標跨地圖互相污染**（spec 1142）。同一格在不同地圖是不同的東西：
`4C06` 在世界地圖是旅行時鐘（`ECL CLOCK 4C06 04`），在密斯卓諾內城是三間房共用的
once-only 標記。症狀不是崩潰也不是錯字，是**房間安靜地不演**——內城五間房加上
散提爾堡城區四格，守衛讀的旗標帶著別張地圖留下的值（`4C07=0x80` 是眼魔洞穴寫的，
而第四章之後沒有任何一處把它寫回 0）。

換 bank 的時機**不是** `NEWECL`、也**不是** `LOAD FILES`——兩個候選各有一個反例
（`ECL4/0x20` 前導讀的是第二章寫的 `4C01`；`ECL3/0x10` 在 `LOAD FILES` 之前寫
`4C05`／`4C0C`，之後整段都在用）。剩下兩種可能靜態讀碼分不出來，**要拿原版當 oracle**。

⚠ **查旗標的寫入端一律用 `cmd/ecl-cell-refs`，不要查事件目錄**：目錄的指令表有可達性
缺口，`4C06` 只列 18 處而走訪有 75 處，密斯卓諾廚房那支 `SAVE 01 4C06` 就不在裡面。
**拿目錄下「沒有人寫這個旗標」的結論會反過來。**

⚠ `zhentil-keep.beholder-cave` 的 `script_block 0x22` **是對的**：那一段跑的時候
`CurrentBlockID()` 就是 `0x22`，而迪姆斯華特那場戲跑在 `0x21`。
**字串出現在哪個 block ≠ 那場戲屬於哪個 block**——一個 block 裝得下不只一場戲。
未解的是幾何怎麼換成 `GEO4/0x25`（`0x22` 的 `LOAD FILES` 是 `21`）。

劇情文字的串接有可重生的分母了：`cmd/ecl-text-coverage` 依**控制流走訪**列出
原作的每一頁，控制流可達的 1022 頁中 `unmatched` 為 0（spec 1110）。
⚠ 這個數字在上一版是 197 頁——`ON GOTO` 沒被走訪、四個變長指令的長度沒算、
頁的歸屬用 offset 順序，三者疊起來讓當時的 `unmatched=0` 什麼都不代表。
剩下 `variable-insert` 16 頁（頁裡印的是執行期的值，靜態驗不到）要靠實機路徑驗。

**overlay 的 far call 現在解得出目標了**（spec 1112）。IDA 把 overlay 從位址 0
載入，於是 `call far ptr 014D:00B6` 被顯示成同一份 `.bin` 裡的 `loc_1584+2`——
規格裡那一票 `<far 1590h>` 都是這樣來的假位址。真正的目標是常駐段的 TPOV 控制
記錄：段選 overlay、位移選 entry。`cmd/far-call-map` 把段位移常數量出來
（DOS `7Bh`、PC-98 `9Ah`），**5,079 個 DOS 呼叫點 0 個解不出來**，並重現三筆
先前手工解出的目標。這把「這個 far call 做什麼」從逐支硬讀變成查表。

存檔那一項也有數字了（spec 1115）：DOS 角色記錄 422 bytes **逐位元組**都有狀態
（`decoded` 299／`documented` 99／`unknown` 24；解析器實際讀到 291／422），
而 `decoded` 是**量出來的**——
改一個位元組、重新解析、比對結果，有差別就是有讀；掃程式碼會漏掉算出來的索引。
⚠ 這個數字衡量的是**看得懂多少**，不是**存得住多少**：匯入保留整份原始記錄，
未知位元組原樣寫回。同一輪替 `Fighter` 建了「每個匯出欄位都要能存進讀回」的
反射閘，當場抓到一個真的 bug——新增 `StatusPartyFled` 之後，逃離結局的存檔
存得下去、讀不回來。

戰鬥系統依此接上三塊：**逃跑**（走出邊界即脫離，比雙方移動率，平手 1d2；
spec 1112／1113）、**AI 移動**（行為模式 1..6 決定五個候選方向、半格成本、
走到射程才攻擊；spec 1114）、**法術主表**（原作 100 筆全部匯出，16 個位元組
逐欄有出處；spec 1111）。

**回合開始那一段整段對回原作了**（spec 1135–1138）。先攻是
`敏捷遠程命中調整 + 1d6` 夾底 1，`20` 是快速戰鬥的哨兵值；
`CHECKFX(07h)` 移到分派 AI／玩家選單之前，先前玩家控制的隊員完全套不到。
**突襲遮罩（`Area2+596h`）是死碼**：全 build 只有一讀一清 0，先攻的 `−6`
走不到，CoAB 傳 0 是有證據的值（spec 1136）。保留中的機會攻擊 `+18Dh^[7]`
的設定端是 GUARD，設定／消費／清除三段接起來。面向、動作計數、累計轉向
三個欄位與背後攻擊的三道條件是**字面移植**（spec 1138）：`+09h` 的幾何意義
兩個寫入端互相矛盾，不解釋；第二個 AC 的值用 `ArmorClassFacingKnown` 留白。
90° 扇形判斷（`INARC`，spec 1002）八個方向讀完並接上機會攻擊的 180° 面向閘。
第二個 AC 也解出來了（spec 1000 §七）：`+19Bh ＝ +19Ah − 敏捷防禦調整 −
盾牌那一槽 − 2`，81 筆 `MON*CHA` 有 78 筆殘差為 0，另外三筆的殘差 1、1、2
正好是 ＋1／＋2 盾牌。

**AC 與命中的刻度也統一了**（spec 1139）。原作把兩者都存成 `60 − 顯示值`，
命中判定是 `d20 ＋ 攻擊者^[199h] ＋ v >= 目標 AC`（儲存值越大越難打）；
remake 一律用顯示刻度，換算後是 `d20 ＋ 命中加值 ＋ 目標 AC >= 20`。
FIRE KNIFE 自己打自己原作要 18，remake 也是 18。原作搬過來的算式
（`CanHitTarget`、ECL 隊伍戰力、`CHECKFX` 的護甲寫入）在邊界換回儲存刻度。

這一批機制**已經搬進共用 engine**：`combat/facing`（八扇形、90° 扇形、轉向記帳、
背後攻擊三條件、開場面向、180° 掃描）、`combat/armorclass`（刻度換算與命中門檻）、
`combat/ability`（敏捷防禦調整）、`combat/footprint`、`combat/dispel`、
`combat/aiscan`、`viewport.Camera`。CoAB 這一側只剩轉接與作品資料。
盤點與剩下該搬的見 [engine 抽取盤點](docs/knowledge/engine-extraction-review.md)。

戰鬥規則再往下三塊。**戰術地圖的兩種障礙格**（spec 1119）：`far 193Eh+1` 用 far call
對照表解出來是 `overlay-32 entry#19`，它把腳印四格各查一次，先前記成抽象旗標的
`b`／`c` 其實是兩個地形碼 `1Eh`（豁免得過就走）與 `1Ch`（`有號(+0E5h) ≥ 7` 就走），
兩張豁免效果清單只有部分重疊。**士氣崩潰**（spec 831）分四段各 10%／50%／20%／20%，
逃走那一段掛效果 `23h`、把 `+18Dh^[10h]` 設 1、士氣補滿、清掉目標。
**自動換裝的評分算式**（spec 1120）讀完了，但物品類別表 `DS:5CF6h` 在常駐資料段
是整片 `FF`——它是 BSS，所以缺的不是算式而是那張表的來源。

法術這一側改成兩張表撐起來。**效果修正表**（spec 1123）：`CHECKFX(timing)` 給
「這個時機有哪些效果要介入」，handler 給數字——141 個 handler 機械分類之後，
祝福是士氣 ＋5、緩速是移動折半、抗寒是冷傷害折半（**有條件**，看傷害屬性旗標）。
**傷害骰表**（spec 1124）：`+0Ah = 0` 的那一批骰子在各自的 handler 裡，
從法術分派表 `DS:72A0h` 一路解到 `NdM`，治療輕／中／重就是 `1d8`／`2d8`／`3d8`。

★ 覆蓋台帳的「已判讀」用最嚴的定義：碼要在某個 timing 裡、那個 timing remake
**真的有查**、它動的那一格 remake **真的有讀**。分母也修過——戰鬥可施放的 79 支
裡有 6 支職業模型不支援（德魯伊、表裡沒有職業），可宣告的是 **73**。
逐支讀完之後**宣告數 73／73，可宣告的分母歸零**（spec 1125）；`handler`／`visual`／
`sound` 三欄都是 observed。剩下的不是接線缺口：13 筆占位玩家取不到、8 支只能紮營、
6 支職業模型不支援。

原版存檔匯入（spec 1121）走上編碼分流：`CHRDAT?.sav`／`.swg` 的版面同時被原版與
remake 自己的槽使用，光看檔案分不出來源，所以由呼叫端明講
（`LoadOriginalSAVGAMSlot`、CLI `-savgam-import`）。⚠ **英文樣本測不出這件事**，
兩條路在 ASCII 上結果完全相同。

## 第一人稱畫面已經有逐格的原版對照（第 572 輪）

提爾佛頓（`GEO2` 區塊 `01h`）**五格 × 四個朝向、共二十張**畫面與原版逐格比對：
十九張完全相同，一張差一格。比的是 88×88 可見區在 EGA 量化之後的 16 色索引
——DOSBox 與 remake 對同一個 EGA 顏色算出的 RGB 差幾個階，直接比 RGB 會有九成
的格子「不同」，把真正的差異埋掉。

修的是天花板：`gamepack` 把 `tilverton.first-person` 的 `indoor_sky_color` 宣告成 0
（→ `skyPalette[0]` ＝ 黑），原版的 runtime 值是 **9**（→ `skyPalette[1]` ＝ 白），
`outdoor_sky_color` 3 → **11**。★ 兩個數字取自原版**在地圖上**紮營存檔寫出來的
`Area1`；從**選單**存的那一份兩個都是 0——**同一支存檔常式，存的時機不同，
欄位意義不同**。

取 oracle 的方式也換掉了：原本是「猜一整串定時按鍵、等一輪、看結果」，
任何一步的載入時間漂移都會讓後面每一個鍵錯位。現在容器常駐、逐步送鍵、
每一步把畫面上的 8×8 文字格讀成字串再決定下一鍵
（`tools/dos-oracle-session.sh` ＋ `tools/dos_screen.py`），一步約一秒。

順帶確定的三件事：`CURSE.CFG` 是四行純文字（顯示介面卡／音效／存檔路徑／第四行），
留著它開機三題整個跳過；DOS 存檔 13,149 bytes 與 PC-98 13,214 是平台差異，
兩份規格各自對自己的平台；存檔裡的角色檔是 `CHRDAT<槽><序>.SAV` ＋ `.FX`，
**檔名欄指到不存在的檔案時原版不報錯、直接跳過那名角色**，八筆全跳過就是空隊伍
——畫面上看起來與「拒絕讀檔」一模一樣。

還開著的：其餘 17 張 `first_person` 地圖的天空色仍是宣告值。那兩個欄位**不是
純存檔狀態**（清成 0 之後進地圖會被重算回來），而改存檔的 `Current3DMapBlockID`
到不了別的地圖（同樣被重算），所以要拿別張地圖的值只能真的推劇情，或用
runtime watchpoint 找出誰寫它。見 [spec 1134](docs/spec/1134-original-first-person-oracle.md)。

其餘系統的逐項狀態一律查完整度矩陣，不在本檔重述。

## 已被推翻的斷言

一筆紀錄只有在**錯誤版本還讀得到**時才值得留：來源文字沒改乾淨、或那個錯誤
容易被下一個人重新推導出來。兩種情況都有更好的處置——

- 來源沒改乾淨 ⇒ 直接改寫來源正文成正確答案，不要靠這張表補救。
- 容易重新推導 ⇒ 寫成規則放進 [`AGENTS.md`](AGENTS.md) §3「容易重犯的反組譯與
  實作錯誤」，或在該規格正文標成「這裡容易擺錯」，並儘量加一條回歸測試擋住。

兩者都做完之後，紀錄本身就沒有讀者了。**目前沒有需要保留的條目。**
逐輪的經過寫在當月分冊，那是追溯用的檔案，不是現況。

## 尚未完成（摘要）

> ⚠ **這一段是指標，不是真相來源。** 手寫的「還沒做」沒有任何東西會在做完那一刻
> 更新它——完成的 commit 改的是程式碼與那一輪的紀錄，散在別處的現在式句子照樣
> 留著。第 664 輪抽查兩條就抓到兩條假的（手札 59 的原圖 renderer 早就做完、
> `+11Ch` 的讀法早就對完）。**要判斷還剩多少，看
> [`docs/audit/remake-status.md`](docs/audit/remake-status.md)**——它由
> `cmd/remake-status` 從既有的覆蓋率報表重生，每一列都附「這個數字證明不了什麼」。

量得到的部分：ECL 可達指令 `partial` 只剩 3 個 opcode／487 條、原作文字頁
unmatched 0、角色記錄 unknown 0、兩平台函式台帳待解讀 0、譯名不一致 0、
逐格實測 250 個索引裡演出來是中文 240／落回原文 0。

第一人稱 fidelity 也量得到了（spec 1185）：18,432 張畫面收斂成 585 種看得到的
牆面配置 ＋ 15 種牆型值，**已比過 31／585**，而且有一張照「一次蓋掉最多」排序
的擷取清單。第 669 輪補拍的二十張裡十八張與 remake 完全相同，兩張不同；第 670 輪用同簽章的
`(1,8)` 判定差異逐項重現，第 672 輪修好：成因是**符號編號跨段**（`0Eh` 的
WALLDEF 引用第 2 段的 `185`，而記錄掛第 3 段 ⇒ 算出負的項次 ⇒ 整格靜默消失）。
40 張參考畫面的不同取樣點 **97 → 1**（剩下的 1 個是 spec 1134 早記過的單格差異）。
第 673 輪補完提爾佛頓另外三格、12 張全綠，覆蓋率 **40／585**；第 675／677／683 輪用自製存檔把原版直接跳到指定格
（`tools/dos-oracle-jump-capture.sh`），覆蓋從 40 拉到 **249／585**（15 張圖、546 張擷取），結果按圖分開看：9 張圖幾乎完全相同（426 張），
但 `geo5-b33`／`b35`／`geo6-b40`／`b43` 共 0 張相符——已知一部分原因是
**選圖 15／18 任何 WALLDEF 檔都沒有**，remake 因此一面牆都不畫且不報錯（spec 1185）。
第 676 輪修好「載入原版存檔之後牆是空氣」（原版存檔沒有牆磚選圖，
改成照 ECL 段查表補；同一份存檔餵兩邊差 0 格）。
⚠ 跨圖仍取不到 oracle：原版的地圖由存檔裡的 ECL 狀態決定，改地圖編號不會換圖，
而 remake 那一側會認 → 兩邊指到不同的圖且都不報錯（spec 1185）。

**還沒有分母的才是主要缺口**：原機音訊的播放生命週期、
地形分派以外的走訪（世界地圖的查表分派、全部寶物、失敗與重訪分支、可達性）、
敵方 AI 與完整戰鬥規則、開場到結局的同一 session，以及三平台發行
（專案明文決定：完整可玩之前不做）。

⚠ 「沒有分母」不等於「快好了」，也不等於「還很遠」——它就是**還沒被量**。
這幾類要先有可重生的報表，剩餘工作才談得上估算。

## 歷史分冊

| 分冊 | 範圍 |
|---|---|
| [`10-architecture-and-boundaries.md`](docs/context/10-architecture-and-boundaries.md) | 獨立 engine／JSON game pack 的分工與早期確認事項 |
| [`20-rounds-through-347.md`](docs/context/20-rounds-through-347.md) | 第 1–347 輪工作序列與成果 |
| [`30-log-2026-07.md`](docs/context/30-log-2026-07.md) | 2026-07-28 至 07-31 |
| [`40-log-2026-08-01-03.md`](docs/context/40-log-2026-08-01-03.md) | 2026-08-01 至 08-03 |
| [`50-log-2026-08-09-13.md`](docs/context/50-log-2026-08-09-13.md) | 2026-08-09 至 08-13 |
| [`60-log-2026-08-14-18.md`](docs/context/60-log-2026-08-14-18.md) | 2026-08-17 至 08-18（第 565–572 輪）|

分冊只作追溯：裡面的「目前」「下一步」都是當時的狀態，不得當成現況引用。
新的輪次紀錄一律寫進當月分冊，本檔只更新現況段落。
