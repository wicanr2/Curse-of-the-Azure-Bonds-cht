# remake 目前量得到什麼

由 `cmd/remake-status` 從 `docs/audit/` 既有的報表併出來，不要手改。

⚠ **這不是完成度百分比。** 每個分母的意義都不同，平均起來只會得到一個沒有意義而且偏樂觀的數字。每一列的「證明不了什麼」跟數字一樣重要。

⚠ **「沒有數字」的那幾列才是重點。** 量得到的部分已經接近收斂，剩下的工作大多落在還沒有分母的類別上——那也是為什麼手寫的「尚未完成」清單會同時漏掉已完成的項目、又講不清楚剩下的工作有多大。

| 類別 | 目前量到 | 出處 | 這個數字證明不了什麼 |
|---|---|---|---|
| ECL 有序副作用與 external routine | 可達指令 14177 條：`done` 13690／`partial` 487；opcode `done` 58／`partial` 3 | `ecl-effect-coverage.json`（`cmd/ecl-effect-coverage`） | `done` 是「這個 opcode 的原作語意讀完並接上」，**不是**「玩家走得到的每一條都驗過」。可達性來自控制流走訪，跟不到的碼不在分母裡。 |
| 原作玩家可見文字接線 | 1022 頁：matched 1002、**unmatched 0** | `ecl-text-coverage.json`（`cmd/ecl-text-coverage`） | 「有一條 `text_rule` 命中」不等於譯文正確，也不等於那一頁的事件副作用已還原。 |
| 存檔相容（角色記錄） | 422 bytes：decoded 299／documented 123／unknown 0；remake 實際消費 291 | `dos-save-field-coverage.json`（`cmd/save-field-coverage`） | 欄位讀得懂不等於**往返**得回去。`MOVEPARTY` 跨遊戲轉移、角色刪除／改名尚未 round-trip。 |
| 反組譯覆蓋（兩平台全模組） | 2874 個函式：已解讀 2137／不阻塞 162／邊界碎片 575／**待解讀 0**；未被 IDA 認成程式碼的位元組 36386 | `coab-function-index.json`（`cmd/re-ledger`） | 狀態是**人工判定**寫在 `re-function-ledger.json` 裡的，工具只讀不寫。「已解讀」是有人讀過並留下 spec，不是自動驗證。「未被 IDA 認成程式碼」的那些位元組多半是資料，但**沒有被逐段判過**。 |
| 譯名一致性 | 詞條 138 條，未解決的不一致 **0** 筆 | `glossary.json`（`cmd/glossary-audit`） | 閘擋不住「誤用的寫法剛好是別的詞條的正確譯名」。那一半只能逐句對照原文。 |
| 玩家文字資料分離（Go 漢字 literal 債） | 合計 1000 次（`localization_debt` 0／`frontend_ui_debt` 15／`runtime_ui_debt` 985） | `go-han-literals-baseline.json`（`cmd/coab-audit`） | 這是**債務**不是進度：數字要往下降。分類只是遷移排序用的 heuristic。 |
| 戰鬥中可用物品（充能效果） | 充能物品 18 件，效果**沒接 0** 個 | `combat-item-use.md`（`cmd/item-use-audit`） | 「已接」是 remake 有那一支 handler 的行為讀法，不是逐件實機驗過。 |
| 每格事件實測（站上去演不演得出來） | 14 個有地形分派的 block、逐格試過 250 個索引：演出來是中文 240、**落回原文 0**、沒演出來 10 | `cell-sweep.md`（`cmd/cell-sweep`） | 量的是**內容與語系**不是可達性——盤點時隊伍被撐起來、隊伍是被放上去而不是走過去，所以「演得出來」不等於「正常隊伍走得到」。分母也只含**以地形碼分派**的 block；世界地圖那種 `GETTABLE` ＋ `ON GOTO` 的查表分派不在裡面，寶物、失敗與重訪分支也不在。 |
| 第一人稱 fidelity（幾何） | 18432 張畫面收斂成 585 種看得到的牆面配置（＋15 種牆型值），**已比過 249／585 種** | `fp-screen-plan.md`（`cmd/fp-screen-plan`） | ⚠ **15 張圖、546 張擷取**。結果要按圖分開看：`geo2` 三張＋`geo3-b10`／`b11`＋GEO4 三張＋`geo6-b42`共 426 張**幾乎全部相同**（提爾佛頓 1 格差異）；但 `geo5-b33`／`b35`／`geo6-b40`／`b43` 共 **0 張相符**，`geo5-b32` 11／20，`geo3-b15` 1／4。⚠ 找到一部分原因：**選圖 15 與 18 任何 WALLDEF 檔都沒有**，而 `ECL5/0x33`、`ECL5/0x35`、`ECL6/0x40`、`ECL6/0x45` 四段要它們 ⇒ remake 一面牆都不畫（天空地板正確、只有牆不見，而且**不報錯**）；第 686 輪找到原版的解法：`LOAD PIECES` handler 有**兩槽模式**（閘門兩格都非零時只載槽 1／3，槽 2 不動，spec 1087）⇒ 那兩塊從來不會被讀，不是資料缺了。已修三處（一槽載不到不再丟掉整組並印出來、載入存檔時還原存檔自己的牆面參數、自製存檔不寫該章沒有的選圖），`geo6-b40` 從差 1,827 降到 1,227。⚠ `geo6-b43` 與 `geo5-b32` 的選圖全都存在卻仍不符 ⇒ **還有第二個原因沒找到**。⚠ 比完 585 種也不等於第一人稱正確：天空色、牆磚選圖與調色盤跟著地圖宣告走，各自要比。⚠ 分母含全部 4,608 格（其中 187 格四面都是牆）：密室也會被玩家站上去，開新遊戲的起點就是一格密室。 |
| 敵方行為的輸入（怪物特殊能力） | 81 隻怪物、119 筆 `MON*SPC` 能力收斂成 52 個相異效果碼，**戰鬥規則會理 8 個**，真的缺 0 個 | `monster-ai-coverage.md`（`cmd/monster-ai-coverage`） | ⚠ 量的是**決策層的輸入齊不齊**，不是「AI 打得像不像」——後者要對照原版實機錄影，這裡不宣稱。⚠ **「不理它」有五種意思，混在一起數會得到誤導的待辦**（第 688 輪先報成 45 個、第 689 輪 9 個，都是混出來的）：`inert` 1 碼（原作那一支就什麼都沒做）、**只出現在死時機 9 碼／14 筆**（時機 `02h`／`03h` 全執行檔沒有任何呼叫端，`cmd/checkfx-callsites` 三個方向都排除過且有正對照 ⇒ **原作永遠不會執行，remake 不做才是對的**）、修正表裡還沒有這個碼 5 碼／7 筆、`unread` 29 碼／71 筆，**真的缺口已經是 0**（第 692 輪接上 `54h`：`+1A4h` ＝ 目前 HP，傷害屬性帶電時回血 8 點）。⚠ 接的過程抓到一個既有 bug：`CHECKFX` 的守衛檢查排在記錄寫入**後面**，所以**有條件的記錄寫入完全繞過自己的條件**——不管什麼屬性都會回血；是負對照測試抓到的。★ 另一個發現：全部 81 隻怪物的記憶法術槽（`+33h..+6Ah`）**逐位元組都是 0** ⇒ `AIChooseSpell` 那條路在 CoAB **一次都不會被觸發**；規則是對的，但拿本作當樣本驗它只會得到一個永遠全綠而什麼都沒驗到的測試。 |
| ECL 查表分派（`ON GOTO`／`GETTABLE`） | 23 個 ECL 段裡有 185 處分派點、1071 個分支目標（相異 **835** 個），另有 151 處 `GETTABLE` 取值 | `ecl-table-dispatch.md`（`cmd/ecl-table-dispatch`） | ⚠ 這是**分母不是覆蓋率**：它說的是「有幾條路」，不是「走過幾條」。要知道玩家實際走到幾條分支得有實跑資料，這一支不宣稱。把分支數當成完成度會把沒跑過的路算成跑過。⚠ 只數靜態追得到的分派點（`TraceGraph` 走得到的），所以這個分母是**下界**。⚠ 相異目標才是真正的分母：同一個目標被兩個索引指到只是一條路。 |
| 原機音訊（音樂／音效生命週期） | **沒有數字** | — | PC-98 音樂與音效的 producer 已有規格與 `cmd/pc98-music-audit`／`cmd/pc98-sfx-audit` 驗本機橋接檔，但**播放生命週期與戰鬥 phase 同步**沒有覆蓋率報表——那是「什麼時候該響」的問題，不是「檔案對不對」。 |
| 開場到結局的同一 session | **沒有數字** | — | 端到端測試跑得到結局，但那是**測試路徑**；真人從開場玩到結局沒有自動化的證明。 |
| 三平台發行 | **沒有數字** | — | 專案明文決定：遊戲完整可玩之前不花時間做（`WORKLIST.md` P2）。 |
| 全城市／全房間走訪（地形分派以外的部分） | **沒有數字** | — | 以地形碼分派的每格事件**已經有分母**（見上一列）。查表分派也有了（`ecl-table-dispatch.md`：185 處分派點、835 個相異分支目標）。**還沒有分母的是**：全部寶物、失敗與重訪分支，以及「正常隊伍走不走得到」這個可達性問題——後者要有實跑資料，靜態盤點答不了。 |
