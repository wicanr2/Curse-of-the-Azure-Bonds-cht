# 逐輪紀錄：2026-08-17 至 2026-08-18（第 565–572 輪）

本檔只作追溯。裡面的「目前」「下一步」都是當時的狀態，不得當成現況引用；
現況一律看 [`CONTEXT.md`](../../CONTEXT.md)。

## 2026-08-17（第 565–568 輪）：法術覆蓋的分母歸零

戰鬥可施放的 79 支法術裡有 6 支職業模型不支援（德魯伊，表裡沒有職業 3），
可宣告的分母是 **73**。最後 23 支逐支讀完之後 **73／73**，
`handler`／`visual`／`sound` 三欄都是 observed（spec 1125）。

三個判讀上會反過來咬人的點：

- **解除魔法**的對抗是「相同 50%、高出去每級 ＋5、低下去每級 −2」——
  兩個方向的係數不一樣。
- **移除詛咒**先拿掉效果 `24h`；拿掉了就結束，沒拿掉才解開第一件詛咒裝備的
  **已裝備**旗標（詛咒本身還在）。
- **疾病**的 `2Bh`／`2Ch` 是**效果 `22h` 的 handler** 掛的，不是法術掛的，
  所以規則要寫在效果那一側。

覆蓋稽核本身也修過：先前只讀 `combat_state.go` 又靠函式名猜 behavior，
把 48 支的 sound 讀成沒有。**那是量測的洞，不是覆蓋的洞**——改成掃整個
`internal/game` 並跟著分派表找真正的施法函式之後 sound 73／73 observed。

演出那一側（spec 1126）：原作不是「一支法術一套圖」。整個遊戲只用三個槽
（18 共用施法投射物、19 閃電電弧、23 魔法命中），而**共用投射物在 `CASTSPELL`
裡、分派到 handler 之前就播**，所以每一支法術都有演出。`槽 = COMSPR 區塊 + 13`。

## 2026-08-18（第 569–571 輪）：README 五張圖背後的四類對齊錯誤

使用者逐張指出 README 圖不對。量下去是同一類問題：**兩層各自算座標，然後對不上**
（spec 1130）。

| 症狀 | 真正的原因 |
|---|---|
| 黃框裡的人像沒佔滿 | sub-image 當繪製目的地時**只裁切**，座標仍是母圖的；呼叫端多扣了一次 `clip.Min` |
| 人站在綠色灌木上 | 地形層沒過相機與鏡像，與戰鬥員走的不是同一條座標路徑 |
| 人站在牆裡、四隻蜘蛛全不見 | 同上 ＋ 敵方縱帶 `7..9` 落在七欄視野外 ＋ 佈陣不看地面 |
| 3D 組成可疑 | 幾何與 spec 1006 逐項相符；問題是**一大半牆磚根本沒畫出來** |

最後一格拉出 spec 1131：`Put8x8Symbol` 把符號編號切成五段，
**第 0 段（`1..45`）是全遊戲共用的一份，先前完全沒載**。它就是 `8X8D1.DAX`
的 `0CBh`——六個 `8X8D*.DAX` 裡唯一一個 45 項的區塊，內容是天空格與斜邊收邊磚。
而且 **EGA index 13（洋紅）是透明鍵不是顏色**，不遮罩會在牆角畫出桃紅三角形。

順帶抓到兩件事：checkpoint 開戰時**完全沒有地形**（三支戰鬥地形投影裝在
`RunGame` 前一行，而確定性 checkpoint 在那之前就已經 `StartCombat`），
以及戰鬥地圖與畫面可以是**兩份**（spec 1132：戰鬥地圖由地城座標生成，
劇情流程會在開戰前移動隊伍，而地圖還是啟動時生的那一份）。

## 2026-08-18（第 572 輪）：第一人稱畫面與原版逐格比對

提爾佛頓五格 × 四個朝向、共二十張畫面與原版逐格比對：**十九張完全相同，
一張差一格**（spec 1134）。修的是天花板——`indoor_sky_color` 宣告成 0（黑），
原版的 runtime 值是 9（白）。

★ 那兩個數字取自原版**在地圖上**紮營存檔寫出來的 `Area1`。從**選單**存的那一份
兩個都是 0：**同一支存檔常式，存的時機不同，欄位意義不同**。

取 oracle 的方式從「猜一整串定時按鍵、等一輪、看結果」換成容器常駐、逐步送鍵、
每一步把畫面上的 8×8 文字格讀成字串再決定下一鍵。先前那條路已經燒掉約 50 輪
擷取，而錯位的鍵可能剛好按到 `EXIT TO DOS`。

過程中被誤讀的一個症狀值得記著：合成存檔載入後「按下槽的字母就回主選單」
看起來像原版拒絕讀檔，實際上是**隊伍載入為空**——檔名欄指到不存在的檔案時
原版不報錯、直接跳過那名角色。**沒有錯誤訊息的失敗，症狀會長得像另一個問題。**

天空色的來源查到一半：那兩個欄位每次進地圖都會重算（清成 0 會變回來），
而改存檔的 `Current3DMapBlockID` 到不了別的地圖（同樣被重算）。靜態掃描在
全 overlay 找不到寫入，但 **disp16 掃描結構上抓不到指標間接寫入**，
所以那只能當弱證據；下一步要用 runtime watchpoint。

## 2026-08-18（第 573–576 輪）：回合開始、突襲遮罩、面向、扇形

回合開始那一段逐項對回原作（spec 1135）。先攻是
`敏捷遠程命中調整(overlay-24 entry#11) + 1d6`，夾底 1；`20` 從擲骰擲不出來
（上限 11），它是快速戰鬥的哨兵值，回合開始再夾成 19。

**修掉一個玩家看得到的 bug**：`CHECKFX(07h)`（「這個人這一回合動得了嗎」）
原本只有 AI 那一側走得到，玩家控制的隊員完全套不到——纏繞術那一族的
效果 `88h` 對隊員沒有作用。原作是在**分派到 AI 或玩家選單之前**就呼叫。
負對照拿隊員測（拿敵人測會過，什麼都證明不了）。

**突襲遮罩（`Area2+596h`）是死碼**（spec 1136）：整個 DOS 與 PC-98 build 裡
只有先攻讀它一次、戰鬥主迴圈清 0 一次，沒有任何地方寫進非 0。所以先攻的
`−6` 那一支走不到，CoAB 一律傳 0 是有證據的值，不是預設值。三重補強：
正對照（同一份掃描抓得到別的欄位的寫入）、指標間接掃描、PC-98 第二證人。

`+18Dh^[7]`（保留中的機會攻擊）的**設定端是 GUARD**（`overlay-24 entry#35`，
印字串 `Guarding`），設定／消費（spec 817）／清除（回合開始）三段接起來。

面向那一格（spec 1137／1138）踩到一個普查方法的洞：`es:[暫存器+位移]` 的掃描
**認不出這個遠指標指向哪一種記錄**，所以「七處分支」裡有兩處根本不是戰鬥狀態
（一處是角色記錄的智力基底、一處是 LOS 的 Bresenham 狀態）。普查要先看到
`les …, es:[…+18Dh]` 才算數。

`+09h` 的幾何意義**兩個寫入端互相矛盾**（攻擊動作寫 `(d+4) mod 8`，轉向記帳
比較時沒有那個 `+4`），所以只做字面移植，不解釋欄位。第二個 AC 欄位 `+19Bh`
的算法還沒讀，用 `ArmorClassFacingKnown` 擋住——機制接上、條件測得到、數值留白。

90° 扇形判斷（`overlay-31:054Ah`，PC-98 符號名 **`INARC`**）八個方向逐條讀完
（spec 1002）。八段是**同一個形狀**：兩支半平面判斷取聯集，程式長度都是
0x55 bytes；正交向的兩支互為鏡像，斜向的兩支是被對角線切開的兩個 45° 楔形。
接上 spec 1010（PC-98 符號名 `CHECKPARTINGBLOWS`）的機會攻擊面向閘：
朝向 −2..＋2 五個方向任一成立就打得到，先攻未歸零或這一輪還沒動過時整段跳過。

★ **錐的頂點是起點往前一格，不是起點**。拿起點當頂點推，正西方的目標會算成
「三個方向都收得到」，實際上只有正西那一支收得到。

## 2026-08-18（第 577 輪）：第二個 AC 解出來，順帶量到刻度不一致

`+19Bh` 的算式在派生數值重算的收尾（spec 1000 §七）。關鍵是 `var_D` 是
`[bp−0Dh]` 起的**五個位元組**，也就是 `var_D`／`var_C`／`var_B`／`var_A`／`var_9`
這五格；`+19Ah` 是五格全加，`+19Bh` 只加其中三格再減 2。相減得到

> `+19Bh ＝ +19Ah − 敏捷防禦調整 − 盾牌那一槽 − 2`

——AD&D 1e 的背後攻擊正是抵銷盾牌與敏捷加值並給 ＋2 命中。

拿原作資料驗過：六章 `MON*CHA` 共 81 筆，78 筆殘差 0，另外三筆是 1、1、2
（DARK ELF LORD 兩筆、TYRANTHRAXUS）——正好是 ＋1／＋2 盾牌。
**盾牌那一槽的加值靜態讀不到（表在 BSS），但殘差把它量出來了。**

★ 投影到 `Fighter` 搬的是**差值**不是絕對值：`monster.CombatArmorClass` 會把
儲存值反轉成顯示值，而命中判定是 `attackTotal >= AC`（數字小才好打），
直接搬 `+19Bh` 會讓背後攻擊變成更難打——而且兩種寫法在「有沒有換到第二個 AC」
上看起來一模一樣。

順著這條線量到一件更大的事：**AC 與命中的刻度在三個地方不一致**。
真實怪物匯進來是 AC 1..7、命中加值 41..53，`d20 ＋ 41 >= 1` 永遠命中；
隊員那一側是 AD&D 降序的 AC 配升序的判定，敏捷越高反而越好打。
原作的對照是 FIRE KNIFE 儲存命中 41、儲存 AC 59 ⇒ 需要 d20 ≥ 18
（畫面 THAC0 19／AC 1）。這一項列進 WORKLIST 沒有動——統一刻度會改到
隊員模型、怪物匯入、效果修正與整批戰鬥測試。

## 2026-08-20（第 578 輪）：AC 與命中的刻度統一

先把證據看齊再動手。`ATTEMPTTOHIT`（DOS `overlay-23:123Fh`）65 條逐條讀完，
宣告順序是**兩個遠指標在前、門檻在後**：加進骰值的是**攻擊者**的 `+199h`，
比較的門檻是傳進來的那個 byte ＝ 防禦者的 AC。`sub_14E8` 挑好三段 AC 之一
就是送進這裡當門檻。

> `d20 ＋ 攻擊者^[199h] ＋ v >= 目標 AC`（儲存值越大越難打）

remake 統一用**顯示刻度**（AD&D 習慣：AC 越小越難打）。兩邊各減一個常數之後
命中式是 `d20 ＋ 命中加值 ＋ 目標 AC >= 20`——同一個函式。
FIRE KNIFE（`+199h ＝ 41` ⇒ THAC0 19、`+19Ah ＝ 59` ⇒ AC 1）自己打自己
原作要 18，remake 也是 18。

選顯示刻度而不是儲存刻度的理由是量出來的：`ArmorClass` 在程式碼裡出現 430 次，
其中 367 次在測試，而那些測試寫的是 AD&D 值（`AC 10 ＋ 命中加值 20` 這種
「一定打得中」的組合）。改成儲存刻度會讓那組數字變成「一定打不中」，
整批測試要重寫；改判定式則一條都不用改。**修完全套測試只紅了自己新加的兩條。**

順著這條線，程式碼裡原本就寫成降序意圖的地方一次全部變正確：隊員的
`10 − (敏捷 − 10) / 2`、`ArmorClass -= 護甲改善`、除錯用的 `ArmorClass = −10`
（不死身）——它們本來都是反的，現在都對了。**這是「選對域」的旁證**。

換算點四處：怪物匯入、`CanHitTarget` 的呼叫端、ECL 隊伍戰力
（`AC > 60`、`命中 > 39` 才算分，一看就是儲存刻度）、`CHECKFX` 的護甲記錄寫入
（`add_capped 2 cap 60`，上限 60 就是儲存值的 AC 0）。

★ 第二個護甲欄位的換算**由第一格決定**：`+19Bh` 系統性地比 `+19Ah` 小 2..8，
會掉出 `CombatArmorClass` 的 `50..70` 視窗——各判各的會變成一格換、一格沒換，
而症狀只是「背後攻擊的 AC 有點怪」。

## 2026-08-20（第 579 輪）：機會攻擊的四道閘、隊員的命中與 AC 表

三項「還開著」的收尾，其中一項根本不是開著的。

**`v` 早就接好了。** 命中式裡那個由攻擊者隊號選的修正是 bank 1 的
`+6E0h`／`+6E2h`，而 bank 1 的映射是 `(ECL 位址 − 7C00h) × 2` ——換算回去就是
ECL 的 `7F70h`（敵方側）與 `7F71h`（隊伍側），劇情用 `SAVE` 寫進去，
remake 接在 `SetSideAttackRollModifier`（spec 390）。**把兩份既有結論放在一起
就有答案，不必再查。**

**機會攻擊的四道閘**：三道靠既有資料解得出來，一道要讀 DOS 側的原始位址。
`overlay-24 entry#6` 的四個效果碼在台帳裡記的是 `49D3h`——那是 PC-98 的位址，
DOS 側是 `27C9h`，而且落在 BSS 界線之前，直接讀得出 `33h 34h 35h 1Fh`。
那四個就是 `CHECKFX(07h)`（這一回合動得了嗎）的成員。
`sub_1144` 的兩個 far call 解出來都是 `CHECKFX`，而時機 `00h` 在 CoAB 是空的、
`01h` 全是可見性那一組 ⇒ 整道閘就是「看得見離場的人嗎」。

★ **子集不是同一組**：那四個碼是 `MonsterIsHeld` 五個碼的子集，少一個 `1Bh`。
借用現成的判斷會多擋一個效果，而症狀只是「某些情況下沒有機會攻擊」。

**隊員的命中與 AC**：`+73h` 的算法在 TRAINING（`overlay-17:0782h`）——八個職業槽
各查 `DS:3E38h`（每槽 13 bytes，索引是等級）取最大值。表在資料段讀得出來，
逐格對回 AD&D 1e 的三組攻擊進程；職業槽的名字來自 game pack 既有的
`class_slots`，兩者對上是**獨立的交叉驗證**。建角另有兩個預設值
`+124h = 32h`（AC 10）與 `+73h = 28h`（THAC0 20）。

★ 一個讀錯的分支被測試當場抓到：空槽那條 `jle` 跳到的是迴圈**尾巴**不是出口，
所以是 continue 不是 break。聖騎士只有槽 3 有等級，讀成 break 會在槽 0 就結束。

## 2026-08-20（第 580 輪）：七個戰鬥契約搬進共用 engine

判準三條：不吃作品資料、介面是 plain value、證人數量講清楚。

搬過去的是 `combat/facing`（八扇形方向分類、最短轉法、90° 扇形、九格位移表、
轉向記帳、攻擊時的面向寫入、回合開始清計數、背後攻擊三條件、開場面向、
180° 五方向掃描）、`combat/armorclass`（`60 − 顯示值` 的雙向換算與命中門檻、
背後那一格的 `Rear`）、`combat/ability`（敏捷防禦調整表）、`combat/footprint`、
`combat/dispel`、`combat/aiscan`、`viewport.Camera`。

CoAB 這一側只剩轉接與作品資料：`Fighter` ↔ `facing.State`、體型碼對照、
開場面向表 `{7,2,3,6}`、AI 的起始門檻 7 與 1d7、以及機會攻擊前面那幾道
作品專屬的閘。

★ engine 的測試在寫的時候當場抓到一個文件錯誤：`AccountTurn` 問分類器的是
「**這個戰鬥員**在對方眼中位於哪個方向」，不是「朝對方的方位」——兩者相反，
而且搞反了數字看起來一樣合理。程式是對的，是註解寫反了。

跨 repo 的流程：engine 側 commit → `tools/engine-proxy.sh` → CoAB
`tools/go.sh get <印出來的版本>`。⚠ engine repo 的 push 需要另外授權，
沒 push 之前 `go.sum` 指到的 commit 只存在本機。

## 2026-08-20（第 581 輪）：力量的命中與傷害調整，以及一份原版存檔的 oracle

spec 694／697 早就把三張表讀完了，這一輪是把它們接上——表放進 engine 的
`combat/ability`（與敏捷防禦調整同一個 package），CoAB 只留 `+125h` 那道閘。

★ **`+125h` 不是「玩家角色」的同義詞**：建角寫 1、全 build 沒有任何地方寫 0，
但六章 `MON*CHA` 的 81 筆記錄裡 **61 筆是 0、20 筆是 1**。怪物那一側兩種都有。

★ **傷害表的索引 19 是單獨一格（＋3）**，不是 `i − 17` 那一段的一部分。
寫成一段會讓 18/01–50 的角色少一點傷害加值——engine 的表格測試當場抓到。

最有價值的一件事是找到 oracle：**原版產生的角色檔把輸入與算出來的結果同時
存了下來**。`CHRDATA1.sav`（戰士 5 級、力量 18/18、敏捷 15、沒有裝備）：

| 欄位 | 記錄 | 重算 |
|---|---:|---|
| `+73h` | 44 | 職業命中表 slot 2 等級 5 |
| `+199h` | 45 | `44 ＋ 力量命中 1`（索引 19） |
| `+1A2h` | 3 | 力量傷害（索引 19） |
| `+19Ah` | 51 | `+124h 50 ＋ 敏捷防禦 1` |
| `+19Bh` | 48 | `51 − 1 − 0 − 2` |

五格全中。一條測試同時釘住四張表與第二個 AC 的算式——任何一張改壞都會紅。

## 2026-08-20（第 582 輪）：讓乾淨 clone 建得起來

兩個東西都不在 CoAB 的版控裡——`golden-box-remake-engine/`（獨立 repo）與
`workplace/`（工作目錄，檔案型 proxy 放在裡面）。所以**剛 clone 完的 CoAB
既沒有 engine 原始碼、也沒有 proxy**，而 engine 是私有 repo，
`proxy.golang.org` 取不到、容器裡又不該放 GitHub 憑證。

`tools/engine-bootstrap.sh` 補上這一段：從 `go.mod` 讀出鎖住的 pseudo-version、
取尾巴那 12 碼當 commit、需要時 clone 或 fetch engine，再用那個 commit 重建
proxy，最後核對打包出來的版本與 `go.mod` 一致。

★ **不 checkout、不動 engine 的工作區**——`git archive <commit>` 不需要工作區
配合，硬要 checkout 只會踩掉開發者自己在 engine 上的改動。

順帶放寬 `engine-proxy.sh` 的髒工作區檢查：那道檢查只在**沒有指定 commit**
（要打包 HEAD）時才有意義，明確指定 commit 時打包內容毫無歧義。不放寬的話，
engine 有本地改動的人就 bootstrap 不了 CoAB。

另外修好 CoAB 那份 git 目錄的分支設定：`remote.origin.fetch` 是空的，
所以從來沒有 `origin/main` 這個 remote-tracking ref，`git status` 也就看不出
領先／落後幾個 commit。補上 refspec 之後才確認得了「8 個在前、0 個在後」
是乾淨快轉。

## 2026-08-20（第 583 輪）：`SEG-01`——block 怎麼換，以及一個假零

主線改用分段驗證，前提是每個 block 的進出邊都說得出來。開工前的假設是
「世界地圖進到各章走的不是 `NEWECL`」。**那個假設是量錯造成的。**

機制本身很單純（spec 1141）：主迴圈 `overlay-02:3772` 讀 `bank0 +1E4h`
（LastECL）決定載哪一個 block，載完寫回去；`20h NEWECL` 就是改那一格。
新遊戲 `LastECL = 0` ⇒ block `1`（`DS:4FBCh ≠ 0` 時是開場 `0x52`）。

★ **`SAVE <自己的編號> → 4BF2h` 不是轉移**，是「記錄我是誰」。六個 block 都這樣寫。

★ **事件目錄的轉移圖是假零**：它的可達性不跟 `ON GOTO`／`ON GOSUB` 的目的地
（它自己的「限制」寫了，但那句話沒被讀進計畫）。差距是 21 條邊對 **47** 條、
9 個「沒有出邊」對 **2** 個，而且**兩個世界地圖 hub 看起來一條出邊都沒有**——
它們是整條主線的樞紐。同一個 block，事件目錄看到 85 條指令，跟 `ON GOTO`
之後是 726 條。

處置：`cmd/ecl-block-graph` 用 `ecl.TraceGraph` 重算並產生
`docs/audit/ecl-block-graph.md`，回歸測試特別檢查兩個 hub 的出邊 ≥ 5。

★ 順帶翻掉一個猜測：`+342h`（City）**不是拿來載區域的**。推導鏈原本是
「共用格子表列出 `overlay-10:0FC7` 存取它」→「所以 City 驅動載入區域」，
中間那一步沒有證據。讀了那支才發現它接的是三支**野外戰場地形生成**（spec 749）。

## 2026-08-20（第 584 輪）：`SEG-02`——段落清單接起來了

對照關係比預期單純：**`LOAD FILES` 的第一個運算元就是那一段要載的地圖區塊**，
第二、三個固定 `02`／`FF`；世界地圖 hub 與開場是 `7F/7F/7F`（不載 3D 地圖）。
game pack 那一側 **`area_id` 就是 ECL 成員編號、`script_block` 就是 block 編號**，
兩邊 join 得起來。

多數 block 載與自己同號的地圖。四個例外（`0x02`→`01`、`0x22`→`21`、`0x31`→`32`）
與四個「沒有 `LOAD FILES`、沿用上一段」（`0x12`、`0x21`、`0x23`、`0x30`）。

★ `ECL3/0x12` 沒有 `LOAD FILES`、只與 `0x11` 互相往返，而 game pack 早就把它
宣告成 `block-11-level-2`——**同一張地圖的第二層**。兩邊互相印證。

★ 開場 `0x52` 在 `NEWECL` 圖裡孤立，**那不是漏算**：它由引擎選中
（`DS:4FBCh ≠ 0`），不是被 `NEWECL` 指過去的。測試把它排除並寫明理由。

⚠ 一組宣告與原作資料不一致：`zhentil-keep.beholder-cave` 宣告 `script 0x22`
＋ `geometry 0x25`，但 `ECL4/0x22` 載的是 `21`、`ECL4/0x25` 才載 `25`。
兩者只能有一個對，要回原始 bytes 或實機判。

## 2026-08-20（第 585 輪）：`SEG-03`——段的 id 不綁地圖名

計畫原本寫「佔位地圖名字沒定，段的 id 也定不了」。**那個相依是自己加的。**
段的 id 一律 `ECL{成員}/0x{block}`，機械且穩定；人類可讀的名字是**標籤**，
放 `docs/plan/segment-labels.json`。順帶省掉一次大搬風——`original.geo*`
那組佔位 id 在程式、測試、game pack 與歷史文件裡有 42 處引用。

標籤逐條有原作敘述為證（`cmd/ecl-block-text`）：ECL3 是猶拉什
（`WAR BLASTED SECTION OF THE CITY`、`SEAL OF ZHENTIL`），ECL5 是哈普村／
古熔岩洞／巫師塔（`ANCIENT LAVA TUBE`、`FIVE STORY TOWER`），
ECL4 的五段是內城／神殿牢房／迪姆斯華特／法庭／眼魔洞穴。

⚠ 文字證據把 `SEG-02` 標的那個不一致講清楚了：**眼魔的內容在 `ECL4/0x25`**
（`THE MULMASTER BEHOLDER CORPS`），而 `ECL4/0x22` 是迪姆斯華特那一段
（`DIMSWART THE SAGE`，`LOAD FILES` 是神殿那張 `21`）。
`zhentil-keep.beholder-cave` 的 `script_block 0x22` 指到的是別段的劇本。
這一輪不改——現行 checkpoint 走得通，改欄位是行為變更，留給拆段時驗。

★ 又量錯一次：第一版撈地名用 `[A-Z][a-z]{2,}`（首字大寫），**十個 block 全部
回空**。原作敘述**整段都是大寫**。正對照（拿已知有文字的 `ECL2/0x03` 跑同一支）
當場戳破：261 條候選，第三條就是敘述。工具檔頭把這件事寫成警告。

## 2026-08-20（第 586 輪）：`SEG-04`——25 段都進得去了

段的入口資料集中到 `internal/segment`：block 編號、直接進入要把 `LastECL`
設成誰、章節、在地城還是世界地圖、以及對應的既有旗標。`-segment <id>` 收三種
寫法（完整 id、只給 block、既有旗標名），`-segment list` 列全部。進入的動作是
`State.StartStorySegment`（切 block → 設 `LastECL` → 跑 initial lifecycle），
既有的 `StartDungeonStoryPreview` 變成它的地城特例。

`EnterFrom` 不准憑印象填：`TestEnterFromIsARealIncomingEdge` 逼它是轉移圖上真的
存在的一條邊。`0x00` 不是某個 block，是「全新開局」——spec 1141 的主迴圈只在
`LastECL ＝ 0` 時走到開場與提爾佛頓第一段，那兩段在圖上本來就沒有進入邊。

`ECL5/0x30` 進去之後停在 `0x50`：72 條可達指令、唯一出邊是 `0x50`，
是純過場 block。註冊表用 `SettlesAt` 記這件事。

★ 把「這一段的第一個畫面」單獨拉出來看，戳破兩個素材鍵錯誤。
**大圖的檔案不該跟著章節走**：原作只有 `BIGPIC1/2/6`、一共四個區塊且互不重複
（`79`／`7B` 在 1、`78` 在 2、`7A` 在 6），第 3～5 章根本沒有 BIGPIC 檔，而世界
地圖那張 `79` 是全遊戲共用的——`ECL5/0x30` 於是去要不存在的 `bigpic5-block-79`。
改成用區塊編號定檔。**世界地圖段也要有章節編號**：`StartStorySegment` 原本只在
地城分支設 `Area.GameArea`，開場於是拿著章節 2 去要 `pic2-block-1D`，而實際存在
的是已匯出的 `pic1-block-1D`。

★ 順帶跑出原作序幕：正常新遊戲走 `0x50 → 0x01`（`BeginAdventure` 直接 reset 到
`0x01`），**沒有經過開場 block `0x52`**。`-segment ECL1/0x52` 是目前唯一進得去的
路徑——營火伏擊圖、三名 NPC 入隊、五枚青色符印的緣起。截圖進 README。

剩一個真的素材缺口：`ECL4/0x22` 要 `character-area-4-head-25-body-23`，
頭與身體是不同區塊的組合，而匯出腳本目前只出同號的那些。

## 2026-08-20（第 587 輪）：`SEG-11` 前半——段落快照往返得回來了

計畫自己寫著「`SEG-11` 是關鍵：沒有快照交接，分段就只是把同一條長跑切成看起來
比較短的樣子」，所以把它排到 `SEG-10` 前面做——先驗交接機制，拆段時才有東西
可以接。

`TestSegmentEntrySnapshotRoundTrips` 對 25 段各做一次「進段 → 存 → 用新的 State
讀 → 比對」。比對的是段的邊界狀態：block、Mode、章節／地城、GEO 檔集與區塊、
地城座標與朝向、隊伍人數。

★ 三個世界地圖段一開始往返不回來，而原因不在存檔在寫入端：**存檔沒有獨立的
GEO 檔集欄位**，`LoadPartyFile` 是拿 `Area.GameArea` 重建它的。所以
`GeoMapSet` 與 `Area.GameArea` 留下不同的值，快照一定回不來。
`StartStorySegment` 原本只在地城分支設 `GeoMapSet`，現在兩個分支都設。

交接也實際跑過：`-segment-snapshot` 進段之後把存檔寫出來就結束，
`-party-load` ＋ `-segment` 接下一段。巫師塔的快照接進 `ECL5/0x30`，
過場之後停在世界地圖 hub，畫面是「你們來到提爾佛頓城外，要進城，還是繼續旅程？」
——與那一段唯一的出邊 `0x50` 一致。

段**結束**時的快照要等每段有自己的測試（`SEG-10`）之後才存得到；本輪存的
是段的入口。

## 2026-08-20（第 588 輪）：`SEG-12`——47 條邊逐條接得起來

`TestEveryNewECLEdgeHandsOff`：來源段存一份快照 → 用新的 `State` 讀回 →
**帶著來源 block 當 `LastECL`** 進目的段（spec 1141 的轉移機制本身）→
驗目的段的 initial lifecycle 跑得完、停在宣告的 block、隊伍跟得過來、
所在層級與宣告一致。47 條邊 47 個子測試，全綠。

邊的清單讀 `docs/audit/ecl-block-graph.md` 的段落清單，條數對不上 47 就直接紅。

反向對照跑過：把目的 block 換成不存在的 `0xEE`，第一條邊就紅——這條測試會失敗，
不是永遠綠的裝飾。

⚠ 來源用的是**段的入口**狀態，不是段真的走完的結束狀態。所以它擋得住「這條邊
接不起來」，擋不住「照劇情走到這裡會如何」——後者要等 `SEG-10`。

## 2026-08-20（第 589 輪）：25 段的接線盤點，推翻計畫裡三條印象分類

`cmd/segment-coverage` 對每一段直接進入，量入口畫面出了什麼、入口文字與選項是
中文還是落回原文、從入口再走一步會到哪裡。結果：**22 段的入口有中文劇情、
3 段的入口不出文字、0 段落回原文**。

計畫階段 2 原本按印象分三類，量過之後三條都不成立：

- 「9 張 `original.geoN.block-NN` 地圖走得動、事件沒接」——那 9 段的入口全部出
  中文劇情文字。
- 「ECL6 那三個沒有出邊」——只有 `0x43` 沒有；`0x40 → 0x42`／`0x50`、
  `0x42 → 0x40`／`0x43`。
- 「其餘 block 完全沒接」——沒有任何一段的入口落回原文。

正文已改寫成量出來的版本。

★ 第一版盤點裡 `ECL2/0x02`、`ECL3/0x12`、`ECL4/0x21` 顯示的是**前一段的文字**
——`StartStorySegment` 沒清掉上一段留下的訊息與選單，於是不出文字的段看起來
像有接。**入口沿用前一段的字，比沒有字更難發現**：盤點工具要先能區分
「這一段出了字」與「畫面上有字」。清掉之後這三段才現形，而它們是從別段被帶
進來的段落（神殿牢房、城區共用地圖、地下第二層），劇情在前一段的轉移與每回合／
搜尋生命週期裡，不在 `initial`。

判準本身有正反對照（`TestLanguageOfSeparatesHanFromOriginalText`），
盤點結果有回歸閘（`TestSegmentEntryTextIsLocalized`：一段都不准落回原文，
不出文字的那三段多一段少一段都紅）。

## 2026-08-20（第 590 輪）：整條主線跑拆成 12 個段測試

`TestRealNewGameRunsToTheEnding` 原本是一個 790 行的函式。
同一條 session 拆成 12 個 subtest（哈普村、古熔岩洞＋巫師塔、兩段世界路線、
希爾斯法城內、猶拉什三層＋邊界、散提爾堡三段、眼魔洞穴與 Dexam）。
一段紅了就 `t.FailNow()`，不讓後面的段拿著壞掉的狀態跑出一串假故障。

★ 「覆蓋的步數不減」是**量出來的**，不是宣稱的：把新舊兩份正規化之後逐行比對
（去縮排、去 `t.Run` 包裝、把提前宣告的變數還原成 `:=`），差異只有開 zip 的
那一段搬到函式開頭、四個跨段變數改成先宣告後指派、12 個閉包的右大括號。
**沒有任何一個遊戲動作被加進來或拿掉。**

每段結束 `SavePartyFile` 存一份，最後一個 subtest 整批驗往返（block、模式、
章節／地城、隊伍人數）。12 份全過——這是 `SEG-11` 缺的那一半：先前只驗得到段的
入口，現在段的**結束**狀態也存得下去讀得回來。

⚠ 快照**不進 repo**：把 12 份存檔當固定 fixture 收進版控，存檔格式一改整批失效，
而分段診斷與快照往返兩項好處已經拿到，不需要付那個代價。

★ 順帶推翻一條自己前兩輪寫下的斷言。`SEG-03` 由「`DIMSWART THE SAGE` 這個字串
在 `ECL4/0x22`」推出「`beholder-cave` 的 `script_block 0x22` 指到別段的劇本」。
實機量過：**眼魔洞穴與 Dexam 跑的時候 `CurrentBlockID()` 就是 `0x22`**，而
迪姆斯華特那場戲跑在 `0x21`。他入隊之後在 `0x22` 還有台詞，所以字串在那裡很合理。
**字串出現在哪個 block ≠ 那場戲屬於哪個 block**——一個 block 裝得下不只一場戲。
相關正文已改寫成正確答案。未解的是幾何怎麼換成 `GEO4/0x25`（`0x22` 的
`LOAD FILES` 是 `21`，remake 目前用 game pack 事件重建）。

## 2026-08-20（第 591 輪）：主線接到第六章

連續 session 從眼魔洞穴接到密斯卓諾遺跡入口。路線是
散提爾堡 → 猶拉什 → 希爾斯法 → 立石群 → 密斯卓諾——散提爾堡的世界圖鄰居
只有帖許瓦／猶拉什／費蘭，接不到立石群，得繞。

★ 路上缺的是立石群那一場戲，而它是**變數插入頁**：灰袍男子說「你們目前仍服侍
`<n>` 位主人。再除掉其中 `<n−1>` 位後回來找我」，數字是執行期插進同一頁的。
game pack 原本只宣告了「四位／三位」那一種，所以這條路徑走到這裡會把原文英文
印給玩家。補上「兩位／一位」那一種。⚠ 其餘數字**不補**——沒有走到過的變體是
猜測，走到哪一種就補哪一種。

新增三段 subtest（立石群灰袍男子、密斯卓諾世界路線、密斯卓諾墓園），段界快照
從 12 份增加到 15 份，全部往返得回來。

整條跑改名 `TestRealNewGameRunsToTheEnding`：
**名字裡的終點是斷言**，走得比它遠就要改名。

密斯卓諾**段內**（紅網、盜墓者、公主祝福、內城遺跡、最終戰）仍只有各自的
直入旗標，還沒接進連續 session。

⚠ 墓園**段內**還走不通：出生點是 `(2,15)`，而紅網那一格是 `(6,14)`，
但 `(3,14)` 往東是牆——`x≥4` 的 `y=14` 走廊接不到出生點那一側，`y=13` 走廊
與它之間也全是牆。走到 `(3,14)` 還會觸發世界地圖邊界，隊伍直接被送出遺跡。
通行性量在報告 §四，下一輪從那裡接。

★ 更正：擋路的不是牆，是**事件**。`x=3` 那一整欄（地形 `0x01`）是墓園的離場格，
踩上去隊伍會被送回世界地圖；往東繞 `y=12` 那一列就通了
（`(2,15) → (2,13) → (2,12) → (6,12) → (6,14)`），拿得到紅網那一場。
第一版自製的判牆規則與引擎自己的 `CanMoveDungeon` 對不上，才把事件誤讀成牆——
**判通不通要用引擎自己的判定式**。

紅網四個選項全部已中文化。⚠ `SPEAK` 不是解法（說通關語只會讓網更亮），
原作的解法是 `HACK IT`：紅光消退、露出金屬套索、蜘蛛出現，打完回到 `(6,14)`。
段測試 17 段、段界快照 17 份。

黛米爾公主那一場也接上了。★ **幽魂是「走進那一格」才出現的**：站在 `(13,14)`
跑生命週期不會觸發，`MoveDungeon` 走進去才會。分段驗收時「站到定位再跑一次」
與「走進去」不是同一件事。段測試 18 段、段界快照 18 份。

⚠ 往外城遺跡（`0x42`）的入口還沒找到：離開墓園回世界地圖再進一次，載回來的
還是 `0x40`。墓園也不是連通的一整片（`(12,12)` 走不到 `(12,10)`），逐格試探
成本很高。地形碼已經全圖量下來（報告 §五），下一輪改成**讀 block `0x40` 的
ECL** 找 `NEWECL 0x42` 的守衛條件，不要再在地圖上撞。

## 2026-08-20（第 592 輪）：開場到結局的完整主線跑通了

同一條新遊戲 session 從開場走到擊敗提朗瑟克斯的結局選單，23 個段 subtest
（`TestRealNewGameRunsToTheEnding`）。最終戰的戰場上 38 名戰鬥員，隊伍打贏，
畫面停在「全隊已恢復，是否在結束前保存？」，選存檔之後回到標題模式。

★ **關鍵是換方法：讀 bytecode，不要在地圖上撞。** 墓園往外城遺跡的入口卡了
好幾輪——那張圖不是連通的一整片，走錯一步就被送回世界地圖，而每次試探都要
重跑十幾秒的 session。新工具 `cmd/ecl-window`（印某條指令前後的指令窗、
找跳進某個位移的來源、依打包文字搜尋）一上手，三個轉移幾分鐘內全部定位：

| 轉移 | 守衛 |
|---|---|
| 墓園 → 外城遺跡 | `ON GOTO C04D`（面向÷2，0 起算）：只有**朝東**跨出邊界 |
| 外城 → 內城遺跡 | `ON GOTO C04D` 索引 0（**朝北**），答兩次「要繼續嗎」 |
| 內城的每格事件 | `ON GOTO (C04F & 0x7F)`：**地形碼**分派。樓梯 `0x97`＝(10,7)，最終戰 `0x9A`＝(6,1) |

★ 三件「少了就走不到」的事：**第六章的怪物表沒載，最終戰不會開打**
（不是報錯，是「戰鬥開始！」之後直接回地城）；**黛米爾公主是走進那一格才出現**
（站在上面跑生命週期不會觸發）；**紅網的解法是 HACK IT 不是 SPEAK**。

整條跑改名 `TestRealNewGameRunsToTheEnding`。

## 2026-08-20（第 593 輪）：階段 3 的跨段不變量

`SEG-32` 語系：`normalCampaignObserver` 記下整條 session 每一句玩家看得到的話，
跑完逐句判有沒有漢字。第一次跑抓到 **12 句英文**（世界地圖的紅羽衛巡邏隊放行、
猶拉什北方哨卡、散提爾堡巡邏隊宣讀死刑判決、八句街頭傳聞），全部補進 game pack。
現在 233 句落回原文 0 句。閘門同時擋「記到的句子太少」——低於 100 直接紅。

★ 兩個寫文字規則會踩的坑：**跨分支的 `all_contains` 在實機接不上**
（紅羽衛那一句的第一句與「戰區」那句分屬放行與開打兩條路徑，永遠不會在同一份
`MatchText` 輸入裡）；**`MatchText` 回傳清單裡第一個命中的規則**，所以廣義規則
一律排在具體規則之後。

`SEG-31` 隊伍連續性：比**逐個角色**的經驗值，不比總和。
★ 第一版比總和，於是艾莉亞絲與龍餌離隊時「54,723 掉到 2,001」被判成故障——
那是同伴帶著自己的經驗走了。**跨段比較要比同一個主體。**
隊伍變動的三個位置寫成宣告，多一處少一處都會紅。

`SEG-33` 音樂：22 個段界都有一首在 game pack 曲目表裡的曲子，沒有一段靜音。
⚠ 只驗「有曲子且查得到」，沒有比對哪一段該播哪一首。

★ 順帶查清一件事：**迪姆斯沃特不入隊是對的**。這條路線在「要讓迪姆斯沃特
同行嗎？」答 YES，而名冊始終沒有他。原作那一段（`ECL4/0x21` 位移 `0x0560`）
答應之後只做 `SAVE 0x01, 4C01`——設旗標，沒有 `ADD NPC`，他不佔隊伍格。

## 2026-08-20（第 594 輪）：內城一樓九間房，與一個安靜的錯

用 `cmd/ecl-window` 的 `-table` 把 `ECL6/0x43` 的 `ON GOTO (C04F & 0x7F)` 拆開，
27 個索引逐個對上「那裡的第一句文字」；再拿 `cmd/geo-probe` 的地形碼反查格子，
就得到一張「哪一格演哪一場」的完整對照。內城一樓九間房照這張表逐間走過。

★ **六間出得來，三間安靜**——廚房、冥想中的祭司、禮拜中的祭司。不是走錯格：
位置與地形碼都對。原因是它們的守衛是 `COMPARE AND 4C06,0 4C07,0`，而走到那裡時
`4C06=0x0A`、`4C07=0x80` 都是**前面幾張地圖**留下的（`4C07=0x80` 是眼魔洞穴寫的）。
`4C00` 起的那一段在原作是 map-local bank，remake 目前只有一份全域的。
`4C03` 先前踩過同一件事，當時記成 `strong inference`；這是第二個獨立案例。
寫成 spec 1142。

★ 這種錯**不會自己現形**：沒有崩潰、沒有錯字，只是三間房不演，看起來像本來
就沒內容。測試把三間標成 `suppressed`——現在出不來是預期的，哪天出得來會紅。

⚠ 順帶一個查詢回空的老坑：`geo-probe` 印的是**大寫**十六進位（`0x9A`），
用小寫 grep 找 `0x9a` 會一條都找不到，而那正好是最終戰那一格。

二樓五間房也照同一張表走過：防腐室、書庫、魔法陣出得來，停屍架與食品儲藏室
被同一個成因壓掉（`4C0F=0x07`／`4C0E=0x6A`、`4C0B=1`，這條 session 根本沒進過
那兩間）。spec 1142 的表補成五間。整條主線 247 句，落回原文 0 句。

★ 修掉一個會讓房間「看起來沒內容」的量測錯：`resolveInnerRoom` 原本先檢查
`Mode != ModeDungeon` 才進迴圈，於是**不會停下來等玩家的敘述**一句都沒被記到。
先 `observe()` 一次再進迴圈，防腐室／書庫／魔法陣三間立刻現形。
**判斷「這裡沒事件」之前，要先確認自己的記錄點抓得到不暫停的事件。**

## 2026-08-20（第 595 輪）：墓園逐格內容盤點，以及語系閘門漏掉的一條路徑

墓園每一格的事件同樣由地形碼分派。用 `cmd/ecl-window -table` 把 `ECL6/0x40` 的
23 個索引對上文字，再拿地形碼反查格子，新增
`TestBurialGlenCellEventsAreLocalized`：**15 格逐格站上去跑一次生命週期**，
驗那一格的敘述是 game pack 的中文。

⚠ 每一格都**重新進段**——once-only 旗標與戰鬥結果會互相污染，接續著跑會讓後面
的格子看起來沒內容。第一版把這個掃描塞進連續 session，結果把內城一樓的房間
測試弄紅了；拆成獨立測試才乾淨。

★ 盜墓者那一格是 `RANDOM 0x63 / COMPARE 0x20 / IF >` 擋著（100 抽 32），
站上去一次不一定演。測試改成換種子重進段最多 24 次——**這不是放寬條件，
是原作本來就有機率**。

★★ 語系閘門原本只記 `state.Message`，於是**遭遇選單的敘述一句都沒被檢查**：
`ENCOUNTER MENU` 的敘述在 remake 走的是 `Prompt` 那一行。把 `Prompt` 也記進去，
又抓到 6 句英文（瓦礫堆裡翻找的人、蟲群／巡邏隊盯著你們、火蜥蜴靠過來查看、
兩句「你們要怎麼做？」）。補完之後整條主線 260 句，落回原文 0 句。
**閘門要涵蓋每一條玩家看得到的字的路徑，少記一條，那條路上的英文就永遠不會紅。**
