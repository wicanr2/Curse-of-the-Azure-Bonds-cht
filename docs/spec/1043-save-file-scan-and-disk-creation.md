# 1043 — 存檔檔案掃描與建立存檔磁片：副檔名就是格式版本

- 證據等級：`exact`（PC-98 `overlay-16:00614h` 205 條、`overlay-16:008E4h` 202 條，
  兩支各自逐條讀完）
- 作法見 spec 783

## `pc98 overlay-16:00614h`（`retf 8`）——依類別掃檔

```pascal
arg_0^ := NIL;  arg_4^ := NIL;                { 兩個輸出 }
case DS:0A816h of
  0: 樣式 := '*.guy';
  1: 樣式 := '*.cha';
  2: 樣式 := '*.sav';
  3: 樣式 := '*.hil';
end;
<sub_164h>(表A[類別], 表B[類別], 表C[類別], arg_4, arg_0, bp);
```

★ `DS:0A816h` 選副檔名，同時決定三張平行表的索引：

| 表 | 基底 | 寬度 | 靜態內容（索引 0..3） |
|---|---|---|---|
| A | `DS:0BFCh` | byte | `00 00 04 F7` |
| B | `DS:0BFFh` | word | `00F7h`(247)、`0084h`(132)、`0013h`(19)、`01A7h`(**423**) |
| C | `DS:0C05h` | word | **`01A7h`(423)、`011Dh`(285)、`00BCh`(188)**、`0000h` |

> ★★★ **表 C 的前三個值 423／285／188 正是 spec 1038 的三種角色記錄長度。**
> 對上副檔名就是：
>
> | 副檔名 | 記錄長度 | ＝ spec 1038 的 |
> |---|---|---|
> | `.guy` | **423**（PC-98 現行） | `DS:7584h = 0` |
> | `.cha` | **285** | `DS:7584h = 1` |
> | `.sav` | **188** | `DS:7584h = 2` |
> | `.hil` | 0 | —（不是角色檔） |
>
> ★★★ **關掉 spec 1038 的未定項「`DS:7584h` 由誰設定、怎麼判斷舊存檔的版本」：
> 版本就是副檔名。** 掃到 `.cha` 就走 285 的轉換路徑、`.sav` 走 188。
>
> ⚠ 三張表的位址區間互相重疊（`0BFFh`..`0C0Ah` 與 `0C05h`..`0C0Ch`），
> 本規格只照組語列出基底與位元組，不宣稱它們是不是同一筆記錄的欄位。

★ `.sav` 與 `.hil` 是 spec 1038 沒看到的兩個副檔名。

## `pc98 overlay-16:008E4h`（`retf`）——建立存檔磁片

```pascal
repeat
    Move(DS:0C0Ch, DS:0A335h, 0Ah);           { 熱鍵塊 }
    選項行 := '   はい                       いいえ    ';   { 40 bytes }
    DS:0A385h := 0Ah;  DS:0A386h := 0Ah;
    提示 := 'セーブディスクを作成しますか？ ';              { 31 }
    鍵 := <overlay-26 entry#5>(…);
until 鍵 in ['A', 'J', #0];
if 鍵 <> 'A' then exit;
顯示('セーブディスクを作成中です。');                       { 28 }
if <sub_5588h>() = 1 then begin
    顯示('セーブディスクの作成に失敗しました。リターン・キーを押してください。'); { 68 }
    exit
end;
…把檔案抄過去…
```

## ★★★ 第四組 Yes／No 熱鍵

```
DS:0C0Ch = 00 00 00 00 00 41 00 00 00 4A
```

⇒ 槽 6 ＝ **`'A'`（はい）**、槽 10 ＝ **`'J'`（いいえ）**，其餘槽 `00`。

| 出處 | 是／否熱鍵 | 空槽 |
|---|---|---|
| 營地（spec 1030） | `Y`／`B` | `06` ＋ 半形空白 |
| 商店（spec 1031） | `A`／`B` | `06` ＋ 全形空白 |
| 戰利品（spec 1032） | `A`／`B` | `00` |
| **本支** | **`A`／`J`** | `00` |

> ★★★ **四個 Yes／No 提示，三種不同的熱鍵配對。**
> 中文化只能**照抄各自的常數塊**，不能統一。

★ 選項行是 **40 bytes 的整列**（「はい」在第 3 byte、「いいえ」在第 30 byte），
與 spec 1030／1031 的 80 bytes 版本不同——**兩種寬度並存**。
★ `DS:0A385h`／`DS:0A386h` 這裡都寫 `0Ah`（spec 1032 看到的是 `0A385h := 1`），
兩者都落在 10 格陣列之後，用途仍不宣稱。

## 中文化

| PC-98 | bytes | 建議中文 |
|---|---|---|
| 「セーブディスクを作成しますか？ 」 | 31 | 「要建立存檔磁片嗎？」 |
| 「セーブディスクを作成中です。」 | 28 | 「正在建立存檔磁片…」 |
| 「セーブディスクの作成に失敗しました。リターン・キーを押してください。」 | 68 | 「存檔磁片建立失敗。請按 Enter 鍵。」 |
| 「   はい                       いいえ    」 | 40 | ⚠ 整列 40 bytes，兩個詞的位置固定 |
| `'*.guy'`／`'*.cha'`／`'*.sav'`／`'*.hil'` | 5 | ⚠ **萬用字元 ＋ 副檔名，不可翻** |

## 明確不宣稱

- 沒有宣稱表 A（`DS:0BFCh`，`00 00 04 F7`）與表 B（247／132／19／423）的用途。
- 沒有宣稱 `.hil` 是什麼檔（記錄長度那一格是 0）。
- 沒有宣稱 `sub_164h`（實際掃目錄的那支）與 `sub_5588h`（格式化／複製）做什麼。
- 沒有宣稱 `DS:0A385h`／`0A386h` 的用途。
- 沒有宣稱 DOS 側有沒有對應的四個副檔名分派（本次只讀 PC-98）。
