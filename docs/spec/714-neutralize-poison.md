# 第七百一十四輪：解毒，以及效果 `37h` 的身分

狀態：`READY`。等級：`exact`（流程）／`strong inference`（`37h` ＝ 中毒）。
日期：2026-08-14
位置：DOS `overlay-22:4194h`。

## 流程

```text
p := DS:7435h
if p^[195h] = 1 then
    DS:7435h := 0；DS:7437h := 0                  ← 只清目標陣列第 1 格，不動筆數
elif <overlay-24 entry#27>(p, 37h, @node) then
    if p^[1A4h] = 0 then p^[1A4h] := 1            ← HP 為 0 補成 1
    DS:6F9Ch := 1
    <overlay-23 entry#3>(p, 37h, 0)
    <overlay-23 entry#3>(p, 16h, 0)
    <overlay-23 entry#3>(p, 0Fh, 0)
    DS:6F9Ch := 0
    <overlay-24 entry#20>(p, '<名字> is unpoisoned'（CS:4178h）, 0Ah, 1)
    p^[196h] := 1
    p^[195h] := 0                                  ← 狀態歸 0
else
    <overlay-24 entry#20>(p, '<名字> is unaffected'（CS:4186h）, 0Ah, 1)
```

## `37h` ＝ 中毒

同一個編號在三個地方出現，串起來就定了：

| 出處 | 用法 |
|---|---|
| 本輪 `4194h` | 有 `37h` 才解，訊息 `is unpoisoned` |
| `44D3h`（spec 707，復活） | 復活時連同 `20h` 一起解除 |
| `24CDh`（spec 709） | 有 `37h` 才套效果 `4Eh` |

復活會解毒，是因為毒可以致死；`24CDh` 那條則是對中毒者做別的事。三處一致。

解毒時**一次解掉三個編號**（`37h`、`16h`、`0Fh`），和 spec 704 `2FD1h` 的「查一個
解另外幾個」是同一種寫死的對應。remake 只解 `37h` 會留下另外兩個。

## 與 `24CDh` 的差異

兩支的前兩步完全一樣（狀態 1 就清目標、否則查 `37h`、HP 為 0 補 1），之後分岔：

| | `4194h` | `24CDh`（spec 709） |
|---|---|---|
| 解除 | `37h`、`16h`、`0Fh` | 不解 |
| 套用 | 無 | 效果 `4Eh` |
| 狀態 | `+195h := 0`、`+196h := 1` | 不動 |
| 訊息 | `is unpoisoned` | `is affected` |
| 查不到時 | `is unaffected` | 靜默返回 |

**查不到 `37h` 時一個有訊息、一個沒有**——這種差異在對照兩支「看起來一樣」的
函式時最容易被抹平。

## 明確不宣稱

- 效果編號 `16h`、`0Fh`、`20h`、`4Eh` 各代表什麼。
- `DS:6F9Ch` 的語意（解除效果期間立起，spec 704／707 同）。
- `+196h` 是什麼。
