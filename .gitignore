# 原始研究素材（僅本地使用，避免把商業遊戲映像與掃描手冊推送到 repo）
*.zip
*.rar
*.pdf
/pc98/
/dist/

# 原版 PC-98 商業配樂的本地 OGG render；可供本機 remake 播放，但未確認公開散布權。
/assets/audio/pc98-bgm-selector-*.ogg

# 本地解包與分析輸出
workplace/original/
workplace/extracted/
workplace/output/
*.pyc
__pycache__/
*.test

# 獨立 Git history；不可誤加為本 repo 的 gitlink
/golden-box-remake-engine/

# IDA／Go／DOSBox 的本地工作產物；.git 目錄本身也在這裡
/workplace/

# `go build ./cmd/<名字>` 會把執行檔丟在 repo 根目錄，檔名就是 cmd 目錄名。
#
# ⚠ **這裡不逐一列名字。** 原本是一條一條列 `/coab-audit`、`/geo-probe`…，
# 而那份清單漏過至少六次（第 647 輪漏一個，後來 `campaign-snapshot-walk`、
# `cell-reachability`、`dungeon-walk-probe`、`ecl-sky-colours`、`segment-handoff`
# 五個執行檔共 28 MB 就這樣被 `git add -A` 收進版控）。**要記得維護的清單一定會漏**。
#
# 改成白名單：repo 根目錄只收 Markdown 與 Go 的模組檔，其餘一律不收；
# `!/*/` 把子目錄放回來，所以 `cmd/`、`internal/`、`docs/` 照常追蹤。
# 新的 cmd 不必再改這個檔。
/*
!/*/
!/*.md
!/LICENSE
!/.gitignore
!/go.mod
!/go.sum

# ⚠ 上面的 `!/*/` 把**所有**子目錄放回來，包括前面已經擋掉的那幾個——gitignore
# 是後面的規則覆蓋前面的，所以要在這裡再擋一次，否則 `workplace/` 裡的 IDA 產物
# 與獨立 history 會整包冒出來。
/workplace/
/golden-box-remake-engine/
/pc98/
/dist/
